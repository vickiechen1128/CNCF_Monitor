# 模块目录与版本对齐总表

> 文档类型：产品需求索引
> **定位**：本文件是各模块 PRD 的**目录 + 版本对齐快照**，回答"每个模块当前到什么版本、原型是否对齐、是否可开发"。它是 prototype-designer 的固定输入（见 `.kimi/agents/prototype-designer.md` Step 3）。
> **主控关系**：产品版本的定义/规划以 [02_Product_Roadmap.md](../02_Product_Roadmap.md) §1.5 功能-版本矩阵为准；各模块版本声明以其 PRD 头部为准；本表是两者的**跨模块快照**（详见 06 Gitflow §2.5）。
> 更新日期：2026-09-15（v1.71）

---

## 1. 版本对齐总表

| 模块 | PRD 版本 | 原型版本 | 对齐 | PRD 状态 | 产品版本覆盖 | 最近合并 PR |
|------|---------|---------|------|---------|-------------|------------|
| Module_00 模块职责与集成关系 | v1.4 | N/A | —（全局索引，无需原型） | draft | MVP/v0.2/v0.3/v0.4/v1.0 | #24 |
| Module_01 监控策略与指标管理 | v3.41 | v3.41 | ✅ 对齐（v3.41 决策 69：规则挂载改为「**检查 → 提交**」两段式——独立「检查」按钮常驻、提交按钮**条件渲染**（`canSubmit = 检查通过 且（无 error 级 job 引用 或 已勾选逃生门）`）、`valid` 语义**扩展为「预检是否可提交」**（新增第三类不可覆盖硬失败「组名全局唯一性冲突」，与提交侧同实现同输入集）、逃生门提前到检查阶段、规则内容变更即作废检查结论与 ack、按钮文案统一「提交并进入变更确认」、结果面板下移到表单下方、「从本地选择 rules.yml」入口维持在编辑框上方；原型同步 v3.41——挂载抽屉两段式 + job 引用分级完整模拟 + 逃生门 + 新增 `rulesCheck.ts` 判定单测 13 条（全量 49 条通过），`package.json` 由滞留的 `3.28.0` 对齐 `3.41.0`。v3.40 决策 67：规则挂载「提交生效」需同步 error 默认阻断 + 显式覆盖逃生门，M09 失败单自动清锁；v3.38/v3.39 为规则粒度注记与 job 引用校验口径（决策 65/66），原型行为不变。v3.31~v3.35 已核对：v3.31 F-32 采集器登记三来源由「仅 internal」开放为 `official`/`third_party`/`internal`（§3.1/§5.2/§9.1/§10），PRD 头部标「原型行为不变」；v3.32~v3.35 为 §0 业务叙事层补充、产品版本影响 0。v3.30 已同步：v0.2 范围定版——克隆 Job（移出待评估）/草稿批量提交/业务健康度看板/service_discovery 挪 v0.3，filter（决策 53）/多网域扇出（决策 54）/网域覆盖表保留 v0.2，新增实例级端口覆盖 `scrape_port` 与 cAdvisor 容器监控口径；纯契约调整、原型行为不变；v3.29 已同步：coverage 三态口径修订——coverage/M07 badge 不感知 M09 下发时序、「待采集 vs 已下发未采到」细分归 M01 Job 回显；§9.1 默认模板稳定身份标签验收收紧为必须含 `resource_id`（47-3 回连键）；纯契约收紧，原型行为不变；此前 v3.28 决策 53/54 原型已对齐、评审确认回归 ready；此前 v3.27 已同步：实例采集状态回显——采集 Job 详情/编辑抽屉已选实例新增「采集状态」列 + 在线数汇总（在线/待采集/已下发未采到/未知 四态），数据源 = M02 `/api/v1/targets` 代理、只读消费（决策 47-2，20s 自动刷新 + 手动刷新，不阻断编辑/保存，异常态撑开失败原因 Tooltip）；Exporter 安装确认降级为**可选登记**（决策 47-1，`unconfirmed` 照常生成 target、纯留痕与人工背书）；v3.26 已同步：采集认证/TLS 最小集——ScrapeJob 新增 `auth_type`（none/basic/bearer）＋`username`/`password` 或 `token`、`tls_skip_verify`、`ca_file`，表单「认证与 TLS」折叠区；冻结（禁用）网域禁止新建 Job、存量 Job 禁止新增该域实例；`change_status=deployed` 回写契约；v3.25 offline 排除提级 P0——决策 29：实例候选集中 `Resource.status=offline` 实例**显示但置灰不可选**（Transfer 数据项 disabled，标题/描述标注「已下线」）、已选实例转 offline 后 M09 配置生成跳过（offline 后下一生成周期即从 targets 移除），§3.1/§5.4/§8/§9 同步，新增 default 域 mysql 下线副本 res-mw-005 演示；v3.24 规则文件挂载 MVP——决策 38-1：规则编辑页新增「文件挂载」视图，上传/粘贴整文件 rules.yml（content_mode=yaml_passthrough + rule_content）落库，保存/启停/删除后由 M09 生成 rules.yml → 变更单人工确认 → 下发、change_status 全链路回写；基础 YAML 校验 + 规则条数启发式展示；「字段化编辑」为 v0.3 预览（structured）；手写规则不再绕过配置中心） | ready（可开发版本） | MVP/v0.2/v0.3/v1.0 | #24 |
| Module_02 查询中心 | v1.15 | v1.7 | ⚠️ 原型待升级（v1.15 决策 68-5：**租户标签键定版**——§7.1 租户行由 `tenant_id` 修正为 **`tenant`**（来源更正为 **M07 LabelTemplate target 级注入**，M09 `external_labels` 不承担）、移除原「遗留待决」注记改为定版；§7.2 第 1 条 matcher 同步为 `tenant=`（硬隔离语义不变：永远存在、用户不可见不可改）+ 补 **fail-closed 严格派**（无 `tenant` 标签 = 普通租户不可见；平台自身 Job 显式 `tenant="platform_admin"`；否决 `{tenant=~"..."}` 共享派）+ v0.2 生成期门禁与同源常量 `models.TenantLabelKey`；新增**通用命名规约**「标签键一律不带 `_id` 后缀，`_id` 只属 DB 列 / API 字段」；§1/§3/§6/§11/§12/§13 注入名全文校正。v1.14 决策 68-1：§7.1「注入标签 key 契约」补权威口径注记——`network_domain` 为全平台唯一 Prometheus 标签键（M09 侧 `external_labels` 键名由 `network_domain_id` 收敛，决策 19 键名 supersede）、消费侧三级解析为过渡层常驻；**并新增「遗留待决」发现**——§7.1 `tenant_id` 行来源标注自 2026-08-19 决策 19 起失效（租户标签改由 M07 target 级 `tenant` 注入），而 §7.2 注入 `tenant_id` matcher，序列标签名与注入名不一致，MVP 骨架恒通过无影响、v0.2 多租户定版前必须统一（本轮仅记录不擅改）。v1.13 为 Track B+ 轻量增量：新增 `/api/v1/alerts/history`（MVP）——基于 Prometheus `ALERTS` 序列重建规则级触发/恢复区间，供 M08 独立「历史告警」页消费，免高保真原型、豁免记录见 design-decisions；PRD 头部标「未对齐，待按 v1.10 修订」；v1.9 新增 §0、v1.10 §0 深化、v1.11 §0 结构优化，原型 v1.7 未同步。v1.8 已同步：coverage 三态判定口径修订——`/api/v1/health/coverage` 不感知 M09 下发时序，选中关系取 DB 当前 `selected_instance_ids`（ready+enabled、不问 `change_status`），选中未采到统一归 `pending_down`（含未下发），「待采集」细分归 M01 回显；纯契约声明，原型行为不变；此前 v1.7 决策 55/56/57 原型已对齐、评审确认回归 ready；告警状态页归 M08、M02 仅交付注入代理 API；`network_domain` 注入语义明确为**授权集合收敛**（授权=全部网域时不注入任何 matcher，跨网域业务聚合天然成立；前端筛选纯 UX 不承担安全）；中心存储预留替换 VictoriaMetrics 决策点（隔离契约保持标签制）；设计思路见 docs/05-execution-records/module-02/m02-vs-m08-boundary-and-injection-design.md；此前 v1.6 决策 51/52 交叉待原型对齐——可视化三层归属（决策 51：自研指标查询=M02、Grafana iframe 嵌入=M05、告警规则=M08）与跨网域看板/blackbox 网域语义（决策 52）落 PRD；v1.5 已同步四类 MVP 欠债项契约并落版原型：PromQL 代理含**租户/网域注入骨架**（MVP 恒 default 网域 + platform_admin 租户、注入 key `network_domain`/`tenant_id` 与 M09 external_labels 对齐）；`/api/v1/targets` 代理字段与按网域/Job/health 筛选即 M01 Job 回显与 M07 badge 共同数据源；采集健康度/覆盖率查询 API 三态提前 MVP（决策 47-3）；**响应 envelope MVP 最小口径**——`data_source` 恒 `central_scrape`、`network_domains` 恒 `["default"]`、`freshness_at` 取最新样本时间戳，决策 50/v1.5；独立目标状态页仍 P1 极简全局排障（决策 47-4）；可视化不自研大屏、走 Grafana iframe 且数据源指向 M02 代理（决策 50）；≤v1.4 覆盖 P1 标注与 coverage 提前已同步） | ready（可开发版本） | MVP/v0.2/v0.3 | #24 |
| Module_03 网关与认证 | v1.3 | v1.1 | ➖ Track B+ 免原型（决策 44） | dev-ready（2026-08-28 确认；v1.3 v0.2 范围定版——统一入口/Ingestion 路由后移 v0.3，MVP 轻量认证不变） | MVP/v0.3/v1.0 | #24 |
| Module_04 自定义服务发现 | v1.5 | v1.5 | ✅ 对齐（v1.3 五大类拆分联动、v1.4 术语分层与 `resource_type`→`resource_category` 改名、v1.5 BlueKing Business 映射改同步为 M07 业务字典；均为 v0.4+ 范围，原型已至 v1.5） | 设计中 | v0.4+ | #24 |
| Module_05 自定义前端门户 | v1.5 | v1.2.1 | ✅ 对齐（原型 v1.2.1 已按 v1.5 修订：六步指引 / Dashboard 布局 / 视觉 Token 规范 / 指标卡资产口径 + 告警卡承载最新告警列表（表头 + 单行五列 + 近 10 条）/ 「查看全部 →」深链均已落原型。v1.5 决策 72-3 首页内容重构 as-built 回填：①指标卡回归资产/治理进度 6 张（资源总数/已监控/采集 Job/已纳管网域/待确认草稿/采集覆盖率），移除「活跃告警」卡，每张右上角 ⓘ 中文口径注释、6 列网格、卡内横向排版；②告警状态卡 = 首页唯一告警入口——主数字「通知中」+ 状态分布 + 最新 10 条告警列表，标题右侧「查看全部 →」深链、首页两块列表均不放分页器；③放宽「零后端改动」约束——`dashboard/summary` 新增 `monitored_count`/`scrape_job_count`/`scrape_job_enabled_count`（已监控口径：被 ≥1 个 `enabled=true` 且 `draft_status='ready'` 且 `job_type=standard` 的 Job 覆盖的资源数）；④`unprocessed` 不计数不进列表、页面不出现「待处理」。v1.4 决策 72-1/72-2：使用指引扩为六步（+配置告警通知）+ 自定义轻量 QueryPage（不复用 Prometheus UI）+ Dashboard 布局 + 视觉 Token 规范；v1.3 决策 72 首页 MVP 子集（快速入口 + 使用指引 + 告警状态数字，零后端改动）。历史：v1.2 决策 51 Grafana 集成三层归属与导航层级） | dev-ready（Track B 增量 v1.5，2026-09-14） | MVP/v0.3/v1.0 | #24 |
| Module_06 租户与平台管理 | v2.15 | v2.15 | ✅ 对齐（v2.15 决策 82 网域生命周期闭环——回收路径修正 + 禁用/纳管联动具象化（**原型已同步 v2.15.0**，见 `docs/prototypes/module-06/README.md` v2.15 变更说明）：①**删除约束修正**——硬拒绝条件收敛为「存在 M07 资源引用」单一条件，「已纳管 EdgeAgent」由硬拒绝改为**级联清退**（已纳管网域删除入口恢复可用 + 「删除网域（退场回收）」二次确认弹窗展示级联影响清单：N 个采集节点将断连 / 凭据废止 / 停止配置下发 / 节点侧无需线下操作、心跳鉴权失败自动转离线 / 既有记录保留供审计 / 软删不可自助恢复；有 M07 资源引用时删除被拒并提示先迁移或删除资源）；②**禁用弹窗补强**——已纳管且存在在线 Agent 时，在「影响范围」（读真实计数）之外追加「【重要】禁用为行政冻结……已接入的采集节点不会自动停止采集」固定提示 + 引导「停止采集并回收用删除 / 仅停采保留网域用 v0.2+ 退纳管」；③退纳管动作定义（v0.2+，决策 62 口径① 具象化）；④**三动作语义边界定版**——禁用 = 行政冻结 / 退纳管 = 停止监控 / 删除 = 退场回收，三者正交不可互替；⑤「更多」菜单不可操作原因修正（已纳管不再是拒绝理由，仅剩「系统预置网域：不可禁用 / 删除」与「存在资源引用（N 条），请先迁移或删除资源」）+ mock 新增 `resource_ref_count` 字段覆盖三类删除场景；落点 §3.2/§4/§6.2/§8/§9/§11.2；原型 `package.json` 2.14.0→2.15.0，build / lint / check:prototype / check:notes 全绿 + headless Chrome 截图走查 10 状态）。v2.14 决策 80 前端设计优化（chenrt「按专业前端设计」返工，**对齐《前端标准》§8 交互选型表 / §9 列表与长文本规范**）：①页首引导条扁平化（Alert + 嵌套 Collapse → 单块扁平容器：一句定义 + 双路由卡「能直连 / 够不到」+ 接入四步常显；原三行 ASCII 拓扑改由双路由卡承载、原图移入抽屉概念详解；收起记 localStorage）；②登记 / 编辑表单 **Modal → Drawer 720px**（字段 9 项 > 6，按 §8）+ 四组分区（是否需要登记 / 行政信息 / 授权与分区 / 网络与描述）；③登记前置判断**裸 Radio → 双选择卡** + 新增**判断口径**块（以中心可达性为准、与人工运维可达性无关；**VPC 经对等连接 / 云企业网（CEN）/ 专线·VPN 打通后属「能直接访问」无需登记**；典型需登记场景修正为独立 K8s overlay 集群 / 物理或逻辑隔离网段 / 未向中心开放入站的托管网络）；④抽屉内身份说明与「什么是网域？」详解**合并为同一提示容器**、就地展开（消除两种提示风格拼盘，全页提示统一为 Callout 单一组件）；⑤**列表列数治理 11 → 8 列**（网域名称 / 接入方式 / 接入进度 / 状态 / 登记归属 / 授权租户 / 网络分区 / 操作），网域 ID、授权租户全量、网段（CIDR）、描述、创建与更新时间**下沉详情 Drawer**，授权租户行内最多 2 标签 + 「+N」折叠；⑥接入进度列**移除「下一步：…」灰字旁注**，四态改点阵 + 当前态文字（四步明细入悬浮），行内主操作与四态一一对应且为全行唯一主按钮（已出数据 → 「查看采集任务」降次级）；⑦状态列改 **Badge + 文字**（禁用补「冻结」语义），启用 / 禁用 / 删除收进「更多」菜单、系统预置与已纳管场景在菜单内直给不可操作原因，主行仅保留 主操作 + 编辑 + 更多。原型同步 v2.14；M09 无落点。v2.13 决策 79：前置判断选「能」改**硬劝阻终点**（删除「或登记为中心直连区」出口——管理域唯一性 / M09 通道矛盾 / 语义互斥，独立授权走「授权租户」字段）；术语再修订 **中心直连域 / 采集节点域**（决策 74 术语表更新，枚举不变）；引导信息归位——概念卡 + 可折叠「网域接入指引」合并为一个可折叠面板两区块（什么是网域 + 怎么接入四步），抽屉移除 3 步 Alert、改为身份说明「你正在登记一个采集节点域」+「什么是网域？」折叠入口，登记域类型只读显示「采集节点域」，行动卡措辞对齐四步；原型同步 v2.13。v2.12 决策 78：接入进度四态 = **聚合视图**而非新状态机（已纳管=M09 纳管状态 / 采集节点已上线=M09 运行状态 online / 已出数据=M01 生效配置聚合），数据流向 = M06 拉 M09/M01，M09 不反向聚合、保持原子列；§11 注记落版，纯文档、原型行为不变。2026-09-14 原型 v2.5→v2.11 同步决策 73~76：①页首概念卡（一句话定义 +「中心能否直连」二选一判断 + 三行拓扑示意，可关闭记住 localStorage）+ 提问式空态 + 新建表单前置判断问题 + 登记成功行动卡（MVP 四件套）；②「监控纳管」二态列 →「接入进度」四态列（v0.2：mock 新增 agent_online/has_data 聚合字段与 accessStepOf 推导，mc-manufacturing / mc-dmz 覆盖四态演示，行内主操作 = 下一步动作——去纳管 / 查看安装指引深链 M09 #/node-status / 去配置采集跳 M01 #/scrape-jobs 预选网域）；③术语降噪落页（中心直连区 / 隔离区（采集节点接入）/ 采集节点 / 接入方式 / 网络分区（可选），模型枚举与 mock 字段名不变）；④zone_type 语义澄清进表单 extra 与选项说明（纯分类标签不影响行为）。决策 75 的 M09 配套（一键复制安装命令）见 Module_09 行、仍待对齐。历史：v2.11 决策 73~76：网域概念用户化与接入引导——①网域 = 网络可达性区域（一句话定义 +「中心能否直连」二选一判断规则，通用化覆盖单域多集群与多域；登记不采集数据，数据从采集节点上线开始流）；②术语降噪（中心直连区 / 隔离区（采集节点接入）/ 采集节点 / 网络分区（可选），模型枚举不变）；③M06 页引导四件套（页首概念卡 + 提问式空态 / 新建表单前置判断问题 / 接入进度四态列 v0.2 / 登记成功行动卡）；④zone_type 语义澄清（纯分类标签不影响行为，政务云 = 安全分区 / 公有云 = region）；原型待对齐概念卡 / 空态 / 前置判断 / 行动卡 / 术语。v2.10 决策 68-5：§2 用户故事表「多租户数据隔离」注入名 `tenant_id` → **`tenant`**（标签键不带 `_id` 后缀；租户标签唯一来源 = M07 target 级注入），纯契约校正、原型行为不变。v2.7~v2.9 已核对：均为 §0「需求背景与典型场景」业务叙事层补充、产品版本影响 0，原型行为不变。v2.6 已同步：v0.2 范围定版——`ip_cidrs` 后移 v0.3（租户数据模型/租户-网域关联保留 v0.2）、新增划域指导原则（K8s overlay 独立建域、VPC 原生 CNI 可并入）；纯文档调整、原型行为不变；v2.5 已同步决策 52：NetworkDomain 新增 `ip_cidrs` 网段字段——供 M07 资源导入/CMDB 同步时按 IP 推导网域归属（解码链第③级，最长前缀优先、同前缀跨网域判歧义）、纯平台侧数据不回写 CMDB、可由 M07「待分配队列」规则化动作一键生成；网域心智原则落页——「接入可见、消费隐藏」：接入侧（导入/录入/同步）推导归属、消费侧（M02 查询/M05 看板/M08 告警路由/M01 采集）默认不感知网域，权限注入 + 可选下钻，不做 CMDB 写回；此前 v1.7~v2.3 已同步：网域登记纳入 MVP、租户与业务解耦、网域可跨租户共享（决策 18~20：登记归属固定 platform_admin + 授权租户 authorized_tenant_ids、ID 部署级前缀 `deploy_code-domain_code` 默认 `mc`、`default` 管理域无前缀、multi_site_enabled 改为「被授权使用多个网域」）、业务字典术语 `biz_code` / `biz_name`（决策 21~22）、列表筛选补齐 §11.1、MVP 缺憾补漏（决策 23：zone-types 只读接口 / 禁用冻结语义 / 空网域删除 / 种子数据 migration upsert / 登记归属创建后不可变更 / 无鉴权风险声明 / deploy_code 前缀 / authorized_tenant_ids 可选缺省）、两段式评审返工（R4 行内「配置纳管（M09）」跳转 Module_09 预选该网域；R5 禁用=冻结传导 M01/M09 影响告知）） | dev-ready（Track B 增量 v2.3，2026-08-28 确认；≤v2.2 为 ready） | MVP/v0.2/v0.4/v1.0 | #24 |
| Module_07 监控对象管理 | v2.33 | v2.33 | ✅ 对齐（v2.33 决策 81 原型已同步——**资源新增双入口**：「登记主机」→ host（OS 层 node_exporter `:9100`，K8s 节点主机 OS 同走此入口）/「登记 K8s 集群」→ generic_target 集群级端点专属表单（API Server 6443 / kube-state-metrics 8080 / etcd 2379 预设联动、集群名必填，明示节点 / Pod / 容器由该域 kubernetes_sd Job 动态发现、无需逐台登记），两入口语义互斥并各带链路说明；**网域字段可达性引导**：问「这台机器的采集端口从哪条链路够得着？」、下拉选项第二行标注中心直连 / 采集节点中转链路说明、`instance_ip` 实时 `ip_cidrs` 推导预览三态（唯一命中显示命中网段 +「采用」、同最长前缀跨域命中〔演示：生产 / 测试集群共用 `10.244.0.0/16`〕提示人工选择、无命中提示默认兜底），预览不替代显式选择；mock 增补 `NetworkDomain.domain_type` 与生产 / 测试两集群网域、`domainReachabilityText` / `previewDomainByIP` / `K8S_ENDPOINT_PRESETS`，不改模型与接口；单测新增 7 条、build / lint / test 全过、`package.json` 2.32.0→2.33.0。K8s 双域场景的网域归属与登记动线（PRD 侧）：资源身份第一性原则（采集端点×采集路径、动态发现产物不登记、同机不拆记录、`instance_ip`=采集端点 IP）；网域=采集路径归属非资产归属，复用 M06 决策 73 用户语言；登记表单问可达性不问归属；K8s 登记动线分流（登记集群→generic_target vs 登记主机→host）；§5.16.4 补 K8s 双域操作规约与冲突告警级处理动作。不改模型与接口。此前 v2.32 已同步：①**资源列表列数治理 11 ~ 14 → 8 列**——五类 Tab 统一为「主标识（fixed left）/ 类型或身份 / 网域 / 归属来源 / 业务 / 运行状态 / 采集状态 / 操作（fixed right）」，端口 / 版本 / 操作系统 / 应用·环境·集群 / 健康检查 URL / 协议 / 端点 / 采集路径 / 自定义标签 / 数据来源 / 负责人 / CMDB 预留字段**下沉详情 Drawer**，类型字段（数据库类型 / 中间件类型 / Exporter 类型）改为随主标识行内 Tag；②**状态语义统一 Badge 语义色 + 文字**（采集状态由彩色 Tag 改 Badge）；③**操作层次化**——主操作「详情」品牌色加粗且全行唯一、次要「编辑」灰色文字、低频破坏性「删除」收进「更多」菜单（业务管理页停用 / 启用同理，`infra` 在菜单内直给不可操作原因）；④**新增 / 编辑资源抽屉 560 → 720px 并按「资源信息 / 归属与状态」分组**（字段 12 ~ 14 项 > 6 且需分组），详情抽屉 680 → 720px；⑤**全页提示统一 Callout 单块容器**（替换 5 处 Alert，新增 `components/Callout.tsx` + `calloutTones.ts`）；⑥`LabelTemplate.description` 落库（类型 + 6 模板数据 + 表单「模板说明」+ 详情展示 + 克隆保留，PRD 6.3）；⑦**五类默认模板补齐 `resource_id → resource_id`**（v2.25 遗留：host / middleware / application / generic_target 原缺失，仅 database 有）；⑧映射字段选项补 `resource_id` / `tenant_id` 并按命名规约（`target_label` 不带 `_id` 后缀）组织，新增「v0.2 内置默认 `tenant_id → tenant`」前瞻注记与就地说明（MVP 单租户不注入）；⑨**运行状态用户语言按 PRD §10 术语表收敛为「运行中 / 已停止 / 维护中」**（原 mock 误用「在线 / 离线」，与筛选器、Excel 状态映射、导入页文案三处冲突）；⑩单测 53 → 59 条（新增稳定身份映射 / 命名规约 / tenant 前瞻 / 字段选项 / description 五组断言），build / lint / check:prototype / check:notes 全过；`package.json` 2.25.0 → 2.32.0。v2.32 决策 77：K8s 集群不设第六资源类型——五大类按采集形态分类、集群为部署形态，集群诉求四归属（网络边界 → NetworkDomain 独立建域 / 分组维度 → `cluster` 字段·标签 / 发现源 → M04 KubernetesProvider / 集群健康监控 → generic_target + M01 `monitor_type=k8s`）；「集群清单」按集群视图（展示层）承接、MVP 不做；纯文档注记、原型行为不变。v2.31 决策 68-5：`tenant_id → tenant` 映射升格为**内置默认前瞻口径**——§5.11 新增命名规约（目标标签不带 `_id` 后缀）与映射必做理由（M02 硬隔离 matcher 名为 `tenant`，缺映射 = 某 Job 序列无该标签 = 按 fail-closed 严格派对普通租户**静默不可见**）；§5.12 A 表新增 `tenant_id → tenant` 行、原「可选映射」注记升级为定版说明（唯一来源 = 本映射，M09 `external_labels` 不承担）；§5.13 落「v0.2 五类默认模板内置该映射、前端默认启用」前瞻注记；平台自身基础设施指标显式 `tenant="platform_admin"`。v2.27 起 PRD 头部标「原型待对齐」：静态资源隐藏「关联实例」Tab 与 badge、`LabelTemplate.description` 必须落库（F-34/L-2）；v2.28~v2.30 为 §0 业务叙事层。v2.26 已同步：v0.2 范围定版——网域归属 IP 推导（决策 52/58）后移 v0.3、新增可选 `scrape_port` 字段（v0.2，实例级端口覆盖）；纯契约调整、原型行为不变；v2.25 已同步：coverage 三态口径修订——「已下发未采到」含变更未确认下发情形、选中关系不感知 M09 时序（细分归 M01 回显）；§5.13 五类默认模板补 `resource_id → resource_id` 稳定身份映射（47-3 coverage 回连前置）；原型 ResourcesPage 文案/提醒同步；此前 v2.24 已同步决策 52：网域归属四级解析链——显式指定 &gt; 冲突告警 &gt; 按资源 IP 与网域已登记 `ip_cidrs` 网段最长前缀推导 &gt; 默认网域兜底，Blackbox 拨测目标取发起侧（采集 Job）网域、不参与推导；「网域」字段可留空由平台按归属解析链自动推导（含 Excel 导入模板）；资源列表/详情新增「归属来源」标注（显式指定 / 冲突待处理 / 网段推导 / 默认兜底 / 发起侧指定）；此前 v2.23 已同步：采集状态三态 badge——采集中 / 已下发未采到 / 未监控，数据 = M01 选中关系 + M02 健康度聚合 API（决策 47-3，修订 31-M1，重建「未监控」筛选四选项）；业务管理页提级 MVP——业务分组字典落 DB、business_domains.yaml 仅首次启动 seed，列表 + 登记 + 受限编辑 + 停用，红线硬化（biz_code 创建后不可改 / 仅 biz_name/description/status 可编辑 / 停用不删除 / infra 禁止停用删除，决策 48）；v2.21 已同步：两段式评审返工——host 导入模板补 `instance_name` 列、导入弹窗去除 upsert/M01/M09/offline 等技术术语、`app_name` / `cluster` 改差异化必填（application/database/middleware 必填、host/generic_target 可空）；v2.20 已同步：offline 排除提级 P0——offline 后下一配置生成周期即从 targets 移除、不触发 reload；资源列表「未监控」筛选——`is_monitored` 由 M01 维护、M07 只读映射；v2.16~v2.19 已同步：is_monitored 展示移除（采集状态归 M01/M02，已由 v2.23 三态 badge 取代）、business_domain 更名 biz_code/biz_name（决策 21/22）、业务字典 MVP 运维口径（配置 business_domains.yaml 热加载 + 兜底 infra + 未登记报错给可执行指引）、导出模板由后端生成静态 xlsx 内置取值说明 sheet、upsert 不删除 + 批量下线动线、simple-agent 示例补 biz_code） | ready（可开发版本） | MVP/v0.2/v0.3/v0.4/v1.0 | #24 |
| Module_08 告警收敛与通知管理 | v1.14 | v1.7 | ⚠️ 原型待升级（v1.14 决策 68-2：**Prometheus→AM 投递接线验收补齐**——§9.1/§9.2 新增 P0「触发测试告警 → 中心 Alertmanager `/api/v2/alerts` 非空 → 告警状态页 AM 通知状态视图可见」（此前决策 59/60 只完成 AM 侧配置挂载，Prometheus→AM 投递从未接线，该视图恒空）；承载方为 M09 生成的 `alerting.alertmanagers`（仅中心 / 条件注入 / AM 地址由 `env/env.sh` 注入）。v1.13 为 Track B+ 轻量增量：新增独立「历史告警」页（MVP，含已恢复，恢复时间按 Prometheus 求值近似）——数据由 M02 `/api/v1/alerts/history` 提供，免高保真原型、豁免记录见 design-decisions；v1.8 决策 61：静默 API v1→v2 迁移，AM ≥0.27 已移除 v1 端点返回 410 Gone，须走 `/api/v2/silences`；原型 `SilencesPage.tsx` 文案已改但版本号未提；v1.9~v1.11 为 §0 业务叙事层。PRD v1.7 决策 59/60 原型已对齐，两段评审通过回归 ready——**MVP 告警分发最小闭环**：`alertmanager.yml` 文件挂载（接收人/路由/抑制低频配置，amtool 校验，同 M01 规则挂载模式）+ 静默管理极简 UI（API 直调），接收人/路由表单化 UI 挪 v0.3；**alertmanager.yml 纳入 M09 变更确认**（管理域 scope、不扇出，人工确认后 M09 下发 reload，决策 60）；Roadmap 矩阵 MVP 列已同步补齐；此前 v1.5 决策 55/56 待原型对齐——告警状态页归属本模块（M02 仅出注入代理 API）；Alertmanager 通知状态代理服务端授权集合过滤 + 静默 matcher 授权校验） | ready（可开发版本） | MVP/v0.3/v1.0 | #24 |
| Module_09 网域与边缘配置中心 | v1.72 | v1.72 | ✅ 对齐（v1.72 决策 82 跨模块落点（主记录在 module-06，**原型已同步 v1.72.0**）：①**网域删除级联清退义务**——M06 删除已纳管网域时本模块**在同一次请求内**完成清退：废止 Token / 停止配置下发 / `EdgeAgent` 记录标记 `retired`，任一环节失败整体回滚；采集节点侧无平台主动动作、Token 废止即心跳鉴权失败自然转离线；清退后该网域不出现在本页可选范围；**`retired` 归属定版**——打在**该网域的 `EdgeAgent` 采集节点记录**上（该记录的生命周期终态、保留供审计），**不是**网域 `registration_status` 取值（网域被删后本身软删、从列表消失，不进入纳管状态机）；②**新增「退纳管」动作定义**（v0.2+，决策 62 口径① 具象化：废止 Token → 停止配置下发 → `registration_status` 归位 `created`（未纳管）→ 心跳鉴权失败转离线；与 M06 禁用正交、不互相触发）；③§1 MVP 注记补决策 82 推进说明；④§8 新增 ⑤ 网域纳管状态（`registration_status`）状态机——`created`（未纳管）⇄ `monitored`（已纳管），**枚举名对齐原型代码**（初稿自造的 `unregistered` / `registered` / `retired` 已于同日勘误）+ 「三个『状态』分工」注记（M06 行政态 / 本模块纳管态 / 本模块运行态，维度不同、独立维护、不得互相推导）；⑤§9.1 新增退纳管验收（P1 / v0.2）/ §9.2 新增级联清退（P0）与「M06 禁用不改变本模块状态」（P0）两条；⑥§10 术语表补「退纳管」；**原型侧落点**——网域纳管页第 3 列「运行状态」更名「采集节点在线」+ 列头 tooltip 点明粒度（网域粒度聚合的运行态；节点与组件级诊断归「采集节点状态」页），与「纳管状态」列（配置态）职责正交、消除「冗余列」误判；退纳管入口（v0.2+）与级联清退在 MVP 无 UI 落点；`package.json` 1.71.0→1.72.0，build / lint / check:prototype / test（54）全绿；**M09 原型 README 版本缺口已闭合**（原变更说明停在 v1.52；v1.53~v1.68 期间 PRD 多次修订但**原型未改动**、原型版本轴 1.52.0 直达 1.69.0，故缺失的原型版本条目仅 **v1.69 / v1.70 / v1.71 三版**，本轮已按 module-09 design-decisions 与本节 §5 Change Log 忠实回填；并修正其陈旧描述——§原型目标 / §核心页面 原写「7 列、运行状态」，改为实际的 5 列、采集节点在线））。v1.71 决策 74（采集节点状态页两列与 M06 对齐裁定，PRD + 原型同轮落地）：①**网域列不展示接入方式 Tag**——本页实例按定义均属采集节点域（中心直连域不产生 EdgeAgent 实例），恒定信息不进列、接入方式归网域纳管页网域列（决策 70-1），同步修掉原型裸枚举 `agent_pull` 口径问题；②**配置同步列形态定版 = Badge + 成因分档**，不采用 M06「接入进度」点阵——点阵表达单调递进生命周期，本列为循环状态机 + 成因分支（「生效中」是 `out_of_sync` 成因之一而非更靠后步骤），且状态由心跳自动流转无需人工推进；表头 Tooltip 一句短定义 + 每格悬浮该档明细；③**mock 流转演示**（跨页桥，sessionStorage 持久）：配置确认页确认 agent_pull 变更 → 本页「生效中」（仅翻转该域待确认变更节点）→ 约 10s 心跳模拟自动流转「已同步」；「立即同步」结果按节点级回写桥、跨页不回退；puppeteer 自验 14/14 PASS（含流转全闭环）；落点 §3.2 节点平铺表行 + §3.2 形态定版注记 + §9 两条验收补充；原型 `package.json` 1.70.0→1.71.0。v1.70 决策 70~72 九条 PRD 显性化回写（原型先行内容追认，纯文档不改模型与契约、原型行为不变）：§3.1 网域列表 + §9 验收改 **5 列口径**（网域列三合一：名称 + 网域 ID 等宽字体 + 接入方式 Tag；接入方式并入网域列按 `domain_type` 措辞、zone_type 下沉详情 Drawer）；**命令口径改静态模板**（「复制安装命令模板」不含真实凭据、占位符由用户行内复制填值，决策 75 表述同步）+「纳管取凭据」前置不占 3 步编号 + 验收边界注记；§9 新增三条验收（详情抽屉不承载组件明细 / 摘要区三态出口 / 采集节点状态页 `network_domain` 深链）；§3.2 新增「跨页深链预筛」行。v1.69 决策 73 原型同步（2026-09-15）：`package.json` 1.52.0 → 1.70.0——①安装指引面板补 **MVP 单节点部署口径**句（一个网域只装一个采集节点即覆盖全域目标；分片 / HA / 多探测点为 v0.4+ 演化）与**中心接入地址方向性指引**句（机器如何到达中心由纳管登记地址决定，需转发先确认登记）；②纳管 / 编辑表单 `center_endpoint` / `remote_write_url` 的 extra 重写为**方向性指引**（方向均为采集节点 → 中心：直连填中心地址 / 经 nginx·网闸转发填转发侧地址 / 回传 = 中心接入地址 + `/api/v1/write` 可自动推导）；③决策 70 两条遗留处置——「编辑」补 600 字重（§8 主操作）、mock 心跳 / 配置拉取时间改**相对当前时间生成**；build / lint / test（54）/ check 全绿 + puppeteer 4 状态截图走查。决策 70~72 的 9 条 PRD 显性化回写待办不影响行为对齐。v1.69 决策 73（网域-采集节点部署口径 + 两类地址用户指引，纯文档不改模型）：①**MVP 单节点部署口径**——一个网域在网域内挑一台常开机器安装**一个**采集节点即完成接入（域内互通、一台覆盖全域目标），安装指引不引导多节点部署；同域多采集节点（分片 / HA / 多探测点）维持 v0.4+ 演化，驱动力为容量 / 可用性 / 拨测语义而非连通性，`EdgeAgent` 模型天然 1:N、MVP 以单通道单采集组约束保证 1:1；②**两类地址方向性指引**——`center_endpoint`（管理面：Agent 心跳 + 配置拉取）与 `remote_write_url`（数据面：指标回传）均登记在网域上、方向均为「采集节点 → 中心」，节点是使用者而非被指向者（身份 = `NETWORK_DOMAIN_ID` + `TOKEN`，平台不登记节点地址）；直连填中心地址、经 nginx / 网闸转发填转发侧地址、回传地址通常可自动推导（§5.1 字段说明重写 + §3.1 纳管行补表单提示要求 + §9 两条新验收）。v1.68 决策 79（M06 v2.13 跨模块落点）：§10 术语「中心直连区 / 隔离区（采集节点接入）」→「**中心直连域 / 采集节点域**」，纯文档、原型行为不变。v1.67 决策 78（M06 v2.12 跨模块落点）：§3.1 补「接入进度映射」注记——M06 四态为聚合视图、本页不对齐展示四态不聚合「已出数据」，保持「纳管状态 + 运行状态」原子粒度，在线态文案沿用「采集节点」口径；纯文档、原型行为不变。v1.66 决策 74/75 配套（M06 决策 73~77 跨模块落点）：§3.1 安装指引补「**一键复制安装命令**」——已纳管 `agent_pull` 网域提供 `NETWORK_DOMAIN_ID`/`TOKEN` 预填环境变量块 + systemd 步骤，承接 M06 列表「已纳管未上线 → 查看安装指引」深链；§10 术语 `domain_type`→接入方式（中心直连区 / 隔离区（采集节点接入））、`zone_type`→网络分区（可选）、新增 `Edge Sync Agent`→采集节点；§11.1 空态文案用户语言化；原型待对齐。v1.65 决策 68-5：**租户标签来源定版 + 生成期门禁**——§3.3.1 明确「**租户标签唯一来源 = Module_07 LabelTemplate target 级注入**」（`external_labels` 永久不承担，决策 19 结论维持、v0.2 起亦不变）与「**M09 生成期门禁**」（v0.2 校验被引用 Job 的标签模板含 `tenant` 映射，缺失则 `validation_status=failed`，经 `validation_details.source` 路由跳回 M07）；§3.3.1「命名空间边界」追加租户键分层与**通用命名规约**（标签键一律不带 `_id` 后缀）；§7.1.4 边界表、§9 验收（新增 {P0 / v0.2} 门禁项）、§10 术语同步。v1.64 决策 68：①**标签键收敛为 `network_domain`**——`external_labels` 键名由 `network_domain_id` 变更（决策 19 键名被 supersede，其「移除 `tenant_id`」字段清单结论不变），消费侧 `network_domain` → 兼容 `network_domain_id` → `default` 双读为**过渡层常驻**，**对象 / API 字段仍用 `network_domain_id`**（新增三层命名空间边界表）；②新增 §3.3.1.1「`alerting` 投递接线」——中心 `prometheus.yml` 生成 `alerting.alertmanagers[].static_configs[].targets`，**条件注入**（仅当有 `alertmanager.yml` 产物）、**AM 地址由 `env/env.sh` 注入禁硬编码**、**仅中心生成**（边缘包永不生成 `alerting` / `rule_files`），补齐决策 59/60 缺失的 Prometheus→AM 投递环节；§9 新增投递接线与端到端告警链路验收。v1.63 决策 67：失败单不锁死源数据、「前往修改」按来源分流 `/rules` / `/scrape-jobs`；v1.61/v1.62 为规则 job 引用校验口径（决策 64/65/66），原型行为不变；v1.54~v1.56 为 §0 业务叙事层补充、原型行为不变；原型已由 v1.51 提至 v1.52。v1.53 已同步：v0.2 规划定版——target 端口解析链补 `Resource.scrape_port` 优先级、新增 K8s 划域备忘，扇出（54）/filter（53）维持 v0.2，纯文档新增、原型行为不变；PRD v1.52 决策 60 待原型对齐——alertmanager.yml 纳入 M09 变更确认（管理域 scope、不扇出，MVP local 通道确认后写中心 Alertmanager 配置并 reload）；v1.51 决策 53/54 原型已对齐；此前 v1.50 已同步：采集认证/TLS 最小集透传——`basic_auth` / `authorization` / `tls_config` 映射进 scrape_configs、blackbox 拨测同理透传 `tls_config`；冻结域不生成新变更单、存量下发与回滚不受影响；变更状态回写 `change_status=deployed` 提前到 MVP（决策 31-M2）；删除「未指定网域资源自动归 default」兜底、`network_domain_id` 必填以 M07 为准（决策 31-M3）；v1.49 网域契约结构性对齐决策 28 + offline 排除提级 P0 决策 29：NetworkDomain 行政模型以 Module_06 为单一事实来源、删除旧「1 租户:N 网域 / 禁止跨租户共享 / 租户前缀」语义；§3.3 实例过滤 offline 排除提级 MVP 必实现——生成 targets/*.json 时按 Resource.status=offline 过滤（offline 后下一生成周期即从 targets 移除）、「配置产物结构说明」ReviewNote 提级改写、targets mock 注释同步；v1.48 规则文件挂载联动——决策 38-1：rules.yml 生成改为「透传并入」，MVP 由 M01 规则文件挂载的 `MonitoringRule.rule_content`（content_mode=yaml_passthrough）原样并入、不按字段派生，保存/启停/删除后进入变更检测 → 变更单人工确认 → 下发、change_status 全链路回写 M01；v0.3 字段级编辑（structured）后恢复按字段派生分组） | ready（可开发版本） | MVP/v0.2/v1.0 | #24 |
| Module_10 监控源登记册 | v1.2 | v1.1 | ✅ 对齐（v1.2 v0.2 范围定版：监控源 CRUD/外部 Remote Write/Ingestion Gateway 整体后移 v0.3，纯版本归属调整） | 设计中 | v0.3/v0.4/v1.0 | #24 |

## 2. 对齐规则

- **原型版本必须与 PRD 修订版本一致**（prototype-designer 强制校验，见 `.kimi/agents/prototype-designer.md`「版本一致性检查」）。
- **产品版本覆盖**以 02_Product_Roadmap.md §1.5 功能-版本矩阵为准，各模块 PRD 头部声明需与之对齐。
- **PRD 状态**：draft / prototyping / ready / 已冻结。`已冻结` = 该轮 review 合并后由 chenrt 标记，是 zhangwq 开发与回退的基线（冻结行只增不改，见 06 Gitflow §2.5）。
- **最近合并 PR**：develop 最近一次合并轮次（PR 编号即追溯点：`git log --merges develop | grep "#<PR编号>"` 可定位该轮 merge commit）。每轮合并后由 chenrt 更新本列。

## 3. 维护纪律

- **更新时机**：任何模块 PRD 版本递增、原型版本变更、PRD 状态流转（ready / 已冻结）时，**与修订表同一次提交**更新本表对应行。
- **更新人**：prototype-designer（迭代后同步）；chenrt（标记已冻结、合并 PR 后同步）。

## 4. 模块 ↔ 代码目录映射

> 定位：回答「某个模块的代码落在哪个目录」。源码目录按功能域组织，模块归属由分支名 + commit message + 本表共同承载。
> 更新时机：新增/迁移模块源码目录时，**与功能代码同一次提交**更新本表。

| 模块 | 功能域 | 后端目录/包 | 前端目录 | 负责人 / 开发分支 |
|------|--------|------------|----------|------------------|
| Module_00 | 全局索引 | — | — | `design` / chenrt, prototype-designer |
| Module_01 | 监控策略与指标管理 | `platform/cmd/metric-center/`（Prometheus 代理入口）<br>`platform/models/`（共享模型） | `ui-custom/web/src/pages/collection/` | `feat/module-01-*` |
| Module_02 | 查询中心 | `platform/api/response/`（统一响应封装）<br>`platform/cmd/metric-center/`（查询代理） | `ui-custom/web/src/pages/query/` | `feat/module-02-*` |
| Module_03 | 网关与认证 | 预留 `platform/gateway/` | 共享路由/鉴权（`src/api/client.ts`、`src/layouts/`） | `feat/module-03-*` |
| Module_04 | 自定义服务发现 | 预留 `platform/discovery/` | `ui-custom/web/src/pages/resources/` | `feat/module-04-*` |
| Module_05 | 自定义前端门户 | — | `ui-custom/web/` 整体 | `feat/module-05-*` |
| Module_06 | 租户与平台管理 / 网域登记 | `platform/admin/networkdomain/` | `ui-custom/web/src/pages/admin/domains/` | `feat/module-06-*` / zhangwq |
| Module_07 | 监控对象管理 | `platform/models/`（共享模型）<br>预留 M07 目录待创建 | `ui-custom/web/src/pages/resources/` | `feat/module-07-*` |
| Module_08 | 告警收敛与通知管理 | 预留 `platform/config/` | `ui-custom/web/src/pages/alerts/` | `feat/module-08-*` |
| Module_09 | 网域与边缘配置中心 | 预留 `platform/collector/`<br>预留 `platform/config/` | 预留页面目录（`#/domain-onboarding`） | `feat/module-09-*` |
| Module_10 | 监控源登记册 | 预留 `platform/discovery/` | 预留 | `feat/module-10-*` |
| **共享基础设施** | 跨模块 | `platform/api/response/`、`platform/db/`、`platform/models/` | `ui-custom/web/src/layouts/`、`src/api/`、`src/components/` | `develop` / 各 Agent |

### 4.1 目录归属原则

1. **源码目录按功能域命名**，不按模块号命名。例如网域功能使用 `networkdomain/` 而非 `module-06/`。
2. **模块归属通过以下四层共同追溯**：
   - Git 分支：`feat/module-XX-*`；
   - Commit message：`feat(module-XX): ...`；
   - 执行记录：`docs/05-execution-records/module-XX/`；
   - 本表：模块到功能目录的映射。
3. **跨模块共享代码**（如 `models/`、`api/response/`、`layouts/`）不归入任何单一模块目录，由 `develop` 统一维护；修改时须在 commit / PR 描述中说明影响的模块。

## 5. Change Log

| 版本 | 日期 | 变更类型 | 变更内容 | 影响范围 | 产品版本影响 | 状态 |
|------|------|----------|----------|----------|--------------|------|
| v1.71 | 2026-09-15 | 修改 | **M06 / M09 原型同步决策 82（网域生命周期闭环）落版 + 同日勘误**（chenrt 指令「请继续优化 m06」+ 范围选定「M06 + M09 原型同步」；PRD 已于 v1.69 落版，本轮为原型与版本回写）：①**M06 原型 2.14.0 → 2.15.0**——已纳管网域删除入口恢复可用（撤销 v2.14「已纳管网域不可删除，请改用禁用」菜单提示条）+ 新增「删除网域（退场回收）」二次确认弹窗展示**级联影响清单**（1 个采集节点将断连 / 凭据废止 / 停止配置下发 / 节点侧无需线下操作、心跳鉴权失败自动转离线 / 既有采集节点记录保留供审计 / 软删不可自助恢复）+ 禁用弹窗补强（已纳管且存在在线 Agent 时追加「【重要】禁用为行政冻结……已接入的采集节点不会自动停止采集」+ 引导用「删除」或 v0.2+「退纳管」）+ 「更多」菜单不可操作原因修正（已纳管不再是拒绝理由，仅剩「系统预置域」与「存在 M07 资源引用（N 条）」）+ mock 新增 `resource_ref_count` 覆盖三类删除场景；build / lint / check:prototype / check:notes 全绿 + headless Chrome 截图走查 10 状态。②**M09 原型 1.71.0 → 1.72.0**——网域纳管页第 3 列「运行状态」更名「**采集节点在线**」+ 列头 tooltip 点明粒度（网域粒度聚合的运行态；节点与组件级诊断归「采集节点状态」页），与「纳管状态」列（配置态）职责正交，**消除与节点页「整体状态」同名指代不同对象导致的「冗余列」误判**；决策 82 的退纳管入口（v0.2+）与级联清退在 MVP 无 UI 落点（后端义务，本模块不提供删除入口）；build / lint / check:prototype / test（54）全绿。③**同日勘误**（落档复核时发现并修正，纯文档）：M09 PRD §8 ⑤ 状态机枚举由初稿自造的 `unregistered` / `registered` / `retired` 改为 `created`（未纳管）⇄ `monitored`（已纳管）——**对齐原型自 v1.29 起的代码枚举**（21 处引用 + 单测依赖），并采用「中文态名 + 括号标注代码枚举值」写法与全篇一致；`retired` **归属定版**为该网域的 `EdgeAgent` 采集节点记录（该记录生命周期终态、供审计），**不是**网域 `registration_status` 取值（网域被删后本身软删、从列表消失、不进入状态机），修正 §3.1 与 §8 ⑤ 的自相矛盾；M09 PRD 一处**无空格斜杠写法** `决策 62/81` 在批量改号时漏改，已修正为 `62/82`。④**M09 原型 README 版本缺口闭合**——该 README 变更说明此前停在 v1.52，本轮补 v1.72 并**忠实回填 v1.69 / v1.70 / v1.71 三版**（v1.53~v1.68 期间 PRD 多次修订但原型未改动、原型版本轴 1.52.0 直达 1.69.0，故无中间原型版本条目），同时把其陈旧描述（§原型目标 / §核心页面 原写「7 列、运行状态」）改为实际的 5 列 / 采集节点在线。⑤§1 总表 M06 行 v2.15/v2.15 ✅ 对齐、M09 行 v1.72/v1.72 ✅ 对齐（两行原「⚠️ 原型待同步」说明同步改写为已同步口径）；两侧 PRD 头部「原型版本」行转已同步；M06 原型 README 加 v2.15 变更说明（含弹窗文案与 mock 场景清单） | Module_06 / Module_09 | MVP / v0.2 | 使用中 |
| v1.70 | 2026-09-15 | 修改 | Module_07 原型 v2.32→v2.33 同步决策 81（K8s 双域登记动线 + 网域字段可达性引导；PRD v2.33 已于 v1.68 落版，本轮纯原型）：①**资源新增双入口分流**——「新增资源」改下拉，置顶语义互斥的「登记主机」（→ host，OS 层 node_exporter `:9100`，K8s 节点主机 OS 同走此入口，表单顶部 Callout 注明只管 OS 层）与「登记 K8s 集群」（→ generic_target 专属表单：集群组件选择器预设 API Server `6443/https` / kube-state-metrics `8080/http` / etcd `2379/http` / 自定义，联动 exporter_type·端口·采集路径·协议，集群名必填，Callout 明示节点 / Pod / 容器由该域 kubernetes_sd Job 动态发现、无需逐台登记），两入口各带两行链路说明；②**网域字段可达性引导**——网域表单项改问「这台机器的采集端口从哪条链路够得着？」，下拉选项第二行标注链路说明（中心直连：平台直接采集 / 采集节点域：经该域采集节点中转），`instance_ip` 变化时实时按 `ip_cidrs` 最长前缀推导预览：唯一命中显示「推导归属 + 命中网段」并提供「采用」按钮、同最长前缀跨域命中提示「命中多个网域、请人工选择」（演示数据：生产 / 测试集群共用 `10.244.0.0/16`，且 /16 优先于 default /8）、无命中提示保存归默认网域兜底、域名不做 DNS 解析；预览仅辅助不替代显式选择，复用决策 52 四级解析链；③mock 增补 `NetworkDomain.domain_type`（management/edge）+ 生产 / 测试两集群网域、`domainReachabilityText()` / `previewDomainByIP()` / `K8S_ENDPOINT_PRESETS`，不改数据模型与接口；④`module-07.test.ts` describe 对齐 v2.33 并新增 7 条断言（domain_type / 重叠网段歧义 / 链路文案 / preview 四态 / K8s 预设合法性）；⑤原型 README 加 v2.33 变更说明、PRD 头部原型版本转「v2.33 已同步」；§1 总表 M07 行 v2.33/v2.33 ✅ 对齐 | Module_07 | MVP / v0.2 | 使用中 |
| v1.69 | 2026-09-15 | 修改 | **M06 / M09 网域生命周期闭环落版（决策 82）**（chenrt 走查 M06 网域管理后提出两问：「已纳管网域在其生命周期里没有可删除的条件和按钮，难道就算安装了采集节点这个网域也不允许被回收吗」「M06 启用/禁用若只是行政登记，就和 M09 网域纳管割裂开了」；纯文档不改数据模型、原型待同步）：**Module_06 PRD v2.14→v2.15** + **Module_09 PRD v1.71→v1.72**——①**删除约束修正（82-1，MVP）**：`DELETE /api/v2/platform/network-domains/:id` 硬拒绝条件**收敛为「存在 M07 资源引用」单一条件**（资源是有主数据，删除报错须列出引用资源名单 + M07 跳转），原「无已纳管 EdgeAgent」由硬拒绝改为**级联清退**——删除弹窗展示级联影响清单（N 个采集节点将断连、凭据将废止）→ 确认后 M06 软删 + M09 级联清退（废止 Token / 停止配置下发 / `EdgeAgent` 标 `retired`；采集节点侧不要求线下卸载，Token 废止后心跳鉴权失败自然离线）；②**禁用弹窗补强（82-2，MVP）**：已纳管且存在在线 Agent 时，禁用二次确认在「影响范围」之外追加固定提示「禁用为行政冻结……已接入的采集节点不会自动停止采集；如需停止采集并回收网域请使用『删除』」，把「禁用不停采集」从隐性设计变为显式告知；③**退纳管动作定义（82-3，v0.2+）**：把**决策 62 口径①**（禁用联动取消纳管 / 冻结 Token）从原则具象化为 M09 可实现动作——废止 Token → 停止配置下发 → `registration_status` 归位 `unregistered`（保留历史）→ 心跳鉴权失败转离线，**并补齐 M06 §6.2 早已许诺「是否停止采集由 M09 退纳管决定」、但 M09 侧从未定义该动作的契约悬空**；④**三动作语义边界定版**（M06 §3.2 新增对照表）：**禁用 = 行政冻结**（不准新入、不强制停旧）/ **退纳管 = 停止监控**（保留网域、清空纳管）/ **删除 = 退场回收**（级联清退、从平台移除），三者**正交不可互替**，覆盖「不再使用某网域」的三种强度。**M06 落点** §3.2（三动作对照表）/ §4（纠错与回收路径）/ §6.2（PATCH status + DELETE 契约改写）/ §8（删除不在状态机内注记）/ §9.1（修订 2 条 + 新增 1 条）/ §9.2（改写 1 条 + 新增 1 条）/ §11.2（重写 2 条 + 新增 1 条）；**M09 落点** §1（MVP 注记补决策 62 推进）/ §3.1（「网域删除」行改写 + 新增「退纳管」行）/ §8（新增 ⑤ 网域纳管状态状态机 + 三状态分工注记）/ §9.1 + §9.2（新增 3 条验收）/ §10（术语表补「退纳管」）；两侧 PRD 修订表轮转（M06 v2.12 / M09 v1.69 迁各自 design-decisions 完整历史）；决策落档 module-06 design-decisions **决策 82**（M09 侧交叉引用登记）；**原型双侧待同步**（v2.15 / v1.72 原型轮落地） | Module_06 / Module_09 | MVP / v0.2 | 使用中 |
| v1.68 | 2026-09-15 | 修改 | Module_07 PRD v2.32→v2.33（决策 81，K8s 双域场景的网域归属与登记动线）：①§5.1 增补资源身份第一性原则（资源身份 = 采集端点 × 采集路径、动态发现产物不登记、同机不拆记录、`instance_ip`=采集端点 IP）；②§5.2 `network_domain_id` 语义修正为采集路径归属非资产归属；③§5.4 增补网域用户侧定义（对齐 M06 决策 73）、登记表单可达性引导策略、K8s 双入口动线分流表；④§5.16.4 补 K8s 双域操作规约与冲突告警级处理标准动作；⑤§3.1 新增「登记入口分流」功能行；⑥§11 新增网域可达性引导与 K8s 动线分流两条全局行为规则；⑦§9 验收补 3 条。不改数据模型与接口契约。§1 总表 M07 行 v2.33/v2.32 ⚠️ 原型待升级 | Module_07 | v0.2 | 使用中 |
| v1.67 | 2026-09-15 | 修改 | M09 采集节点状态页两列与 M06 对齐裁定落版（决策 74，chenrt 拍板「接入方式列是否与 M06 同步、配置同步列是否对齐 M06 接入进度点阵、状态自动更新」；PRD + 原型同轮落地）：Module_09 PRD v1.70→v1.71——①**网域列不展示接入方式 Tag**（决策 74-1）：本页实例按定义均属采集节点域（中心直连域不产生 `EdgeAgent` 实例、仅出现在空态说明），恒定信息不进列（原型原 Tag 还用裸枚举 `agent_pull`、口径不符），接入方式归网域纳管页网域列；②**配置同步列形态定版 = Badge + 成因分档**（决策 74-2）：不采用 M06「接入进度」点阵——点阵表达单调递进生命周期（走完不回退），配置同步为循环状态机 + 成因分支（「生效中」是 `out_of_sync` 成因之一、非更靠后步骤）且状态由心跳自动流转无需人工推进；「状态自动更新」语义上已成立（心跳上报驱动，与 M06 聚合查询自动前进同理）；表头 Tooltip 一句短定义 + 每格悬浮该档明细；③mock 流转演示（决策 74-3，原型行为）：跨页流转桥 `mockSyncFlowOverrides`——配置确认页确认 agent_pull 变更后写入「生效中」、采集节点状态页重新挂载可见，约 10s 心跳模拟自动流转「已同步」；「立即同步」结果回写桥、跨页不回退。落点 §3.2 节点平铺表行 + §3.2 新增形态定版注记 + §9 两条验收补充；原型 1.70.0→1.71.0（EdgeAgentsPage / ConfigPreviewPage / mocks）；PRD 修订表轮转（v1.68 迁 module-09 design-decisions 完整历史）；决策落档 design-decisions 决策 74（74-1/74-2/74-3）；§1 总表 M09 行 v1.71/v1.71 ✅ 对齐 | Module_09 | MVP | 使用中 |
| v1.66 | 2026-09-15 | 修改 | M09 决策 70~72 九条 PRD 显性化回写落版（chenrt 指令；原型先行内容追认，纯文档不改模型与契约、原型行为不变）：Module_09 PRD v1.69→v1.70——①§3.1「网域列表」行 + §9 验收改 **5 列口径**（决策 70-1/70-2：网域列三合一（名称 + 网域 ID 等宽字体 + 接入方式 Tag），接入方式并入网域列按 `domain_type` 措辞「中心直连域 / 采集节点域」、`local`/`agent_pull` 保留为技术判据，zone_type 下沉详情 Drawer）；②命令口径改**静态模板**（决策 71-4：「复制安装命令模板」不含真实凭据、`<网域 ID>`/`<凭据>` 占位符由用户行内复制填值，决策 75 表述同步）+「纳管取凭据」前置不占 3 步编号（决策 71-3）+ §9 验收 1261 边界注记（行内禁的是展示指引内容、复制动作不受限，决策 70-3）；③§9 新增三条验收：详情抽屉不承载组件明细（决策 72-1）/ 摘要区三态出口（决策 72-2）/ 采集节点状态页 `network_domain` 深链（决策 72-3）；④§3.1 字段语义注记补三态出口、§3.2 新增「跨页深链预筛」行。PRD 修订表轮转（v1.67 迁 module-09 design-decisions 完整历史）；决策 70~72 九条待办映射表落档 design-decisions「回写登记」；原型版本轴对齐 `package.json` 1.69.0→1.70.0（无代码改动）；§1 总表 M09 行 v1.70/v1.70 ✅ 对齐 | Module_09 | MVP | 使用中 |
| v1.65 | 2026-09-15 | 新增 | M09 网域-采集节点部署口径与两类地址用户指引落版（决策 73，chenrt 两轮答疑驱动：「网域是连通域为什么能 1 对 N」「center_endpoint / remote_write_url 定义不清晰，a→b 经 nginx 拓扑下用户应得什么指引」；纯文档不改模型与契约）：Module_09 PRD v1.68→v1.69——①**MVP 单节点部署口径**（决策 73-1）：一个网域在网域内挑一台常开机器安装**一个**采集节点即完成接入（域内互通、一台覆盖全域目标），安装指引不引导多节点部署；同域多采集节点（分片 / HA / 多探测点）维持 v0.4+ 演化（驱动力为容量 / 可用性 / 拨测语义而非连通性），`EdgeAgent` 模型天然 1:N、MVP 以单通道单采集组约束保证 1:1；落点 §3.1 安装指引行 + §3.1.1 新增「用户侧部署口径」注记 + §9 新增验收项；②**两类地址方向性指引**（决策 73-2）：`center_endpoint`（管理面：Agent 心跳 + 配置拉取）与 `remote_write_url`（数据面：指标回传）均登记在网域上、方向均为「采集节点 → 中心」，节点是使用者而非被指向者（身份 = `NETWORK_DOMAIN_ID` + `TOKEN`，平台不登记节点地址）；填写指引 = 直连填中心地址、经 nginx / 网闸转发填转发侧地址、回传地址通常可自动推导；落点 §5.1 两字段说明重写 + §3.1 纳管行补表单方向性提示要求 + §9 新增验收项。PRD 修订表轮转（v1.66 迁入 module-09 design-decisions「Change Log（完整历史）」）；决策落档 module-09 design-decisions 决策 73（73-1 / 73-2）；§1 总表 M09 行同步 v1.69；**原型不动（维持 v1.52.0）**，纳管表单 extra 文案同步留待下轮 | Module_09 | MVP | 使用中 |
| v1.64 | 2026-09-14 | 修改 | M07 原型前端设计优化 + 契约对齐落版（chenrt「请继续优化 M09 和 M07 的原型」，承 M06 决策 80 的同一套规范口径；本质为**对齐《前端标准》§8 交互选型表 / §9 列表与长文本规范**）：原型 module-07 由 v2.25 同步至 **v2.32**（`package.json` 2.25.0 → 2.32.0）——①**资源列表列数治理 11 ~ 14 → 8 列**（主机 11、数据库 / 中间件 / 应用 12、通用目标 14），五类 Tab 统一为「主标识 `fixed: left` / 类型或身份 / 网域 / 归属来源 / 业务 / 运行状态 / 采集状态 / 操作 `fixed: right`」，端口 / 版本 / 操作系统 / 应用·环境·集群 / 健康检查 URL / 协议 / 端点 / 采集路径 / 自定义标签 / 数据来源 / 负责人 / CMDB 预留字段**下沉详情 Drawer**（详情抽屉同步补「数据来源 / 网域含归属来源 / 采集状态」三行，避免下沉即丢失），类型字段改随主标识行内 Tag；②**状态语义统一 Badge 语义色 + 文字**（采集状态由彩色 Tag 改 Badge，新增 `COLLECTION_BADGE_STATUS` 映射）；③**操作层次化**——主操作「详情」品牌色加粗且全行唯一、次要「编辑」灰色文字、低频破坏性「删除」收进「更多」菜单（业务管理页停用 / 启用同理，`infra` 兜底条目在菜单内直给不可操作原因，替换原「disabled + Tooltip」）；④**新增 / 编辑资源抽屉 560 → 720px 并分「资源信息 / 归属与状态」两组**（字段 12 ~ 14 项 > 6 且需分组，新增 `FormSection` 局部组件），详情抽屉 680 → 720px；⑤**全页提示统一 Callout 单块容器**——新建 `src/components/Callout.tsx` + `calloutTones.ts`（与 module-06 / module-09 同款视觉语言），替换 ResourcesPage / LabelTemplatesPage / BusinessManagementPage（2 处）/ ImportHistoryPage 共 5 处 Alert，消除多风格提示拼盘；⑥**`LabelTemplate.description` 落库**（PRD 6.3 / v2.27）——类型加可选字段、6 个模板补说明、新增 / 编辑抽屉加「模板说明」TextArea 并回填、模板详情头部展示、克隆保留；⑦**五类默认模板补齐 `resource_id → resource_id`**（v2.25 遗留缺陷：host / middleware / application / generic_target 原缺失，仅 database 有）并补进五类资源字段选项；⑧映射字段选项补 `tenant_id`，按 **v2.31 命名规约**（`target_label` 一律不带 `_id` 后缀，`resource_id` 为约定俗成例外）组织，新增 `TENANT_DEFAULT_MAPPING` / `TENANT_MAPPING_NOTES` 常量、ReviewNote 两条前瞻说明与映射表单「关于「租户」映射」就地 Callout（**MVP 单租户不注入、v0.2 起内置默认并默认启用**）；⑨**运行状态用户语言按 PRD §10 术语表收敛为「运行中 / 已停止 / 维护中」**——原 mock `STATUS_MAP` 误用「在线 / 离线」，与资源列表筛选器、Excel 状态映射字典、导入记录页「已停止」文案三处冲突，本轮一并对齐；⑩`mocks/module-07.test.ts` 53 → **59 条单测**（新增稳定身份映射五类校验 / 命名规约 / tenant 前置口径与字段选项 / description 落库四组断言），`pnpm build` / `pnpm lint` / `pnpm check:prototype` / `pnpm check:notes` 全过；⑪PRD 头部「原型版本」行由「v2.25 ✅ 已对齐 + v2.27~v2.29 待对齐」改为「**v2.32 ✅ 已对齐**」；⑫MainLayout 决策清单追加 3.26 ~ 3.29 四条，design-decisions 追加「补充对齐：2026-09-14」记录。**注**：M09 侧「一键复制安装命令」等改动为上一轮已完成工作，本轮同时复核通过（build / lint / check:notes 全绿），未再变更 | Module_07 | MVP / v0.2 | 使用中 |
| v1.63 | 2026-09-14 | 修改 | M06 前端设计优化落版（决策 80，chenrt 走查 v2.13 原型后按「专业前端设计」返工；本质为**对齐《前端标准》§8 交互选型表（>6 字段表单 → Drawer / 状态语义 → Badge）/ §9 列数治理（≤8 列，其余下沉详情 Drawer / 行内 Tag 上限）**）：M06 PRD v2.13→v2.14（§9.1 验收五条修订 + 新增「列数治理」「状态与操作层次」两条；§11.2 契约重写「页首网域引导条」、重写「登记前置判断」（抽屉容器 720px / 四组分区 / 双选择卡 / 判断口径澄清 / 身份说明与概念详解同容器就地展开）、「接入进度列」补「展示形态与主操作」（点阵 + 去灰字旁注 + 全行唯一主按钮、已出数据→查看采集任务）、新增「列表列数治理与详情抽屉」与「状态列与操作层次」；Change Log 轮转 v2.11 迁完整历史）；M06 原型同步 v2.14（NetworkDomainsPage 重构：Callout / FormSection / DomainGuidePanel / CenterDirectChoice / CenterDirectRationale / AccessProgress 局部组件，Modal → Drawer，新增详情 Drawer，11 → 8 列；build / lint / check:notes 全过，headless Chrome 实测 10 个界面状态）；M09 PRD 无落点（不改）；决策落档 module-06 design-decisions 决策 80 | Module_06 | MVP / v0.2 | 使用中 |
| v1.62 | 2026-09-14 | 修改 | M06 走查返工落版（决策 79，chenrt 逐条反馈）：M06 PRD v2.12→v2.13（前置判断「能」改硬劝阻终点、删除「或登记为中心直连区」出口；术语再修订中心直连域 / 采集节点域；引导信息归位——概念卡+接入指引合并折叠面板、抽屉移除 3 步 Alert 改身份说明+概念详解折叠入口、行动卡对齐四步）+ M09 PRD v1.67→v1.68（§10 术语同步）；M06 原型同步 v2.13（对齐 ✅）；决策落档 module-06 design-decisions 决策 79；Change Log 轮转（M06 v2.10 / M09 v1.65 迁完整历史） | Module_06 / Module_09 | MVP / v0.2 | 使用中 |
| v1.61 | 2026-09-14 | 修改 | 接入进度四态聚合视图定版（决策 78，chenrt 确认方案 B）：M06 PRD v2.11→v2.12（§11「接入进度列」补注记——四态为聚合视图而非新状态机（已登记=M06 行政状态 / 已纳管=M09 纳管状态 / 采集节点已上线=M09 运行状态 online / 已出数据=M01 生效配置聚合），数据流向 = M06 列表接口拉 M09/M01 数据、M09 不反向聚合「已出数据」，M09 保持原子列不对齐四态；头部原型版本行修正为 v2.11 已对齐）+ M09 PRD v1.66→v1.67（§3.1 补「接入进度映射」注记——保持「纳管状态 + 运行状态」原子粒度、在线态文案沿用「采集节点」口径，职责口径 M06=全景+动线路由 / M09=纳管动作+运行明细）；M06/M09 原型双侧不动；决策落档 module-06 design-decisions 决策 78；Change Log 轮转（M06 v2.9 / M09 v1.64 迁完整历史） | Module_06 / Module_09 | 文档自身 | 使用中 |
| v1.60 | 2026-09-14 | 修改 | M05 执行记录与原型补齐（决策 72-3 回填收尾）：①`api-contract-snapshot.md` 登记 `dashboard/summary` 新增 3 计数字段 + 补登 `/api/v1/query` 代理（上一轮遗留），版本刷新 v1.5，补「查看全部 →」深链表与六步深链表；②`dev-feedback.md` 补 clipping-1（指标卡集合偏离原枚举，关联评审 MEDIUM-2）+ clipping-2（AM `unprocessed` 不计数不进列表）；③**新建** `frontend-prototype-map.md`（首页组件 ↔ PRD v1.5 条目映射，补齐前批审查遗留风险）；④`05_Code_Implementation_Plan.md` / `task-sequence.yaml` 追加 T13（首页内容重构）/T14（后端 summary 三字段）/T15（测试与验证）并回填 T16/T17 与 T8~T12 状态，计划版本刷新 v1.5；⑤原型 `docs/prototypes/module-05/` 改造为 **v1.2.1**（DashboardPage 按 v1.5 §3.1 重构：6 张资产指标卡 + ⓘ 口径注释 / 告警状态卡承载最新 10 条告警列表与表头 / 快速入口 / 六步指引 / 最近下发「查看全部 →」；移除首页大屏入口卡与旧 Agent 状态表；`UsageGuidePage` 同步六步；package.json 0.1.0→1.2.1）；README §1 M05 行原型版本 v1.1→v1.2.1、对齐改「✅ 对齐」 | Module_05 | MVP / v0.3 | 使用中 |
| v1.59 | 2026-09-14 | 修改 | M05 首页内容重构 as-built 回填（决策 72-3）：Module_05 PRD v1.4→v1.5（§1 / §3 首页行 / §3.1 / §6 同步）——①指标卡由「含告警维 6 张」回归「纯资产/治理进度 6 张」（资源总数/已监控/采集 Job/已纳管网域/待确认草稿/采集覆盖率），移除「活跃告警」卡，每张右上角 ⓘ 中文口径注释、6 列网格、卡内横向排版；②告警状态卡改为首页唯一告警入口——主数字「通知中」+ 状态分布 + 最新 10 条告警列表（表头 + 单行五列）+ Prom 仅参考小字，标题右侧「查看全部 →」深链、首页两块列表均不放分页器；③放宽「零后端改动」约束——`dashboard/summary` 新增 `monitored_count`/`scrape_job_count`/`scrape_job_enabled_count`（已监控口径 B：enabled+ready+standard Job 覆盖）；④`unprocessed` 不计数不进列表、页面不出现「待处理」；⑤§6 验收新增 72-3 清单；PRD Change Log 轮转（v1.2 迁入 module-05 design-decisions「完整历史」）；决策 72-3 落档 module-05 design-decisions；README §1 M05 行由陈旧 v1.2/设计中 刷新为 v1.5/dev-ready、产品版本覆盖补 MVP。原型仍 v1.1 待对齐（六步指引 / Dashboard 布局 / 视觉 Token / 指标卡资产口径 + 告警卡列表） | Module_05 | MVP / v0.3 | 使用中 |
| v1.58 | 2026-09-14 | 修改 | 同步 Module_06 决策 73~76 原型对齐：原型 v2.5→v2.11（页首概念卡 + 提问式空态 + 新建前置判断问题 + 登记成功行动卡 MVP 四件套；「监控纳管」二态列 →「接入进度」四态列 v0.2（mock `agent_online`/`has_data` 聚合字段 + mc-manufacturing / mc-dmz 四态演示，行内主操作 = 下一步动作深链 M09/M01）；术语降噪落页（中心直连区 / 隔离区（采集节点接入）/ 采集节点 / 接入方式 / 网络分区（可选））；zone_type 语义澄清进表单文案），对齐改「✅ 对齐」；README §1 M06 行原型版本同步 v2.5→v2.11 | Module_06 | MVP / v0.2 | 使用中 |
| v1.57 | 2026-09-14 | 新增 | 网域概念用户化与接入引导落版（决策 73~77，chenrt 反馈驱动：用户不理解 Edge Sync Agent / 管理域概念、无缘无故注册边缘域、注册后不知网域用途、跳 M09 机械化；要求网域通用化为网络可达性区域、讲清 zone_type 政务云/公有云含义、评估 K8s 集群类型；本轮全为文档，不改代码与数据模型）：①**决策 73** 网域 = 网络可达性区域（一句话定义 +「中心能否直连」二选一判断规则，覆盖单域多集群与多域；登记不采集数据）；②**决策 74** 术语降噪（用户侧：中心直连区 / 隔离区（采集节点接入）/ 采集节点 / 网络分区（可选），模型枚举与后端术语不变，仅修订决策 9 的 UI 展示名口径）；③**决策 75** M06 网域管理页引导四件套（页首概念卡 + 提问式空态 / 新建表单前置判断问题 / 接入进度四态列 v0.2 / 登记成功行动卡）+ M09 一键复制安装命令；④**决策 76** zone_type 语义澄清（纯分类标签不影响行为；政务云 = 安全分区 / 公有云 = region）；⑤**决策 77** K8s 集群不设第六资源类型（四归属：NetworkDomain 建域 / cluster 标签 / M04 发现源 / generic_target 监控）。落点：Module_06 PRD v2.10→v2.11（§3.2/§5.2/§9.1/§10/§11）、Module_07 PRD v2.31→v2.32（§5.1 注记）、Module_09 PRD v1.65→v1.66（§3.1/§10/§11.1）；决策落档 module-06 design-decisions 决策 73~77；Change Log 轮转（M06 v2.8 / M07 v2.29 / M09 v1.63 迁完整历史）；M06/M09 原型待对齐（概念卡 / 空态 / 前置判断 / 行动卡 / 术语 / 一键复制安装命令） | Module_06 / Module_07 / Module_09 | MVP / v0.2 | 使用中 |
| v1.56 | 2026-09-11 | 修改 | M01 规则挂载「检查 → 提交」两段式落版（决策 69，交互重排；**修订决策 67-2 的「提交期校验」为「检查期校验」**）：Module_01 PRD v3.40→v3.41（§3.1「规则文件挂载」行与「双层模型」注记改两段式——「检查」按钮常驻、`canSubmit = 检查通过 且（无 error 级 job 引用 或 已勾选逃生门）`、提交按钮**条件渲染**且文案统一「提交并进入变更确认」、逃生门提前到检查阶段、规则内容变更即作废检查结论与 ack；§3.2 明确 v0.3 字段化编辑沿用同一状态机；§5.5 新增「检查与提交门禁」注记；§6.2.4 补 `POST /monitoring-rules/{id}/validate-yaml` 契约行与语义说明 + POST/PUT 补 `ack_job_ref_errors` 与组名冲突错误码；§9.1 规则挂载验收由 1 条扩写为 6 条；§9.2 新增 `validate-yaml` 语义与 `ack_job_ref_errors` 技术验收；§11.1 新增 5 行检查态；§11.2 原「提交生效分级门」重写为「检查 → 提交两段式」并写入**用户可见免责边界**「检查通过 ≠ 提交必过」）；**原型 module-01 同步 v3.37→v3.41**（挂载抽屉两段式 + 独立「检查」按钮 + 提交按钮条件出现 + 组名全局唯一性与 job 引用分级**完整模拟** + 逃生门提前 + 结果面板下移到表单下方；文件选择入口维持编辑框上方＝生产向原型回归；新增 `src/pages/rulesCheck.ts` 判定逻辑与 13 条单测，全量 49 条测试通过；`package.json` 由滞留的 `3.28.0` 一次性对齐 `3.41.0`），对齐改「✅ 对齐」；PRD Change Log 轮转 v3.38 迁入 module-01 design-decisions「完整历史」 | Module_01 | MVP / v0.3 | 使用中 |
| v1.55 | 2026-09-10 | 修改 | 租户标签键定版 + 生成期门禁（决策 68-5，用户拍板「现在就定版，不等 v0.2」）：①**通用命名规约**——`_id` 后缀只属 DB 列与 API JSON 字段，Prometheus 标签键及对齐的 Query 参数 / Excel 列 / envelope 字段一律不带 `_id`；②**租户标签统一为 `tenant`**，唯一来源 = Module_07 LabelTemplate target 级注入（M09 `external_labels` **永久不承担**）；③**v0.2 三配套**——同源常量 `models.TenantLabelKey`、M09 生成期门禁（被引用 Job 的标签模板缺 `tenant` 映射则 `validation_status=failed`，经 `validation_details.source` 路由跳回 M07）、matcher 注入强制化；④**fail-closed 严格派**——无 `tenant` 标签 = 普通租户不可见，平台自身指标显式 `tenant="platform_admin"`，否决共享派。落点：Module_02 PRD v1.14→v1.15（§7.1/§7.2 注入名全文校正 + 命名规约）、Module_06 PRD v2.9→v2.10（§2 用户故事表注入名校正）、Module_07 PRD v2.30→v2.31（§5.11 命名规约 + §5.12 A 表新增 `tenant_id → tenant` 行 + §5.13 v0.2 前瞻）、Module_09 PRD v1.64→v1.65（§3.3.1 租户标签唯一来源 + 生成期门禁 + §9 新增 {P0 / v0.2} 验收 + §10 术语）、`03_Functional_Architecture` v3.3→v3.5；各模块 design-decisions 登记 + Change Log 轮转（M06 v2.7 迁完整历史）；M06 结论 8 标经 68-5 定版确认；**原型（module-02 / module-09）仍展示旧键名 `tenant_id` / `network_domain_id`，待后续同步**（本轮补登记：原提交 f55b29b7 已声明「README v1.53→v1.55」但漏加本行） | Module_02 / Module_06 / Module_07 / Module_09 / 全局架构 | MVP / v0.2 | 使用中 |
| v1.54 | 2026-09-10 | 修改 | 网域标签键收敛 + `alerting` 投递接线（决策 68，源自 F-07 网域列缺陷评审的两个遗留发现）：①**标签键收敛为 `network_domain`**——`external_labels` 键名由 `network_domain_id` 收敛（决策 19 键名被 supersede，其「移除 `tenant_id`」字段清单结论不变），消费侧 `network_domain` → 兼容 `network_domain_id` → `default` 三级解析为**过渡层常驻**（唯一入口 `models.ResolveNetworkDomain`），**对象 / API 字段仍用 `network_domain_id`**（Module_09 §3.3.1 新增三层命名空间边界表）；②**新增 §3.3.1.1「`alerting` 投递接线」**——中心 `prometheus.yml` 生成 `alerting.alertmanagers[].static_configs[].targets`，**条件注入**（仅当存在 `alertmanager.yml` 产物）、**AM 地址由 `env/env.sh` 注入禁硬编码**、**仅中心生成**（边缘包永不生成 `alerting` / `rule_files`），补齐决策 59/60 缺失的 Prometheus→AM 投递环节。落点：Module_09 PRD v1.63→v1.64、Module_02 PRD v1.13→v1.14（§7.1 注入 key 权威口径 + 遗留待决发现）、Module_08 PRD v1.13→v1.14（§9.1/§9.2 补投递与端到端链路验收）；新增设计记录 `module-09/network-domain-label-key-convergence-and-alerting-wiring.md`；同步修正 `04_Implementation_Map.md` / `05_Code_Implementation_Plan.md` 中 68-1 漏网的旧键名（本轮补登记：原提交 f55b29b7 已声明「README v1.53→v1.55」但漏加本行） | Module_02 / Module_08 / Module_09 | MVP / v0.2 | 使用中 |
| v1.53 | 2026-09-10 | 修改 | 规则 job 引用校验动线修复落版（用户确认决策 67）：Module_01 PRD v3.39→v3.40（§3.1/§3.2 编辑期校验改 **error 默认阻断「提交生效」+ 显式覆盖逃生门**（修订决策 66 第 2/4 条）；§5.5 补 M09 草稿 `failed + user_config` 自动清锁；§8 补规则侧自动解锁注记 + 采集 Job 侧为未决项；§11.2 新增两条全局行为规则）；Module_09 PRD v1.62→v1.63（§3.4/§3.5.1/§5.4/§8 失败单不锁死源数据；`validation_details` 补 `source`；§11.2「前往修改」按来源分流）；新增设计记录 `module-09/rule-jobref-validation-lifecycle-fix.md`（含代码级证据索引与决策 66 修订对照）；module-01/09 api-contract-snapshot 同步（`ack_job_ref_errors`、失败清锁语义、`source` 字段）；两模块原型待同步 | Module_01 / Module_09 | MVP / v0.2 | 使用中 |
| v1.52 | 2026-09-09 | 新增 | 历史告警 MVP 增量同步（用户书面确认，Track B+）：Module_08 PRD v1.12→v1.13（新增独立「历史告警」页——含已恢复、恢复时间按 Prometheus 求值近似，§1/§2 M08-OPS-08/§3.1/§5.4/§9/§10）；Module_02 PRD v1.12→v1.13（新增 `/api/v1/alerts/history`——基于 `ALERTS` 序列 query_range 重建触发/恢复区间，§1/§3.1/§5.4/§6.1/§11/§12/§13）；全局用户故事库 §4.8 新增 M08-OPS-08；两模块原型仍为 v1.7（Track B+ 免高保真原型，豁免记录见各自 design-decisions）；M09 规则 job 引用校验口径（up/absent(up) 不匹配=error、其他=warning）已用户确认、登记于 module-09 design-decisions，PRD 落版待下一轮 | Module_02 / Module_08 / 全局库 / Module_09（决策登记） | MVP | 使用中 |
| v1.51 | 2026-09-05 | 修改 | 终验前版本清单全量刷新（prototype-designer 检查项 19 回写）：§1 总表 PRD 版本对齐至各 PRD 2026-09-04 最新修订版——Module_00 v1.3→v1.4、Module_01 v3.30→v3.35、Module_02 v1.8→v1.11、Module_04 v1.2→v1.5、Module_06 v2.6→v2.9、Module_07 v2.26→v2.30、Module_08 v1.7→v1.11、Module_09 v1.53→v1.56；原型版本以 `docs/prototypes/module-XX/package.json` 为准核对，Module_04 原型 v1.2→v1.5、Module_09 原型 v1.51→v1.52；**对齐状态翻转 3 项**：Module_02 / Module_07 / Module_08 由「✅ 对齐」改「⚠️ 原型待升级」（三者 PRD 头部均标注原型待对齐，且 M08 v1.8 决策 61 静默 API v1→v2 迁移为 MVP 契约变更）。同步 `04_Implementation_Map.md` / `05_Code_Implementation_Plan.md` 头部版本串（终验 1.1 要求三处一致）；订正 `module-08/task-sequence.yaml` T08-04 由 `/api/v1/silences` 改 `/api/v2/silences`（决策 61，代码已按 v2 落地，仅任务描述滞后） | Module_00 / 01 / 02 / 04 / 06 / 07 / 08 / 09 | 文档自身 | 使用中 |
| v1.50 | 2026-09-02 | 修改 | v0.2 范围定版同步（2026-09-02 v0.2 规划评审，详见 `docs/05-execution-records/module-00-infrastructure/v0.2-scope-and-architecture-decisions.md`）：Module_01 v3.29→v3.30（克隆/草稿批量/业务看板/service_discovery 挪 v0.3，filter 53/扇出 54/网域覆盖表保留 v0.2，新增 scrape_port 与 cAdvisor 口径）；Module_03 v1.2→v1.3（整体后移 v0.3，覆盖改 MVP/v0.3/v1.0）；Module_06 v2.5→v2.6（ip_cidrs 后移 v0.3、新增划域指导原则）；Module_07 v2.25→v2.26（IP 推导后移 v0.3、新增 scrape_port 字段，覆盖改 MVP/v0.2/v0.3/v0.4/v1.0）；Module_09 v1.52→v1.53（端口解析链 + K8s 划域备忘）；Module_10 v1.1→v1.2（整体后移 v0.3，覆盖改 v0.3/v0.4/v1.0）；Roadmap v2.1→v2.2；各模块对齐均保持「✅ 对齐」（纯契约/归属调整，原型行为不变），M09 维持「⚠️ 原型待升级（决策 60）」 | Module_01 / 03 / 06 / 07 / 09 / 10 / Roadmap | v0.2 / v0.3 | 使用中 |
| v1.49 | 2026-09-02 | 修改 | 同步 coverage 三态口径修订与默认模板 `resource_id` 补齐（planner 阻塞项闭环）：Module_02 PRD v1.7→v1.8（coverage 不感知 M09 下发时序，「待采集」细分归 M01）；Module_07 PRD v2.24→v2.25 / 原型 v2.24→v2.25（「已下发未采到」含变更未确认下发情形、§5.13 默认模板补 `resource_id → resource_id` 映射、原型文案同步）；Module_01 PRD v3.28→v3.29（§5.10 coverage 边界声明、§9.1 稳定身份标签验收收紧为必须含 `resource_id`，原型行为不变）；M08 review-round1/design-decisions「校验失败留痕口径」待澄清项标注已闭环（PRD v1.7 §6.6 已统一为不落库，原型 README 措辞订正） | Module_01 / Module_02 / Module_07 / Module_08 | 文档自身 | 使用中 |
| v1.48 | 2026-08-31 | 修改 | 同步 Module_02/Module_08 决策 55/56/57 落版（M02/M08 边界与注入语义澄清）：Module_02 PRD v1.6→v1.7（§7 更名「注入与授权校验规则」、§7.2 三层语义——tenant_id 硬注入 / network_domain 授权集合收敛（授权=全部网域时不注入）/ 前端筛选纯 UX；§11.1 验收项 9 收敛为 API 契约，告警状态页归 M08；§1 存储可替换性决策点——扁平拓扑 + 预留 VictoriaMetrics + 标签隔离契约）；Module_08 PRD v1.4→v1.5（§5.4 告警状态页归属本模块 + AM 代理服务端授权过滤；§5.2 静默 matcher 授权校验；§9.2 技术验收补两条；Change Log 去编号收敛 3 版、旧版迁 design-decisions）；新建设计思路文档 `docs/05-execution-records/module-02/m02-vs-m08-boundary-and-injection-design.md`；两模块原型待升级 | Module_02 / Module_08 | 文档自身 | 使用中 |
| v1.47 | 2026-08-31 | 修改 | 同步 Module_06/Module_07 决策 52 原型对齐：Module_06 PRD v2.4→v2.5 / 原型 v2.4→v2.5（NetworkDomain 新增 `ip_cidrs` 网段字段——供 M07 按 IP 推导网域归属、纯平台侧数据不回写 CMDB；网域心智原则「接入可见、消费隐藏」落页）；Module_07 PRD v2.23→v2.24 / 原型 v2.21→v2.24（根域归属四级解析链——显式指定 &gt; 冲突告警 &gt; IP 推导 &gt; default 兜底、blackbox 例外=发起侧；网域字段可留空由平台推导、含 Excel 导入；资源列表/详情新增「归属来源」标注）。两模块对齐改「✅ 对齐」 | Module_06 / Module_07 | 文档自身 | 使用中 |
| v1.43 | 2026-08-22 | 新增 | 新增「模块 ↔ 代码目录映射」章节（§4）及目录归属原则：源码目录按功能域命名、不按模块号命名；模块归属由分支名 + commit message + 执行记录 + 本表四层追溯；跨模块共享代码不归入单一模块。同步更新 agent 规范：backend-developer.md 要求 Go 包注释注明模块/PRD 路径；frontend-developer.md 要求页面入口 JSDoc 注明模块/PRD 路径 | 全部模块 / Agent 规范 | 文档自身 | 使用中 |
| v1.42 | 2026-08-21 | 修改 | PRD 状态推进 ready（可开发）：Module_01（v3.26）、Module_06（v2.2/原型 v2.3）、Module_07（v2.21）、Module_09（v1.50）四模块 PRD 状态由 设计中/prototyping 流转为 `ready`（两段式评审通过、原型对齐、可用于后续 plan-maintainer 派生 Implementation Plan）；本表对应四行使序状态列同步更新，对齐保持「✅ 对齐」 | Module_01 / Module_06 / Module_07 / Module_09 | 文档自身 | 使用中 |
| v1.41 | 2026-08-21 | 修改 | 两段式评审返工修复 + 版本对齐：Module_07 原型 v2.20→v2.21（host 导入模板补 `instance_name` 列、导入弹窗去技术术语 upsert/M01/M09/offline、`app_name`/`cluster` 差异化必填）；Module_06 原型 v2.2→v2.3（R1 ID 前缀 `nd-*`→`<deploy_code>-<domain_code>` 默认 `mc`、`default` 无前缀；R2 `multi_site_enabled` 裸暴露去用户层；R3 去「v0.2+」阶段标记；R4 未纳管网域行内「配置纳管（M09）」跳转 Module_09 预选该网域——M09 `NetworkDomainsPage` 补读 `?network_domain=` 自动打开纳管弹窗；R5 禁用=冻结确认补充 M01/M09 传导影响）；Module_09 原型版本声明 v1.49→v1.50（对齐 PRD）。三模块对齐保持「✅ 对齐」 | Module_07 / Module_06 / Module_09 | 文档自身 | 使用中 |
| v1.40 | 2026-08-21 | 修改 | 原型同步落版（决策 28~31 / 31-M1~31-M3 / 30-3）：Module_01 原型 v3.25→v3.26（认证与TLS 折叠面板 auth_type/basic/bearer + tls_skip_verify + ca_file、冻结网域 Select 置灰+Tooltip、提交兜底校验冻结域/认证必填、mock 补 frozen legacy-dc 与 default 域下线 res-mw-005）；Module_07 原型 v2.9→v2.20（「未监控」筛选器 + 采集状态列 + Tab 未监控计数、资源列表 is_monitored 只读映射）；Module_09 原型 v1.49→v1.50（scrape_configs 透传 basic_auth/authorization/tls_config示例 + blackbox HTTP/HTTPS tls_config、ReviewNote 补冻结域不生成变更单/认证TLS透传/offline 排除说明）。三模块对齐均改「✅ 对齐」，原型版本与 PRD 版本拉齐 | Module_01 / Module_07 / Module_09 | 文档自身 | 使用中 |
| v1.39 | 2026-08-21 | 修改 | 同步 Module_01/M07/M09 四模块 MVP 残余缺陷收敛（决策 28~31，落地结构对齐决策 28 与 offline 提级决策 29 的剩余部分）。①PRD 落版：Module_01 PRD v3.25→v3.26（§5.4 新增认证/TLS 最小集 auth_type/username/password|token/tls_skip_verify/ca_file——决策 31、冻结域禁止新建 Job 及新增实例——决策 30、§6.2/§11/§9 同步）；Module_09 PRD v1.49→v1.50（§3.3 认证/TLS 透传映射进 scrape_configs——决策 31、§3.4 冻结域不生成新变更单——决策 30、§3.5 变更状态回写 deployed 提前 MVP——决策 31-M2、§5.1 删除未指定网域资源自动归 default 兜底——决策 31-M3）；Module_07 PRD v2.19→v2.20（§8.1 offline 排除提级 P0——决策 29、资源列表「未监控」筛选——决策 31-M1）。②原型：M01/M07/M09 原型本次未同步，对齐状态改「⚠️ 原型待同步」。③M06 §9.2 决策 30-3 联动用例已存在、无需新增 | Module_01 / Module_07 / Module_09 | 文档自身 | 使用中 |
| v1.38 | 2026-08-21 | 修改 | 同步 Module_01/M09 四模块 MVP 残余缺陷收敛（决策 28/29）。①PRD 落版：Module_01 PRD v3.24→v3.25（§3.1/§5.4/§8/§9 `offline` 排除提级 P0——候选集 `Resource.status=offline` 实例显示但置灰不可选、已选实例转 offline 后 M09 配置生成跳过）；Module_09 PRD v1.48→v1.49（§1/§3.1.1/§5.1 网域契约结构性对齐——NetworkDomain 行政模型以 Module_06 为单一事实来源、删除旧语义、ID 规则置 M06；§3.3 实例过滤 offline 排除提级 MVP 必实现）。②原型同步：Module_01 原型 v3.24→v3.25（实例候选 Transfer 对 offline 置灰 disabled + 标题/描述标「已下线」、实例选择区 ReviewNote 提级改写、新增 default 域 mysql 下线副本 res-mw-005 演示）；Module_09 原型 v1.48→v1.49（「配置产物结构说明」ReviewNote offline 提级改写、targets mock 注释补 offline 过滤语义、MainLayout 版本声明同步）。两模块对齐标记「✅ 对齐」 | Module_01 / Module_09 | 文档自身 | 使用中 |
| v1.37 | 2026-08-21 | 修改 | 同步 Module_01/M09 规则文件挂载（决策 38-1，MVP 补齐 M01↔M09 规则链路契约）：Module_01 PRD v3.23→v3.24 / 原型 v3.23→v3.24（§5.5 MonitoringRule 启用 content_mode=yaml_passthrough + rule_content、§3.1 新增「规则文件挂载」功能行、§3.2 澄清与 v0.3 字段级编辑边界、§6.2 规则 API 契约、§9 验收新增挂载项；原型规则编辑页新增「文件挂载」视图——粘贴/上传 rules.yml + 基础 YAML 校验 + 规则条数展示、保存/启停/删除后经 M09 变更单确认下发、change_status 全链路回写，「字段化编辑」为 v0.3 预览）；Module_09 PRD v1.47→v1.48 / 原型 v1.47→v1.48（rules.yml 生成改为 rule_content 原样透传并入、不按字段派生，v0.3 structured 后恢复派生；注释/ReviewNote/规则分组说明同步）；两模块对齐改「✅ 对齐」；产品路线图 §2.4 MVP 规则管理口径随决策 38-1 同步（roadmap v1.7） | Module_01 / Module_09 / Roadmap | 文档自身 | 使用中 |
| v1.36 | 2026-08-21 | 修改 | 同步 Module_09 MVP 缺憾补漏（决策 42 系列）：PRD v1.46→v1.47 / 原型 v1.46→v1.47（同域 pending「后单取代前单」superseded、校验失败草稿「重新校验/废弃」闭环、`local` reload 失败重试入口、configgen 异常「生成失败」态、§9 验收收敛 MVP 边界标注 {v0.2}），对齐保持「✅ 对齐」（本轮为契约与闭环补口子、不改原型行为） | Module_09 | 文档自身 | 使用中 |
| v1.35 | 2026-08-19 | 修改 | 同步 Module_06 决策 23 MVP 缺憾补漏：PRD v2.1→v2.2（§1 无鉴权声明 + 种子数据 migration upsert；§4 新增 4.0 MVP 网域登记流程；§5.1 suspended 预留标注；§5.2 id deploy_code 前缀来源 / 登记归属创建后不可变更 / authorized_tenant_ids 可选缺省；§6.2 新增 GET zone-types 只读接口 + DELETE 空域删除 + PUT 移除登记归属 + PATCH 禁用冻结语义；§9 补 6 条技术 + 3 条用户验收；§11.2 删除限制规则）/ 原型 v2.1→v2.2（登记归属编辑只读、授权租户去必填默认回填、禁用二次确认展示影响范围、空网域删除按钮、mock zone-types 接口来源注释），对齐保持「✅ 对齐」 | Module_06 | 文档自身 | 使用中 |：Module_01 PRD v3.22→v3.23 / 原型 v3.20→v3.23（§3.1 实例选择与实例选择方式补 offline/maintenance 排除目标语义、M01-ARCH-01「未纳入任何 Job」筛选器声明 MVP 不保证随本模块落地；实例选择区 ReviewNote + 版本声明同步）；Module_09 PRD v1.45→v1.46 / 原型 v1.45→v1.46（§3.3 实例过滤声明 offline 排除目标语义、MVP 不保证随 M01 节奏落地；配置产物结构 ReviewNote + 版本声明同步）；本轮为契约声明，不改原型行为，两模块对齐改「✅ 对齐」 | Module_01 / Module_09 | 文档自身 | 使用中 |
| v1.33 | 2026-08-19 | 修改 | 同步 Module_07 评审结论落版（2026-08-19 第三轮）：PRD v2.18→v2.19 / 原型 v2.8→v2.9（业务字典 MVP 运维口径——配置 business_domains.yaml 热加载 + 兜底 infra + 未登记报错可执行指引；导出模板后端生成静态 xlsx 内置取值说明 sheet；upsert 不删除 + 批量下线动线；offline 排除语义降级随 M01 节奏；5.17 示例补 biz_code），对齐改「✅ 对齐（v2.17~v2.19 已同步）」 | Module_07 | 文档自身 | 使用中 |
| v1.32 | 2026-08-19 | 修改 | 同步 Module_09 external_labels 标签集收敛（2026-08-19 决策 19/23）：PRD v1.44→v1.45（§3.3.1 external_labels 移除 tenant_id、保留 network_domain_id / zone_type / replica 部署级元数据；§3.3/§5 明确 biz 与 tenant 均由 M07 LabelTemplate 以 target 级注入、M09 不单独注入；§5 配置目录 MVP 按 network_domain 分目录、多租户命名空间 {v0.2+} 占位）/ 原型 v1.44→v1.45（prometheus.yml 各域 external_labels 收敛、targets 注入链路注释补全、测试断言与 ReviewNote 同步），对齐保持「✅ 对齐」 | Module_09 | 文档自身 | 使用中 |
| v1.31 | 2026-08-19 | 修改 | 同步 Module_06 决策 21~22 落版 + §11.1 筛选补齐：PRD v2.0→v2.1（§4.3/§9.2/§10 `business_domain` 改 `biz_code` / `biz_name`，指标标签保持 `biz`）/ 原型 v2.0→v2.1（租户页名称/状态筛选、网域页登记归属/网络区域类型/状态/授权租户筛选、审计日志页操作类型/操作人/操作时间筛选），对齐保持「✅ 对齐」 | Module_06 | 文档自身 | 使用中 |
| v1.30 | 2026-08-19 | 修改 | 同步 Module_06 决策 18~20 落版（网域可跨租户共享）：PRD v1.9→v2.0（§3.2 清除「1 网域 : 1 租户 / 禁止跨租户共享」残留，改网域为部署级资源、可跨租户共享、租户通过授权 scope 使用；§5.2 新增 `authorized_tenant_ids`、`tenant_id` 语义改登记归属；§6.2 创建不再校验唯一租户归属；§5.2 id 前缀改部署级）/ 原型 v1.9→v2.0（网域页「所属租户」改「登记归属」固定 platform_admin + 新增「授权租户」多选与表格列、ID 部署级前缀 nd-；租户页 multi_site_enabled 语义改「被授权使用多个网域」；mock nd-default 演示 1 网域 : N 租户共享），对齐保持「✅ 对齐」 | Module_06 | 文档自身 | 使用中 |
| v1.29 | 2026-08-19 | 修改 | 同步 Module_09 业务-网域正交性对齐（2026-08-19 业务登记与网域-业务正交性决策）：PRD v1.43→v1.44（§5.1 补「网域与业务正交」说明）/ 原型 v1.43→v1.44（biz 标签注入链路说明补全 + 多业务共用 1 网域演示 + 业务归属变更待确认草稿 draft-gov-004），对齐保持「✅ 对齐」 | Module_09 | 文档自身 | 使用中 |
| v1.28 | 2026-08-19 | 修改 | 同步 Module_06 原型对齐（PRD v1.7~v1.9）：原型 v1.3→v1.9（网域登记纳入 MVP、租户与业务解耦——移除 CMDB 业务列/表单项、租户 mock 与用户表 tenantName 改为团队/组织语义（平台运营部 / 电商研发部 / 金融运维部）、README 更新），对齐改「✅ 对齐（v1.7~v1.9 已同步）」 | Module_06 | 文档自身 | 使用中 |
| v1.27 | 2026-08-19 | 修改 | 同步 Module_06 决策 12~17 落版收尾：PRD v1.8→v1.9（§3.2 补「租户、网域、业务三者正交」说明；§10 术语表 `cmdb_business_id`/`cmdb_business_path` 改指 M07 业务字典条目），原型仍 v1.3，对齐保持「⚠️ 原型待同步（v1.7~v1.9）」 | Module_06 | 文档自身 | 使用中 |
| v1.26 | 2026-08-18 | 修改 | M07 评审结论落版跨模块同步：Module_07 PRD v2.15→v2.16（评审结论落版：导入 upsert、Application 粒度=服务实例、取消 is_monitored、status 行为语义、网域登记纳入 MVP、必填差异化、模板快照等，对齐改「⚠️ 原型待同步」）；Module_06 PRD v1.6→v1.7（网域登记纳入 MVP，原型待同步）；Roadmap v1.5→v1.6（五类资源、M06 MVP 补网域登记、去隐藏网域概念、排期待重估）；全局用户故事库 ARCH-03 落点调整；全局架构网域 Owner 口径回写（行政归 M06 / 纳管归 M09） | Module_06 / Module_07 / Roadmap / 全局库 / 全局架构 | 文档自身 | 使用中 |
| v1.25 | 2026-08-18 | 修改 | 同步 Module_09 PRD 与原型对齐（决策 41）：PRD v1.41→v1.42 / 原型 v1.40→v1.42（未同步按成因分档标签化展示：待确认变更 / 生效中 / 本地校验失败；进程异常行级高亮 + 抽屉高危横幅；平铺表新增 Edge Sync Agent 状态列；manual_override 术语统一「人工覆盖」），对齐改「✅ 对齐」 | Module_09 | 文档自身 | 使用中 |
| v1.24 | 2026-08-18 | 修改 | 同步 Module_01/M09 草稿/批量提交生效方案迭代：Module_01 PRD v3.21→v3.22（新增 `draft_status`/`change_status`，草稿仅新建阶段单向 draft→ready、不做快照；MVP 四态占位；v0.2 Job 草稿 + 批量提交生效 + 克隆 Job；v0.3 规则草稿），原型版本仍为 v3.20，对齐改「⚠️ 原型待同步」；Module_09 PRD v1.40→v1.41（配置生成过滤 draft_status=ready、change_status 扩展 deployed、回写 M01 规则），原型版本仍为 v1.40，对齐改「⚠️ 原型待同步」 | Module_01 / Module_09 | 文档自身 | 使用中 |
| v1.23 | 2026-08-18 | 修改 | 同步 Module_07 PRD 按 prototype-designer.md 骨架（章节 1-11 冻结 + Change Log）重构：PRD 版本 v2.14→v2.15，状态保持设计中，原型版本仍为 v2.7（结构改造不涉及原型），对齐改「⚠️ 原型待同步（v2.15 章节骨架结构改造，不涉及原型）」；完整历史迁 design-decisions.md（补迁 v2.7-v2.12）；本次为结构改造，不改变产品语义 | Module_07 | 文档自身 | 使用中 |
| v1.22 | 2026-08-18 | 修改 | 同步 Module_01 PRD 按 prototype-designer.md 骨架（章节 1-11 冻结 + Change Log）重构：PRD 版本 v3.20→v3.21，状态保持设计中，原型版本仍为 v3.20（结构改造不涉及原型），对齐改「⚠️ 原型待同步（v3.21 章节骨架结构改造，不涉及原型）」；完整历史迁 design-decisions.md（补迁 v3.16/v3.17/v3.18）；本次为结构改造，不改变产品语义 | Module_01 | 文档自身 | 使用中 |
| v1.21 | 2026-08-18 | 修改 | 同步 Module_06 PRD 按 prototype-designer.md 骨架（章节 1-11 冻结 + Change Log）重构：PRD 版本 v1.5→v1.6，状态保持设计中，原型版本仍为 v1.3（结构改造不涉及原型），对齐改「⚠️ 原型待同步（v1.6 章节骨架结构改造，不涉及原型）」；本次为结构改造，不改变产品语义 | Module_06 | 文档自身 | 使用中 |
| v1.20 | 2026-08-18 | 修改 | 同步 Module_09 PRD 按 prototype-designer.md v1.27+ 骨架改造：PRD 版本 v1.38→v1.40，状态保持 prototyping，原型版本同步 v1.40，对齐改「✅ 对齐（v1.40 已按 prototype-designer.md 骨架改造）」；本次为结构改造，不改变产品语义 | Module_09 | 文档自身 | 使用中 |
| v1.19 | 2026-08-17 | 修改 | 同步 Module_01/M09 版本与原型对齐：Module_01 v3.19→v3.20（决策 D28 规则编辑引导确认 + 规则列表下发状态列）；Module_09 v1.37→v1.38（决策 38 分级下发 target 自动生效 / 校验失败「重新校验」动线 / 重试语义）；两模块原型同步（M01 v3.20 / M09 v1.38），对齐保持 ✅ | Module_01 / Module_09 | 文档自身 | 使用中 |
| v1.18 | 2026-08-17 | 修改 | 同步 Module_01/M09 原型对齐（12 条断点修复）：Module_01 原型 v3.2→v3.19（下发状态列 + 保存/启停/删除引导「变更将由 M09 生成变更单」+ URL 网域预选 + multi_site_enabled=false）；Module_09 原型 v1.24→v1.37（配置变更确认页网域级配置同步状态 + 单域收敛 + 校验失败草稿迁至 default 域可达 + DeploymentsPage 重试/详情 Drawer/定位 + EdgeAgentsPage 五档同步状态引导修正 + mock 数据修复），对齐改「✅ 对齐」 | Module_01 / Module_09 | 文档自身 | 使用中 |
| v1.17 | 2026-08-17 | 修改 | 同步 Module_01/M09 版本对齐：Module_01 v3.18→v3.19（第二十九轮 MVP 单域动线闭环，决策 D27——规则编辑 UI 确认 v0.3 不进 MVP / change_status 提前 MVP + 保存引导 + §4 步骤 3 补人工确认 + pull 口径）；Module_09 v1.36→v1.37（决策 37——local 通道网域级配置同步状态承载于配置变更确认页 + reload 失败重试路径 + 检测状态可观测 P1→P0）；两模块原型本轮不修改，对齐改「⚠️ 原型待同步」 | Module_01 / Module_09 | 文档自身 | 使用中 |
| v1.16 | 2026-08-15 | 修改 | 同步 Module_09 原型对齐：PRD v1.31→v1.32 / 原型 v1.23→v1.24（v1.31 网闸拓扑 center_endpoint/zone_type/网闸约束 + v1.32 告警规则职责重构 rules group 派生/审批分级 alertmanager.yml 由 M08 管理），对齐改「✅ 对齐」 | Module_09 | 文档自身 | 使用中 |
| v1.15 | 2026-08-15 | 修改 | 网域两层关系落地同步（政务云互联网区/政务外网区需求讨论）：Module_06 v1.3→v1.4（zone_type 部署级字典 + 网域定义唯一入口）；Module_07 v2.11→v2.12（区域属性 SSOT + 网域列下拉 + 唯一性按网域收敛）；Module_09 v1.30→v1.31（center_endpoint + 绝对下载地址 + 网闸连接约束 + MVP 后实测标记）；三模块原型待同步，对齐改 ⚠️ | Module_06 / Module_07 / Module_09 | 文档自身 | 使用中 |
| v1.14 | 2026-08-15 | 修改 | ready 前内容缺口修正后同步：Module_01 PRD v3.9→v3.10 / 原型 v3.1→v3.2；Module_07 PRD v2.10→v2.11 / 原型 v2.6→v2.7；Module_09 PRD v1.29→v1.30 / 原型 v1.22→v1.23 | Module_01 / Module_07 / Module_09 | 文档自身 | 使用中 |
| v1.13 | 2026-08-14 | 修改 | 同步 Module_01 原型对齐：原型 v2.9→v3.0（双层锚点落地：指标库按 CI 类型分组 + 来源标注 + 语义域、默认采集器 UI + is_default、删「登记自定义模板 P2」占位入口、Job 手填模式），对齐改「✅ 对齐」 | Module_01 | 文档自身 | 使用中 |
| v1.12 | 2026-08-14 | 修改 | 同步 Module_01 版本对齐：v3.7→v3.8（指标库锚点演进 + Exporter 模板降级为采集实现，第十七轮 + 用户评审修正双层锚点；原型待同步 v3.0，对齐改 ⚠️） | Module_01 | 文档自身 | 使用中 |
| v1.11 | 2026-08-14 | 修改 | 同步 Module_01 版本对齐：v3.6→v3.7 + 原型 v2.8→v2.9（两库定位区分与导航归组 / 自定义微服务仍属 application_http + et-app-go 样本 / 业务视图 Tab + 采集落地列，第十六轮评审新问题落地） | Module_01 | 文档自身 | 使用中 |
| v1.10 | 2026-08-14 | 修改 | 同步 Module_04 原型对齐：原型 v1.1→v1.2（BlueKing 字段映射 business_domain 同步说明 + 待分类 CI 新类型引导闭环），对齐改「✅ 对齐」；补全 M07 完整历史 v2.5 行 | Module_04 / Module_07 | 文档自身 | 使用中 |
| v1.9 | 2026-08-14 | 修改 | 同步 Module_07 版本对齐：v2.8→v2.9 + 原型 v2.4→v2.5（组合字段 MVP 内部默认：前台隐藏 + 默认模板标注，第十七轮） | Module_07 | 文档自身 | 使用中 |
| v1.8 | 2026-08-14 | 修改 | 同步 Module_09 原型对齐：原型 v1.20→v1.21（targets labels 补 app/biz 注入 + 确认页 ITIL 边界声明 + Agent 断网自治说明），对齐改「✅ 对齐」 | Module_09 | 文档自身 | 使用中 |
| v1.7 | 2026-08-14 | 修改 | 同步 Module_09 版本对齐：v1.27→v1.28（审批分层 ITIL 边界声明 + 版本一致性语义 + 断网草稿说明，第十六轮；原型待同步） | Module_09 | 文档自身 | 使用中 |
| v1.6 | 2026-08-14 | 修改 | 同步 Module_01 原型对齐：原型 v2.7→v2.8（角色切换 + 业务指标库页 + application_http 语义 + biz 筛选），对齐改「✅ 对齐」；两段式评审通过（MVP 范围） | Module_01 | 文档自身 | 使用中 |
| v1.5 | 2026-08-14 | 修改 | 同步 Module_01 版本对齐：v3.5→v3.6（业务指标动线分离：登记动作≠语义所有权 + 聚合视图 v0.2+，第十五轮；原型待同步） | Module_01 | 文档自身 | 使用中 |
| v1.4 | 2026-08-14 | 修改 | 同步 Module_01 版本对齐：v3.4→v3.5（业务指标库 BusinessMetric，第十四轮；原型待同步） | Module_01 | 文档自身 | 使用中 |
| v1.3 | 2026-08-14 | 修改 | 同步跨模块版本对齐（第十三轮 + 评审前完善）：Module_09 v1.26→v1.27（业务指标标签规范消费标注，对齐）；Module_01 v3.4（application_http + filter + service_discovery，原型待同步）；Module_04 v1.2（business_domain 同步映射，原型落后） | Module_01 / Module_04 / Module_09 | 文档自身 | 使用中 |
| v1.2 | 2026-08-14 | 修改 | 同步跨模块版本对齐（第十三轮）：Module_01 v3.3→v3.4（application_http 语义 + 新类型引导闭环 + filter 字段语义 + service_discovery 预留，原型待同步）；Module_04 v1.1→v1.2（business_domain 同步映射，原型落后） | Module_01 / Module_04 | 文档自身 | 使用中 |
| v1.1 | 2026-08-14 | 修改 | 同步 Module_07 版本对齐：PRD v2.7→v2.8、原型 v2.3→v2.4、对齐改为「⚠️ 原型待同步」（第十二轮 标签双场景治理 + 业务指标关联，仅文档层落地） | Module_07 | 文档自身 | 使用中 |
| v1.0 | 2026-08-13 | 初始 | 创建模块目录与版本对齐总表 | 全部模块 | 文档自身 | 使用中 |
