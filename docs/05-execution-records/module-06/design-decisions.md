# 设计决策记录：module-06

## 会议/对齐信息

- 日期：2026-08-02
- 参与 Agent：prototype-designer、orchestrator
- 触发原因：基于现有 PRD 生成可点击原型并验证设计理解

## 关键决策

### 决策 1：原型风格与呈现方式

- 问题：原型采用何种视觉风格以便领导/业务方快速理解产品形态？
- 结论：采用火山引擎 Volcengine 设计 Token（主色 #0ECDEB、头部 #0B1B2A）作为原型风格，保持企业级云产品观感。
- 依据：用户需求 / 火山引擎品牌色参考
- 影响范围：docs/prototypes/module-06/ 全部页面

### 决策 2：模块原型独立拆分

- 问题：全模块统一原型还是按模块独立原型？
- 结论：按 prototype-designer 规范，每个模块产出独立的 Vite + React 原型项目，便于后续按模块评审、冻结与开发。
- 依据：`.kimi/agents/prototype-designer.md` 目录规则
- 影响范围：docs/prototypes/module-01/ ~ module-10/

### 决策 3：当前 PRD 范围确认

- 问题：当前 PRD 是否足以支撑原型验证？
- 结论：PRD v1.0 已覆盖本模块核心数据模型、页面与 MVP 边界，原型按 PRD 实现，未发现 [待验证] 技术缺口。
- 依据：docs/02-product-requirements/Modules/Module_06_Multi_Tenant.md
- 影响范围：原型页面范围

## 待确认项

- [ ] 领导评审后对页面信息架构的反馈
- [ ] 是否需要针对 MVP 范围进一步裁剪页面字段

## 关联文档

- `docs/02-product-requirements/Modules/Module_06_Multi_Tenant.md`
- `docs/prototypes/module-06/`

---

## 补充对齐：2026-08-15（M06 网域行政层与 M09 监控纳管层拆分）

- **参与 Agent**：prototype-designer
- **触发原因**：用户指出 M06「网域生命周期管理」与 M09「网域注册页」存在功能重叠，需明确职责边界。
- **关联模块**：Module_09、Module_01、Module_07。

### 关键决策

#### 决策 4：M06 作为 NetworkDomain 行政 Owner

- **问题**：M06 与 M09 各自应管理 `NetworkDomain` 的哪些字段和流程？
- **结论**：
  1. **M06 是网域的行政 Owner**：负责创建、编辑、禁用、删除网域，以及将网域分配给租户、设置配额。
  2. M06 网域创建/编辑表单只维护行政信息：`id`（可自动生成）、`name`、`tenant_id`、`status`；**不包含**监控参数（`agent_type`、`remote_write_url`、WAL 参数等）。
  3. `Tenant.network_domain_ids` 表示「该租户被授权可使用的网域列表」，但**不等于**这些网域都已接入监控；真正的「已纳管」状态由 M09 维护。
  4. `platform_admin` 租户拥有 `default` 管理域，系统预置且默认视为已存在；`default` 的监控纳管状态由 M09 特殊处理（中心 Prometheus 直接采集，不部署 Edge Agent）。
- **依据**：M06 是权限与平台管理模块，必须先确认租户对网域的拥有权；M09 是监控运维模块，只在用户有实际监控需求时执行纳管。
- **影响范围**：M06 PRD 3.1 / 5.1；网域创建/编辑页字段；租户详情页「被授权网域」展示。

#### 决策 5：租户内按网域授权的 RBAC 预留 {v1.0+}

- **问题**：用户提出「按不同域对人分权限管理」，是否把网域抬到租户之上？
- **结论**：
  1. **权限顶层仍是 Tenant**；网域不作为最高权限边界。
  2. v1.0+ 或外部 IAM 接入后，可在租户内角色策略中增加 `network_domain_id` scope，实现「某用户在医院 A 专网是 editor、在医院 B 专网是 viewer」。
  3. MVP / v0.2 阶段不做细粒度按域授权，仅保证租户级数据隔离。
- **依据**：M06 PRD 关键约束已明确「MetricCenter 不存在跨租户的全局平台管理员身份」。
- **影响范围**：M06 PRD 3.1 / 5.1；用户/角色权限设计（v1.0+）。

### 已确认项（2026-08-15）

- [x] M06 作为 `NetworkDomain` 行政 Owner，M09 作为监控纳管层（用户确认）。
- [x] 权限顶层仍是 Tenant，网域仅作为租户内二级授权维度（v1.0+）（用户确认）。

### 关联文档

- `docs/02-product-requirements/Modules/Module_06_Multi_Tenant.md`
- `docs/05-execution-records/module-09/design-decisions.md`（决策 25/26/27/28，跨模块主记录）

---

## 补充对齐：2026-08-18（PRD 章节骨架结构改造，决策 6）

- **参与 Agent**：prototype-designer
- **触发原因**：用户要求将 M06 PRD 按 prototype-designer.md 章节骨架（章节 1-11 编号冻结 + Change Log）重构，与 Module_09 v1.40 对齐。
- **结论（决策 6：结构改造不影响产品语义）**：本次仅调整文档结构——
  1. 正文本体去除 `{v1.4}` / `{v1.5}` / `（v1.3）` 等 PRD 演变标注（去历史化，历史迁移至本文件）；
  2. 数据模型表新增「UI 展示名」列，并补 `NetworkDomain` 行政字段表（完整模型仍以 Module_09 5.1 为唯一事实）；
  3. 新增章节：4 核心流程、6 接口设计、8 数据模型状态机、10 术语映射、11 前端交互契约；
  4. 原「隔离方案 / 平台管理子能力」归并进 3.x 核心功能；原 4.x / 5.x / 6.x / 7.x 重新编号；
  5. 验收标准拆为「用户验收 / 技术验收」并标注 P0/P1/P2；
  6. PRD Change Log 精简为最近 3 版，完整历史迁至下方「Change Log（完整历史）」。
- **影响范围**：`docs/02-product-requirements/Modules/Module_06_Multi_Tenant.md`、本文件、`Modules/README.md`（版本对齐总表 v1.21）；不涉及原型，原型版本仍为 v1.3，对齐保持 ⚠️。
- **PRD 版本**：v1.5 → v1.6。

---

---

## 补充对齐：2026-08-18（MVP 租户语义、登录模型与网域分类）

- **参与 Agent**：chenrt、AI 助手
- **触发原因**：用户澄清 MVP 为单租户场景，明确登录、网域创建、网域分类、管理域/边缘域边界等需求，并与当前 PRD 逐项对齐。
- **关联模块**：Module_03（网关认证，MVP 不实现）、Module_09（网域纳管层）。

### 关键决策

#### 决策 7：租户 = 数据隔离与权限作用域单位，不是公司客户

- **问题**：租户应定义为「公司客户」还是「客户内部的业务单元」？
- **结论**：
  1. 租户 = 数据隔离与权限作用域单位，与 BlueKing CMDB 的 Business 1:1 映射；
  2. 「公司客户」是更外层的运营/交付概念，由私有化部署实例本身承担；
  3. MVP 只存在系统预置租户 `platform_admin`，租户 CRUD 归 v0.2。
- **依据**：M06 PRD §3.3（Tenant ↔ CMDB Business 1:1）、§10 术语映射、原型 mock 租户示例（平台运营部/电商业务/金融业务）。
- **影响范围**：M06 PRD 3.3 / 10 术语映射、MVP 范围说明。

#### 决策 8：MVP 不实现登录，但数据模型按「租户 + 用户 + 密码」三段式预留

- **问题**：登录页是否要走「租户名 + 密码」两段式？
- **结论**：
  1. 长期方向为「租户名 + 用户名 + 密码」三段式：租户名定作用域、用户名定操作人、密码做认证；
  2. MVP 不开发 M03 网关认证功能，网域管理接口在 MVP 期间处于无细粒度鉴权状态，依赖部署网络隔离；
  3. PRD 需显式声明 MVP 无鉴权是已知风险；
  4. 即使 MVP 只有 `platform_admin` 租户管理员，数据模型也要从「用户挂靠租户」开始设计，避免 v1.0 接入 IAM/SSO 时推翻认证体系。
- **依据**：M06 PRD §6.3（用户/权限 v1.0+ 或外部 IAM 承接）。
- **影响范围**：M06 PRD 6.3、MVP 风险说明；M03 PRD 认证/登录页范围（MVP 不实现）。

#### 决策 9：网域术语统一为「管理域 / 边缘域」，且全系统仅有一个管理域

- **问题**：用户原话中的「配置域」与 PRD 中的「边缘域」如何统一？
- **结论**：
  1. 统一使用「管理域」与「边缘域」：管理域由系统预置（`default`），其余用户创建的采集网域均为边缘域；
  2. 全系统有且只有一个管理域；
  3. 创建网域接口不接受 `domain_type=management`；编辑接口不允许修改 `domain_type`。
- **依据**：M06 PRD 3.2 / 5.2 `domain_type` 枚举与状态机。
- **影响范围**：M06 PRD §10 术语映射、网域创建/编辑接口约束、验收标准。

#### 决策 10：`zone_type` 受控字典 + `category` 自由分类双字段

- **问题**：用户希望网域分类「用户想怎么管就怎么管」，但 PRD 已规定 `zone_type` 来自部署级字典、不开放自由文本。
- **结论**：
  1. `zone_type` 保持受控：其值会进入 M09 指标标签，必须受控以保证 PromQL 聚合查询稳定；
  2. 受控体现在「值集受控」，用户填写时从下拉框选择，不能自由输入；
  3. 用户的自由分类诉求由新增字段 `category`（行政分组）承接，`category` 不进标签、可自由文本或租户级自维护；
  4. 字典治理规则：条目 = `code`（进标签，不可变） + `display_name`（展示用，可改） + `description`；被引用的条目只能停用不能删除；后端校验 `zone_type` 属于启用字典项；
  5. MVP 采用配置文件预置字典 + 只读接口 `GET /api/v2/platform/zone-types`；v0.2 升级为「平台预置 + 租户扩展」两级字典，并增加字典维护入口。
- **依据**：M06 PRD 3.2（`zone_type` 部署级字典与标签注入语义）、验收标准 L307（zone_type 不开放自由文本）。
- **影响范围**：M06 PRD 3.2 / 5.2 / 6.2 / 11.2；网域数据模型新增 `category` 字段（待确认是否进 MVP）；M09 指标标签注入。

#### 决策 11：网域 ID 从 MVP 起强制租户前缀

- **问题**：PRD 建议 `network_domain_id` 采用 `<tenant_id>-<domain_code>` 前缀，但 MVP 单租户下是否执行？
- **结论**：
  1. MVP 起所有用户创建的网域 ID 强制按 `<tenant_id>-<domain_code>` 自动生成；
  2. `default` 管理域是唯一例外（历史预置）。
- **依据**：M06 PRD 3.2 `network_domain_id` 全局唯一与租户前缀建议、技术验收 9.2。
- **影响范围**：M06 PRD 4.1 / 6.2 / 9.2；网域创建接口实现；v0.2 多租户上线无需数据迁移。

### 已确认项（2026-08-18）

- [x] 租户语义 = 业务/CMDB Business，不是公司客户（用户确认）。
- [x] MVP 不开发 M03 登录，但模型按「租户 + 用户 + 密码」三段式预留（用户确认）。
- [x] 网域术语统一为「边缘域」，管理域唯一，其余均为边缘域（用户确认）。
- [x] `zone_type` 保持受控字典，用户自由分类诉求由 `category` 字段承接（用户确认）。

### 待确认项（2026-08-18 新增）

- [ ] 网域禁用级联语义：网域下存在 M07 资源或 M09 已纳管 EdgeAgent 时，禁用是直接禁止还是允许但提示影响范围？
- [ ] 管理域唯一性强约束：是否在数据库层加唯一索引 `domain_type = 'management' AND is_deleted = false`？
- [ ] `platform_admin` 租户与 `default` 管理域的种子数据初始化路径：Go migration 启动时 upsert，还是部署脚本一次性 seed？
- [ ] MVP 是否在 `NetworkDomain` 行政字段表增加 `created_by` / `updated_by`（MVP 固定为 `system`），为 v1.0 审计留结构？
- [ ] `category` 自由分类字段是否进入 MVP 网域表单，还是放 v0.2？

### 关联文档

- `docs/02-product-requirements/Modules/Module_06_Multi_Tenant.md`
- `docs/05-execution-records/module-06/2026-08-18-tenant-and-network-domain-alignment.md`

---

## Change Log（完整历史）

> 完整逐版详情（含影响范围），主文档只保留最近 3 版一句话摘要。

| 版本 | 日期 | 变更类型 | 变更内容 | 影响范围 | 产品版本影响 | 状态 |
|------|------|----------|----------|----------|--------------|------|
| v1.6 | 2026-08-18 | 修改 | 按 prototype-designer.md 骨架（章节 1-11 冻结 + Change Log）重构：去历史化、数据模型加「UI 展示名」列、补 5 个新章节、验收拆两层、Change Log 精简 | 全文档结构 | 文档自身 | 设计中 |
| v1.5 | 2026-08-16 | 修改 | 刷新 `multi_site_enabled` 语义（决策 31 对齐）：①明确为「是否允许该租户创建/管理多个网域的行政能力开关」，不控制 M09 页面入口（M09 入口由数据驱动）；②3.1 新增「网域地位」说明——网域是逻辑操作上下文与采集边界，不是控制面层级；③5.1 `multi_site_enabled` 字段描述同步更新 | 3.1 租户与网域关系、5.1 数据模型 | MVP / v0.2 | 设计中 |
| v1.4 | 2026-08-15 | 修改 | 网域两层关系落地（需求讨论：政务云互联网区/政务外网区逻辑隔离）：①3.1 新增「网域定义唯一入口原则」（单一事实来源，下游只引用 `network_domain_id`）；②3.1 新增 `zone_type` 行政字段（网络区域类型，部署级字典不做死枚举：政务云预置互联网区/政务外网区，公有云预置 region；AZ 不进 NetworkDomain）；③M06 行政字段范围同步（`zone_type` 归 M06，`center_endpoint` 归 M09）；④7 验收补 2 条；原型待同步 | 3.1 租户与网域关系、验收 | MVP / v0.2 | 设计中 |
| v1.3.1 | 2026-08-15 | 修改（原型对齐） | 原型落地 v1.3 职责边界：①module-06 原型新增「网域管理」页面（网域行政唯一创建入口：名称/租户/状态/描述，ID 按租户前缀自动生成，编辑/禁用，管理域不可禁用）；②租户表单「被授权网域」下拉数据源从硬编码改为网域管理行政记录，选项标注纳管状态；③module-09 原型同步移除「新增网域」，改为「纳管网域」 | 原型 `docs/prototypes/module-06/`、`docs/prototypes/module-09/` | MVP / v0.2 | 设计中 |
| v1.3 | 2026-08-15 | 修改 | 明确 M06 与 M09 网域职责边界：M06 为 `NetworkDomain` 行政 Owner（创建/分配/配额），M09 为监控纳管 Owner；`Tenant.network_domain_ids` 含义改为「被授权可使用的网域列表」，不等于已纳管；网域创建/编辑表单只维护行政信息 | 3.1 租户与网域关系、5.1 数据模型 | MVP / v0.2 | 设计中 |
| v1.0 | 2026-07-31 | 初始 | 模块 PRD 初始版本 | 全部 | MVP / v0.2 / v0.4 / v1.0 | draft |
