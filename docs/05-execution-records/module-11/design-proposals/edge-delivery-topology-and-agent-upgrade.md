# M11 边缘部署拓扑选址与 Agent 升级策略设计说明（四轮讨论汇总）

> **状态**: approved —— 2026-09-20 第五轮用户拍板全部待决决策（D1 = a、D2 = a、D3 = b、D4 = b，见 §8），本文结论全部生效并已回填 PRD v0.4。本文只做设计论证，**不动代码**。
> **日期**: 2026-09-20（第三轮 2026-09-18；第四轮 2026-09-20 用户确认方案 A；第五轮 2026-09-20 拍板 D1-D4）
> **触发**: 用户四轮提问 + 第五轮拍板——① Edge Sync Agent 能否由中心推送安装 vs 页面下载人工安装；② 真实三区（政务外网区 / 政务云互联网区 / 腾讯云公有云区）拓扑下中心放哪、要不要代理服务器；③ 备选方案「中心放腾讯云 + 互联网区跳板机」是否可行；④ **（第四轮）用户澄清实际采用方案 A**（中心整体在 G2，vmagent 分装 T / G1），并确认 MVP 不引入 PostgreSQL、采集器强制 vmagent 单一化；⑤ **（第五轮）按本文建议拍板 D1-D4**：部署形态与 A′ 备选入 PRD、Agent 自升级 {v0.3} 带门槛引入、中继选型约束一句话入 PRD。
> **关联**: `Module_11_Edge_Access_and_Agent_Delivery.md` v0.2（draft，2026-09-17）、`module-09/design-decisions.md`（决策 5 / 9 / 86 / 87，决策 86 记录见 L2352）、`module-11/task-sequence.yaml`（T11-01 ~ T11-19）、`module-09/deploy-package-and-edge-agent-code-organization.md`
> **关联 PRD**: `Module_11_Edge_Access_and_Agent_Delivery.md` v0.4（2026-09-20 已回填，draft）
> **角色**: prototype-designer
> **归属说明**: 原拟落 `module-09/design-proposals/`；因决策 86「M09 1 拆 2」已把边缘接入与 Agent 交付域整体拆出（M09 v2.0 收敛为配置生成与下发中心），**本文档内容全属 M11 职责边界**，经用户 2026-09-18 指示改为落本目录。
> **核对范围说明**: §6 的代码级依据在 **feat 工作树**（`CNCF_Monitor-feature`，分支 `feat/module-09-config-center`）核对，标注行号以该工作树 2026-09-18 状态为准；§6 的文档级依据在 **design 工作树**（本树）核对。

---

## 1. 结论摘要

| # | 议题 | 结论 |
|---|------|------|
| 1 | 中心推送安装 | **不做**。安全性上与市场既有决策 5（否决 Agent 持 SSH 凭据）同源且爆破半径更大；网络上前提出不成立（中心到边缘无入站端口） |
| 2 | 真正的待解问题 | 不是「推 vs 下载」，而是「**装完之后怎么升级**」（见 §3） |
| 3 | Agent 自升级 | **（第五轮拍板 D3 = b）{v0.3} 引入**：拉模式自升级，默认关闭 + 规模门槛触发（节点数 ≥ 10 或年升级 ≥ 3 次），ed25519 签名 + 原子回滚 + 按网域灰度（见 §3.4、§8） |
| 4 | 三区中心选址 | 中心放**政务云互联网区**（唯一与另两区都有通道的枢纽区）；政务外网区 + 腾讯云区各部署一个采集节点（`agent_pull`）；边缘区**不加**二级汇聚代理（见 §4） |
| 5 | 代理服务器的性质 | 网闸穿越**不需要**代理（Agent 已支持 `HTTPS_PROXY`）；中心侧**建议**加反向代理；边缘区中继设备**只在必要时**引入，且必须是应用层反代 + 存储转发（见 §4.4、§5.3） |
| 6 | 方案 A/A′/B 决策 | **（第四轮拍板）确认方案 A = 中心整体留 G2**（控制面 + 数据面 + UI 同址），vmagent 分装 T / G1；**A′（UI 外移腾讯云）降级为可选演进备选**，仅当运维办公网访问动线成为瓶颈时启用（见 §5.6） |
| 7 | Edge Agent 的拓扑无关性 | **成立，且不需要改 Agent 代码**。差异只落在网域表 `center_endpoint` / `remote_write_url` 两个字段填什么（见 §5.4） |
| 8 | 阻断级缺口 | 中心侧 **remote_write 接收端缺失**（`--web.enable-remote-write-receiver` 未开、`platform/` 无 ingest 实现）→ 边缘指标目前无处可写，A/A′/B 全部依赖此项（见 §7-G1） |
| 9 | **（第四轮）UI 部署形态** | 方案 A 下 UI 与中心**同址** G2，走 **A2 形态**（metric-center `--web.static-dir` 托管静态文件 + 相对路径 + 单端口），**不需要 nginx / 跨区反代 / CORS**；nginx 如加则加在 **G2 中心侧入口**做边缘入站治理，与 UI 无关（见 §5.7） |
| 10 | **（第四轮）数据库演进** | **MVP 不引入 PostgreSQL**，维持 SQLite；SQLite→PG 为生产期独立改造（`platform/db/db.go` driver 分派），与 UI 形态完全正交（见 §5.8） |
| 11 | **（第四轮）采集器单一化** | **强制 vmagent，砍掉 prometheus-agent 选型**：网闸断流保障依赖 vmagent 磁盘持久队列（先落盘再发）；Prometheus agent 为「内存优先 + WAL 兜底」，长中断 + 高吞吐会溢出丢数据；单一采集器同时收敛契约概念与交付包（见 §5.9） |
| 12 | **（第四轮）契约字段改名** | `wal_backlog_bytes` → `queue_backlog_bytes`：vmagent 无 Prometheus WAL，实际语义为「磁盘持久发送队列积压」；契约未发布、改名成本最低（见 §5.10） |

### 1.1 四轮讨论的因果链

```
① 怎么装？  ──→  装的形态不是瓶颈，"装完之后怎么升级"才是（人工重装 N 节点不可持续）
                        │
② 三区怎么放？──→  由连接的**方向性**决定：边缘主动出站 ⇒ 中心必须落在"人人可达"的枢纽区
                        │
③ 能不能把中心放腾讯云？──→  真正在意的是**访问面**（运维办公网），不是数据面
                        │
                        ├──→  解耦观察：控制面+数据面留政务云，只把 UI 搬到你方便的地方（A′）
                        │
④ 实际怎么部署？──→  **用户拍板方案 A**：中心整体留 G2（控制面+数据面+UI 同址），
                        │     vmagent 分装 T / G1；A′ 降级为演进备选
                        │
                        └──→  收束：MVP 不引 PostgreSQL、采集器强制 vmagent、契约字段改名
⑤ 剩余 2 项拍板？──→  **用户拍板 D1-D4**：D1/D2 = a（部署形态入 PRD，A′ 留演进备选）；
                        D3 = b（{v0.3} Agent 自升级默认关 + 规模门槛）；
                        D4 = b（中继选型一句话约束，不展开细则）
```

---

## 2. 背景

### 2.1 用户真实拓扑与约束（第二轮、第三轮输入）

| 区域 | 简称 | 网络约束 | 运维可达性 |
|------|------|---------|-----------|
| 政务云·政务外网区 | G1 | ↔ G2 走**双向网闸**，**来回映射地址不同**；↔ T 不可达 | 需申请**云桌面**访问，不便 |
| 政务云·互联网区 | G2 | ↔ G1 经网闸；↔ T 经物理防火墙 + 云防火墙，**开策略可通**；出口有物理防火墙 | 需 **VPN + 堡垒机**进入 |
| 腾讯云·公有云区 | T | ↔ G2 开通策略后可通 | **零信任**直达 |

**运维日常工作位置：普通互联网办公网。**

中心（管理域 `default` / `local` 通道）只能有一个；其余两区为采集节点域（`agent_pull`）。

### 2.2 三条已落版的硬约束（不可绕过）

| # | 约束 | 出处 |
|---|------|------|
| 1 | **中心无入站端口**：心跳 / 拉包 / remote_write 全部由边缘主动出站，中心无需开放任何防火墙入站规则 | M11 §6.1；`module-09/design-decisions.md` 决策 9 |
| 2 | **不提供 `curl\|bash` 一键脚本**（政务网 / 金融专网普遍禁用）；交付形态为离线二进制包 + systemd 一体化包 | M11 §3.3「交付形态」 |
| 3 | **`force_restart` 不做**：中心远程重启边缘进程的指令通道不做，维持 outbound-only 安全边界 | M11 §1.2 边界声明 |

> 约束 1 是选址问题的**决定性**前提：一旦接受「只有出站」，中心的物理位置就必须满足"所有采集节点域都能主动连到它"。

---

## 3. Agent 升级策略（第一轮）

### 3.1 问题重述

原问题是「能否由中心推送安装」。结论是**不能**，理由有二：

- **网络层不成立**：中心→边缘方向本来就无入站通道（约束 1），推送需要额外的可达性工程。
- **安全模型冲突**：既有决策 5 已否决「Agent 持 SSH 凭据为下游节点装 exporter」（5 条风险：凭据暴露面、供应链篡改、敏感配置注入、审计缺口、与人工确认流程冲突，见 `module-09/design-decisions.md`）。**中心推送是同一问题的更严重版本**——爆破半径从单个网域放大为整个平台（中心需持有所有网域的主机凭据）。

### 3.2 真正的瓶颈：安装是低频的，升级是高频的

`platform/edge-sync-agent/packaging/README.md`（feat 工作树）明确写死：二进制升级需**人工替换 + `systemctl restart`**。由此推导：

> 每发布一次边缘包，就要对**每一个**采集节点执行一次「下载 → 摆渡 → 覆盖安装 → 重启」。节点数 N × 升级次数 M 的人工成本线性增长，而 version bump / 安全补丁属于常态需求。

若 go-no-go 条件成熟再评估推送，门槛是：**中心→边缘有可控入站通道 + 客户书面授权主机凭据托管（KMS/Vault 短期凭据）+ 网域数规模化 + 具备安装回执与回滚审计**。即使满足，推荐形态也是**「可编排交付」**（平台出幂等产物与脚本，由客户侧 Ansible / Salt 执行，凭据留在客户侧），而非平台内置 SSH——后者是下策。

### 3.3 建议的四层路径模型

| 层 | 机制 | 方向 | 自动化程度 | 现状 |
|----|------|------|-----------|------|
| 首次安装 | 下载一体化离线包 + 人工三步 | 人从中心拉取 | 人工 | ✅ 已覆盖（§3.3 下载入口） |
| 凭据注入 | 网域 Token（完全脱敏 + 复制） | 一次性 | 人工 | ✅ 已覆盖（§3.1 纳管取凭据） |
| 配置下发 | 心跳拉包 | 边缘 → 中心出站 | 自动 | ✅ 已覆盖（§6.2 / §6.3） |
| **Agent / 组件升级** | **心跳带版本 → 中心返回 latest + URL → 拉签名包 → 校验 → 原子替换 → systemd 重启** | 边缘 → 中心出站 | **建议新增，默认关** | ❌ PRD 现为「不做」 |

自升级**不扩大信任半径**（无入站、无主机凭据），复用既有拉包链路。

### 3.4 ⚠️ 与既有 PRD 决策的冲突声明（必读）

**M11 PRD §3.3 现文明示：`Agent 自升级 | 不做（风险保守原则）`**（另有决策 86 记录 `module-09/design-decisions.md` L2352「Agent 不做自升级」；`Modules/README.md` L74 v1.79 条目同）。

本文**不满足于绕开该决策**，而是正式提出：**以 {v0.3} 拉模式自升级为目标重新审视**。理由与边界：

| 维度 | 主张 |
|------|------|
| 为什么不推翻而是"重新审视" | 「风险保守」在小规模（节点数 ≤ 5、年升级 1~2 次）时**是正确的**；成本问题在规模化后才尖锐。应把它设为带门槛的能力，而非无条件禁用 |
| 触发重评的条件（建议） | 采集节点数 ≥ 10 **或** 年升级次数 ≥ 3 时，默认走自升级；低于该规模维持人工引导升级（§4.4 流程） |
| 护栏（缺一不可） | ① **必须 ed25519 签名**，不能只靠 checksum——checksum 只防传输损坏，防不住供应链篡改，而供应链正是决策 5 否决推送的第 2 条理由；② 默认 `auto_update=false`、升级窗口可配；③ 保留上一版二进制，启动失败原子回滚；④ 按网域灰度；⑤ 升级结果与错误进心跳上报 |

> **（第五轮拍板 D3 = b）**：{v0.3} 引入拉模式自升级，默认 `auto_update=false` + 规模门槛（采集节点数 ≥ 10 或年升级次数 ≥ 3）触发；低于门槛维持人工引导升级（§4.4 流程）。本文 §3.3 第四层成立。需在 `module-11` 新建设计决策记录并在 PRD 更新「Agent 不做自升级」旧口径（见 §8）。

### 3.5 一条实现侧文案缺陷（次要）

生产前端 `ui-custom/web/src/pages/config-center/nodes/EdgeAgentsPage.tsx`（feat 工作树）空态文案为「按**页面顶部**「安装指引」…」，但按 PRD §11.3，安装指引 / 下载区块位于**网域纳管页**顶部，采集节点状态页并无该区域 → 文案指向本页不存在的元素。建议改为「前往网域纳管页查看安装指引」。（属 `dev-feedback` 层，非本文主线）

---

## 4. 三区拓扑选址（第二轮）

### 4.1 为什么中心必须在 G2（政务云互联网区）

`agent_pull` 的全部流量方向为「采集节点 → 中心」（§6.1），因此**中心必须被另外两个采集节点域都能主动访问到**：

| 候选位置 | 结论 | 理由 |
|---------|------|------|
| G1 政务外网区 | ✗ | T 的 Agent 到不了（需穿网闸再出公网，两跳）；且「公有云主动访问政务外网区」在合规上几乎不可能批 |
| T 腾讯云公有云区 | ✗ | G1 出不去；且**政务指标出域**到公有云，数据合规风险最高（这是 §5 中方案 B 的核心代价） |
| **G2 政务云互联网区** | ✅ | G1 经网闸可达、T 经防火墙策略可达；且 G1 + G2 指标**全部留在政务云内**，只有 T 指标**回传进入**政务云（入方向，比出方向好批） |

代价是 G2 暴露面相对较大，用三件事收敛：**来源 IP 白名单**（网闸映射 IP + 腾讯云 NAT 出口 IP）、**每网域独立 Token**（退纳管即废止）、**UI 不向边缘暴露**。

### 4.2 分区配置建议

| 区域 | 网域 | `channel` | `zone_type` | `center_endpoint`（该区视角） |
|------|------|-----------|-------------|------------------------------|
| G2 互联网区 | `default`（管理域） | `local` | internet | 无（同区直连） |
| G1 政务外网区 | 待定 | `agent_pull` | extranet | **网闸映射后的中心地址** |
| T 腾讯云公有云区 | 待定 | `agent_pull` | public | 中心公网地址 |

> 用户提到「来回通道的映射地址不同」——这正是 `center_endpoint` / `remote_write_url` 设计为**网域级**字段、语义为「**该网域视角的可达地址**」的原因（§5.1）。（核对：旧 M09 PRD 曾表述为「网闸映射后地址」；M11 v0.2 §5.1 表述为「转发场景填转发侧可达地址」，语义一致且覆盖更广——**已包含中继转发场景**。）
>
> **两个边缘网域必须填各自视角的地址，不可复用同一个。**

### 4.3 边缘区是否需要二级汇聚代理：不建议

「网闸映射地址稀缺」的问题已被 PRD 的单节点口径解决——**一个网域只需装一个采集节点**（§3.1「单网域只需一台常开机器部署一个采集节点」），因此 G1 全网域只需 **1 条**映射，无需给每台业务机器开映射。

只有该区规模大到单 Agent 扛不住时才需要区内汇聚，而这属于 {v0.4+} 同域多节点演化的范围，MVP / v0.2 不展开。

### 4.4 代理三问

| 问题 | 结论 |
|------|------|
| 网闸穿越需要代理吗 | **默认不需要**。若网闸只放行 HTTP 代理协议，Agent **零改动已支持**：`platform/edge-sync-agent/internal/client/client.go:45` 使用 `&http.Client{Timeout: …}`（Transport 为 nil → `http.DefaultTransport` → `Proxy: http.ProxyFromEnvironment`），配 `HTTPS_PROXY` 环境变量即可走 CONNECT 隧道。**待验证**：vmagent 的 remote_write 是否同样可走代理（VictoriaMetrics 提供 `-remoteWrite.proxyURL`） |
| 中心侧需要反向代理吗 | **建议加**（Nginx / Envoy，不自研）。四个作用：统一地址服务多个边缘网域（省网闸映射资源）、IP 白名单与 TLS 终结、agent 接口与 UI 分流、**承载指标写入接收**（见 §7-G1） |
| 边缘区需要中继吗 | **仅在 G1↔G2 网闸实测不穿透时**引入，且形态有硬性要求，见 §5.3 |

---

## 5. 访问面解耦与方案 A′/B 决策（第三轮）

### 5.1 核心洞察：把三个面分开看

用户提出「中心能否放腾讯云」的真实动因是**运维在互联网办公、进政务云要 VPN+堡垒机不方便**。这是**访问面**问题，不是数据面问题。

| 面 | 内容 | 选址依据 | 能否搬走 |
|---|------|---------|---------|
| **数据面** | TSDB 落地、指标查询回源 | 枢纽位置 + **数据合规** | 不宜搬 |
| **控制面** | 心跳 / 纳管 / 配置下发 | 必须全局唯一，且须被所有采集节点域可达 | 不宜搬 |
| **访问面** | UI 控制台、告警通知出口 | **跟着运维办公网走** | ✅ **可以搬** |

按 §1.2 边界，既有 PRD 的 §11 前端契约隐含「UI 与中心同址」假设；这里建议把它显式拆出来。

### 5.2 三个方案对比

| | 方案 A：中心在 G2 | 方案 B：中心在 T + G2 跳板机 | **方案 A′：中心在 G2 + UI 外移 T** |
|---|---|---|---|
| G1 部署 | 采集节点（出站至 G2） | 采集节点（出站至 G2 跳板） | 采集节点（出站至 G2） |
| G2 部署 | **监控中心**（控制面+数据面+UI） | **relay 缓冲转发**（运维成本≈半个中心） | **监控中心**（仅开放受限接口） |
| T 部署 | 采集节点 | **监控中心** | 采集节点 + **UI 控制台（静态资源）** |
| 运维入口 | VPN + 堡垒机（不便） | 零信任直达 | **零信任直达** |
| 数据流向 | 全部留政务云 | **政务数据落到公有云** | **全部留政务云** |

### 5.3 为什么不建议把跳板机做成"转发管道"

用户的设想（G2 放一台机器让 G1 remote write 过去）技术上可行，但**不能是 L4 / 纯端口转发**：

| # | 理由 |
|---|------|
| 1 | `remote_write` 是持续 HTTPS 流 + snappy/protobuf，网闸与公网会切断空闲长连接 → **直接丢数据**；这正是 PRD §3.1「网闸按标准 HTTPS 可穿透设计、**待客户环境实测**」所列的待验证项 |
| 2 | 因 G1↔T 不可达，G1 的**心跳 / 拉配置也必须过这台机器**（不只是数据面）→ 它必须是**应用层反向代理**，不是 `iptables` / `stream` 转发 |
| 3 | L4 转发做不了网域身份透传，远端中心认不出是哪个网域的数据 → `network_domain` 标签丢失或必须每网域一条独立通道 |
| 4 | 一台机器成为**整个 G1 的单一故障域** |

若必须引入，正确形态应是**「互联网区监控专用缓冲 / 转发网关」**：

- **管理面**（心跳 / 拉包 / 升级包下载）：Nginx / Envoy 应用层反代；
- **数据面**（remote_write）：带持久化队列的存储转发组件（如 vmagent + `-remoteWrite.tmpDataPath`，断网自动续传），**不要**用纯 Prometheus receiver 裸跑，也不要用 TCP 转发；
- relay 尽量**无状态**（除缓冲队列），可快速重建。

### 5.4 Edge Agent 对拓扑无关：A / A′ / B 都满足，且无需改代码

| # | 依据 | 位置 |
|---|------|------|
| 1 | `center_endpoint` / `remote_write_url` 为**网域级**字段，语义「转发场景填转发侧可达地址」 | M11 §5.1；代码 `platform/models/network_domain.go:78/79` |
| 2 | 明确的方向约束「**全部为上行（采集节点 → 中心）**，中心不主动入站」 | M11 §6.1 |
| 3 | HTTP client 走 `http.DefaultTransport`（`Proxy: http.ProxyFromEnvironment`）→ 支持 `HTTPS_PROXY` CONNECT 隧道 | `platform/edge-sync-agent/internal/client/client.go:45` |
| 4 | 每网域独立 Token 鉴权，中继两侧可用不同凭据 | M11 §3.5 |

> **结论**：Agent 对"目标是中心还是 relay"**完全无感**。中心选址与中继与否，差异只落在**网域表两个地址字段填什么**——这是产品可交付能力，不是代码变更。

补一条有利证据：`contract.HeartbeatRequest` 已带 `wal_backlog_bytes` / `remote_write_queue_size`（对齐 §5.3 EdgeHeartbeat 模型），多一跳后若堆积积压**是可观测的**。

### 5.5 方案 A′（UI 外移）的落地要点与代价——已降级为演进备选

> **（第四轮更新）** 用户 2026-09-20 拍板采用**方案 A**（中心整体留 G2），A′ 不再作为推荐方案，降级为**可选演进备选**：仅当「运维办公网 → G2」的 VPN + 堡垒机访问动线成为日常瓶颈时启用。本节保留落地要点作为启用时的执行依据。

A′ 落地只需三件事：

1. UI 静态资源部署在腾讯云（零信任网内），构建期注入 `VITE_API_BASE_URL` 回指 G2 的 `/api/v2/platform` —— **前端已原生支持**：`ui-custom/web/src/api/client.ts:6` `const BASE_URL = import.meta.env.VITE_API_BASE_URL || ''`；
2. G2 侧反向代理公开后端 API，限定来源 IP = 腾讯云 UI 出口 IP；
3. UI 与 agent 接口分流，UI 不向边缘映射地址暴露。

**为什么合规上优于方案 B**：认证链路方向是「腾讯云 UI → 政务云互联网区 API」，属**入方向**访问。**指标一条都没有离开政务云**；而 G2 本就是对外服务区，T 主动访问它与"互联网用户访问该区"差别不大，只需 1 条防火墙策略。

**代价（须如实告知）**：

- UI → API 跨区，**PromQL 查询代理（M02）/ 告警代理（M08）的响应数据要走这条链路**，大时间窗查询延迟可能上升（数十至数百毫秒），需实测；
- 腾讯云侧多一台静态资源机（轻量、无状态、可多副本）；
- **零号备选（建议优先尝试）**：相当多堡垒机 / 零信任产品自带 Web 应用反向发布能力，可直接把 G2 的 UI 发布到运维办公网，连腾讯云这台机器都省了。

### 5.6 何时才真的该用方案 B

需**同时**满足：

1. 监管书面允许政务数据出入公有云；
2. 腾讯云侧已有统一可观测平台要做收敛；
3. 政务云侧确实拿不到资源。

仅为了「运维少拨一次 VPN」而把控制面与数据面搬去公有云，是**拿数据合规换操作便利**——这笔账不划算。把 UI 搬走就够了。

### 5.7 （第四轮）方案 A 基线下的 UI 部署形态：A2 同址，不引入 nginx

用户实际采用**方案 A**：中心（控制面 + 数据面 + UI）整体部署在政务云互联网区 G2，vmagent 分别部署在腾讯云 T 与政务外网区 G1。此形态下：

- **UI 与中心同址 G2**，走项目已落地的 **A2 形态**（`frontend-backend-deploy-topology.md` §4 决策）：metric-center 以 `--web.static-dir` 托管前端静态文件、前端**相对路径**构建、单端口 8080 对外——**自包含、无跨域、无新组件**，当前 MVP 已验证。
- **前后端"分离"的边界**：产物级分离（前端 `pnpm build` 独立静态 `dist/`）始终成立；部署级分离（跨区 / 分机）在方案 A 下**不需要**。`VITE_API_BASE_URL` 注入能力保留，作为 A′ / 同区 S2 分机的零改动切换点。
- **nginx 的定位**：方案 A 下 UI 侧**不加 nginx**。如加，加在 **G2 中心侧入口**做**边缘入站治理**——统一地址服务多网域（省网闸映射资源）、TLS 终结、`/api/v2/platform/edge/*` 与 UI 接口分流、承载 remote_write 写入接收（§4.4 四个作用）——与 UI 无关。
- **安全重点**落在 G2 入站链路上：来源 IP 白名单（网闸映射 IP + 腾讯云 NAT 出口 IP）+ 每网域独立 Token + agent 接口与 UI 接口分流。

### 5.8 （第四轮）数据库演进：MVP 不引入 PostgreSQL，与 UI 形态正交

- **MVP 维持 SQLite**（`metric_center.db`），不引入 PostgreSQL。
- SQLite → PostgreSQL 为**生产期独立改造**：`platform/db/db.go` 现硬编码 `gorm.io/driver/sqlite`，`METRIC_CENTER_DB_DSN` 仅覆盖连接串；需引入 `gorm.io/driver/postgres` 并按 DSN 前缀（`postgres://` / `pg://`）分派 driver，约 10 行改动；`AutoMigrate` 为 GORM 跨库通用，模型层零改动。
- 该改造与 UI 部署形态（A2 / S2 / A′）**完全正交**——数据库在 G2 后端内部，不影响任何前端 / nginx / 拓扑决策。

### 5.9 （第四轮）采集器强制 vmagent 单一化

**决策：砍掉 prometheus-agent 选型，采集器仅 vmagent。** 依据：

| 维度 | vmagent 磁盘持久队列 | Prometheus agent WAL |
|------|---------------------|----------------------|
| 模型 | scrape → **先落盘队列** → 发送 | 内存 buffer → 尝试发送，**失败才写 WAL 兜底** |
| 长中断表现 | 数据全在磁盘，容量可配（`-remoteWrite.maxDiskUsagePerURL`），重启不丢 | 内存 buffer（默认 2500 样本/队列）先满，**溢出即丢** |
| 可观测性 | `vm_persistentqueue_*` 丰富，积压字节 / 队列深度直接可查 | WAL 状态观测弱，无积压字节类指标 |
| 容量上限 | 磁盘配额，显式可配 | 受内存约束，隐性上限 |

- 网闸恰为「可能长时间不可用」场景，vmagent「先落盘再发」是唯一能保证长中断不丢数据的模型。**Prometheus agent 不是做不到，而是长中断 + 高吞吐下保障强度不足且不可观测**——这是写决策记录时的准确表述。
- 收敛收益：网闸可靠性是 PRD 级承诺，不能被 agent 选型绕过；`AgentType` 枚举单一化消除契约概念混乱（§5.10）；交付包只产一套 vmagent 配置模板。
- **边界**：中心接收端 G1 与选型无关——vmagent 也是 remote_write 协议，G1（`--web.enable-remote-write-receiver` + platform ingest）仍然必做、优先级最高。

### 5.10 （第四轮）契约字段改名：`wal_backlog_bytes` → `queue_backlog_bytes`

- vmagent 缓冲模型为 scrape →（磁盘持久队列）→ remote_write，**无 Prometheus 式 WAL**；`wal_backlog_bytes` 实际捕获的是磁盘持久队列积压（`vm_persistentqueue_bytes_pending`）。
- 契约尚未对外发布，**现在改名成本最低**；同步在字段注释写明「采集器统一为 vmagent，语义为持久发送队列积压字节」。
- 波及面：`contract.HeartbeatRequest`、`EdgeAgent` / `EdgeHeartbeat` 模型、前端节点状态页「WAL 积压」展示文案（改「回传积压」）、PRD §5.2 / §5.3 / §10 术语映射。

---

## 6. 依据核对表

### 6.1 代码级依据（feat 工作树 `CNCF_Monitor-feature`，2026-09-18 核对）

| 依据 | 位置 |
|------|------|
| `channel` / `center_endpoint` / `remote_write_url` / `token` 为网域级字段 | `platform/models/network_domain.go:74/78/79` |
| HTTP client 走 `http.DefaultTransport`（支持 `HTTPS_PROXY`） | `platform/edge-sync-agent/internal/client/client.go:45` |
| 心跳契约已含 `wal_backlog_bytes` / `remote_write_queue_size` / `components` | `platform/edge-sync-agent/internal/contract/contract.go`（`HeartbeatRequest`） |
| `HeartbeatResponse` 仅 `config_changed` / `config_version` / `config_download_url`，**无版本升级字段** | 同上（对应 §8-D3 若采纳需扩展） |
| 升级需人工替换 + `systemctl restart` | `platform/edge-sync-agent/packaging/README.md` |
| 前端已支持构建期注入 API 基址 | `ui-custom/web/src/api/client.ts:6` |
| **中心 Prometheus 未开启 remote_write 接收** | `Makefile` `run-prometheus` 仅 `--web.enable-lifecycle`；`platform/` 下无 ingest 实现 |

### 6.2 文档级依据（design 工作树，本树）

| 依据 | 位置 |
|------|------|
| 交付与升级（下载入口 / 版本清单 / 升级维护 / **Agent 自升级不做** / 交付形态） | `Module_11_Edge_Access_and_Agent_Delivery.md` §3.3 |
| 地址语义与网闸约束（**转发场景填转发侧可达地址**） | 同上 §3.1 |
| 地址字段定义 | 同上 §5.1 |
| `{v0.3}` 升级流程（覆盖安装保留 Token/配置 → 重启） | 同上 §4.4 |
| `EdgePackageRelease` 模型 {v0.3} 占位 | 同上 §5.4 |
| `force_restart` 不做、维持 outbound-only | 同上 §1.2 |
| 接口可达性与地址约束 | 同上 §6.1 |
| 决策 86（M09 1 拆 2，含「Agent 不做自升级」） | `module-09/design-decisions.md` L2352 |
| v1.79 变更条目 | `Modules/README.md` L74 |
| 既有 M11 任务编号止于 T11-19 | `module-11/task-sequence.yaml` |

---

## 7. 缺口清单

| ID | 缺口 | 影响 | 建议版本 |
|----|------|------|---------|
| **G1** | **中心侧 remote_write 接收端缺失** — Makefile 未开 `--web.enable-remote-write-receiver`，`platform/` 无 ingest 实现 | **阻断级**：边缘 vmagent 指标无处可写；A / A′ / B 全部依赖。§5.1「`remote_write_url` 通常 = 中心接入地址 + `/api/v1/write` 可自动推导」这一前提也不成立 | **{v0.2} 必修** |
| **G2** | 中继（relay）**形态无选型约束** — PRD 仅写「转发场景填转发侧可达地址」，易被实施为 L4 / Nginx 纯转发而踩坑 | 数据丢失、网域身份丢失（§5.3） | {v0.2} 补 §6.1.1 |
| **G3** | 中继自身**不可观测** — `EdgeAgent` 只覆盖边缘节点，relay 不在模型内，挂了中心看不到，而其下压着整个区 | 故障域盲区 | {v0.2} 评估轻量心跳或纳入既有主机监控 |
| **G4** | **部署形态未入 PRD**（第四轮已拍板方案 A） | 方案 A 已确认，但 §11 仍隐含「UI 与中心同址」未成文；A′ 作为演进备选无据可依 | {v0.2} 补部署形态小节（见 §9.1 #6） |
| **G5** | Agent 自升级现为「不做」 | 规模化后人工升级成本线性增长（§3.2/§3.4） | 待拍板，建议 {v0.3} |
| **G6** | 前端文案指向不存在元素（§3.5） | 轻微误导 | 随 {v0.2} 前端批次修 |
| **G7** | **采集器选型未收敛**（第四轮已确认强制 vmagent） | PRD v0.2 保留 `prometheus-agent` 枚举；网闸断流保障依赖 vmagent 持久队列，选型开放会绕过保障承诺并造成概念混乱（§5.9） | {v0.2} 回填 PRD（§3.1 / §5.1 / §5.2 / §6.4 / §10） |
| **G8** | **契约字段命名与 vmagent 模型不符**（第四轮已确认改名） | `wal_backlog_bytes` 语义实际为「磁盘持久发送队列积压」，沿用 WAL 术语误导实现者（§5.10） | {v0.2} 改名 `queue_backlog_bytes` |

---

## 8. 已确认决策与待拍板清单

### 8.0 （第四轮）已确认决策

| ID | 决策项 | 结论 | 回填目标 |
|----|--------|------|----------|
| **C1** | 部署方案 | **方案 A**：中心（控制面+数据面+UI）整体部署 G2，vmagent 分装 T / G1；A′（UI 外移）降级为演进备选 | PRD §1.2 部署形态 |
| **C2** | UI 部署形态 | A2 同址（metric-center 托管静态 + 相对路径 + 单端口），不加 nginx；nginx 仅用于 G2 中心侧边缘入站治理 | PRD §1.2 |
| **C3** | 数据库演进 | MVP 不引入 PostgreSQL，维持 SQLite；SQLite→PG 为生产期独立改造（db.go driver 分派），与 UI 形态正交 | PRD §7.2 |
| **C4** | 采集器单一化 | 强制 vmagent，砍掉 prometheus-agent 选型（§5.9） | PRD §3.1 / §5.1 / §5.2 / §6.4 / §10 |
| **C5** | 契约字段改名 | `wal_backlog_bytes` → `queue_backlog_bytes`（§5.10） | 契约 / 模型 / PRD §5.2 / §5.3 / §10 |

### 8.1 （第五轮）已拍板决策

| ID | 决策项 | 拍板结论 | 回填目标 |
|----|--------|----------|----------|
| **D1** | 方案 A 部署形态 + A′ 演进备选是否入 PRD | **a. 入 PRD** —— 部署形态小节落 §1.2 边界声明 + §7.2 技术依赖 | PRD §1.2 / §7.2 |
| **D2** | 是否承认「UI 控制台外移」为演进备选形态（方案 A′） | **a. 承认** —— PRD §1.2 补一行演进备选说明，留作运维动线瓶颈时的切换点 | PRD §1.2 |
| **D3** | Agent 自升级是否从「不做」改为带门槛选项 | **b. {v0.3} 引入，默认关**（`auto_update=false`）+ ed25519 签名 + 原子回滚 + 按网域灰度；规模门槛：采集节点数 ≥ 10 或年升级次数 ≥ 3 触发，低于门槛维持人工引导 | PRD §3.3 / §4.4 |
| **D4** | 中继（relay）是否在 PRD 中给出选型约束 | **b. 只写"必须应用层反代 + 存储转发、禁用 L4/TCP 转发"一句**——方案 A 下中继备而不用，不展开细则、不单独排任务（踩坑代价是静默丢数据，故保留一句话硬约束） | PRD §6.1 |

---

## 9. 回填清单

> 按 PRD 冻结门禁，以下动作须**在当前 feat 收尾后**由产品负责人回 `design` 分支执行版本化迭代，不在开发轮做。

### 9.1 `Module_11_Edge_Access_and_Agent_Delivery.md`（v0.2 → v0.4）

> **执行状态（2026-09-20）**：C1-C5 已于 v0.3 回填；D1-D4 已于 v0.4 回填（#2 / #7 / #9 完成；#5 按 D4 = b 收口为 §6.1 一句话约束）。

| # | 目标章节 | 动作 | 依赖决策 | 状态 |
|---|---------|------|---------|------|
| 1 | §3.1 网域纳管 | 在「地址语义与网闸约束」行后补：**网域视角地址取值规则（含中继转发侧）** | D1 | 随 v0.3 完成 |
| 2 | §3.3 离线包交付与升级 | 把「Agent 自升级 \| 不做」改为带门槛表述，或在其下加备注引用本文 §3.4 | D3 | **v0.4 完成**（改为 {v0.3} 拉模式自升级，默认关 + 规模门槛） |
| 3 | §3.3 | 补「离线包版本清单展示」与前端下载区块的实现状态标注 | — | 待办 |
| 4 | §5.1 数据模型 | `center_endpoint` / `remote_write_url` 说明补「中继场景填写转发网关地址，见 §6.1.1」 | D1/D4 | 随 v0.3 完成 |
| 5 | §6.1 接口可达性与地址约束 | **新增 §6.1.1 中继网关选型约束**：管理面必须应用层反代、数据面必须存储转发、禁用 L4/TCP 转发、per-domain Token 透传口径 | D4 | **v0.4 完成**（按 D4 = b 收口为 §6.1 一句话约束，不新增子节） |
| 6 | §1.2 边界声明 / §7.2 技术依赖 | **（第四轮 C1/C2/C3）新增部署形态小节**：方案 A 基线（中心 + UI 同址 G2，A2 形态：metric-center 托管静态 + 相对路径 + 单端口，不加 nginx）；nginx 仅用于 G2 中心侧边缘入站治理；A′ 为演进备选（UI 外移腾讯云，VITE_API_BASE_URL 注入回源 G2）；MVP 数据库维持 SQLite | C1/C2/C3 | 随 v0.3 完成 |
| 7 | §4.4 升级流程（{v0.3}） | 若 D3 = b，改写为自动化链路：心跳版本 → latest + URL → 验签 → 原子替换 → 回滚 | D3 | **v0.4 完成**（补拉模式自升级链路，保留人工引导为默认） |
| 8 | §9 验收标准 | 增补：多网域不同 `center_endpoint` 的心跳/拉包连通性；remote_write 写入接收可用性；中继断网续传与恢复窗口 | D1/D4 | 待办 |
| 9 | **（第四轮 C4）§3.1 / §5.1 / §5.2 / §6.4 / §10** | **采集器单一化为 vmagent**：`agent_type` 枚举收敛、删除 prometheus-agent 选型表述、§6.4.1 参数表对齐 vmagent 磁盘持久队列（落盘路径 / 磁盘配额 / 队列满行为） | C4 | 随 v0.3 完成 |
| 10 | **（第四轮 C5）§5.2 / §5.3 / §10 / §11.4** | **契约字段改名**：`wal_backlog_bytes` → `queue_backlog_bytes`，UI 展示名「WAL 积压」→「回传积压」，术语映射同步 | C5 | 随 v0.3 完成 |
| 11 | 头部 Change Log | 记录 v0.3 / v0.4 变更摘要；旧版历史按"主文档仅保留最近 3 版"约定迁出（v0.1 已迁出） | — | v0.4 完成 |

### 9.2 `module-09/design-decisions.md`

| # | 动作 | 依赖决策 |
|---|------|---------|
| 1 | **（第四轮 C1/C2/C3）新增决策条目**：「方案 A 部署形态与数据库演进」——中心 + UI 同址 G2（A2 形态）、nginx 定位（仅 G2 中心侧边缘入站治理）、A′ 为演进备选、MVP 维持 SQLite / 生产期按需切 PG（按既有格式：背景 / 选项 / 推荐 / 影响表） | C1/C2/C3 |
| 2 | **（第四轮 C4）新增决策条目**：「采集器强制 vmagent 单一化」——显式引用 PRD v0.2 `prometheus-agent` 枚举口径，说明推翻理由（网闸断流保障依赖 vmagent 持久队列）与影响面 | C4 |
| 3 | **（第四轮 C5）新增决策条目**：「契约字段改名 wal_backlog_bytes → queue_backlog_bytes」——语义修正与波及面（§5.10） | C5 |
| 4 | 新增决策条目：「中继网关形态约束」 | D4 |
| 5 | 若变更 Agent 自升级结论：新增决策条目并**显式引用决策 86 与 §3.3 原文**，说明推翻理由与规模门槛 | D3 |
| 6 | 更新决策 86 引用关系（避免两处口径打架） | D3 |

### 9.3 `module-11/task-sequence.yaml`

| # | 动作 | 建议编号 | 依赖决策 |
|---|------|---------|---------|
| 1 | 新增任务：中心 Prometheus 开启 remote_write 接收 + 平台侧 ingest 通路验证 | T11-20（建议） | 无（G1 阻断级，建议无条件先做） |
| 2 | 新增任务：多网域不同 `center_endpoint` 的集成用例 | T11-21 | D1 |
| 3 | 新增任务：中继场景联调（含断网续传与恢复窗口） | T11-22 | D4 |
| 4 | 新增任务：UI 控制台外移形态的构建与部署验证（`VITE_API_BASE_URL` 注入） | T11-23 | D2 |
| 5 | 新增任务：Agent 自升级（验签 + 原子替换 + 回滚 + 灰度） | T11-24（{v0.3}） | D3 |

### 9.4 `module-11/dev-feedback.md`（待新建）

| # | 条目 |
|---|------|
| F-1 | 采集节点状态页空态文案指向本页不存在的「页面顶部安装指引」，应改为「前往网域纳管页查看安装指引」（对应 §3.5 / G6） |
| F-2 | 离线包版本清单（组件版本 / sha256 / 包大小）前端未上屏，PRD §3.3 已要求 P0 |
| F-3 | `remote_write_url` 的自动推导在中心无 ingest 时会推导出不可用地址，需在写入校验或提示中暴露（对应 G1） |

### 9.5 `module-09/api-contract-snapshot.md` 与原型

| # | 动作 | 依赖决策 |
|---|------|---------|
| 1 | `contract.HeartbeatResponse` 若支持自升级：增 `latest_agent_version` / `agent_download_url`（`HeartbeatRequest.Version` 已可用） | D3 |
| 2 | **（第四轮 C5）`contract.HeartbeatRequest` 字段改名**：`wal_backlog_bytes` → `queue_backlog_bytes`，注释同步「采集器统一为 vmagent，语义为磁盘持久发送队列积压」 | C5 |
| 3 | 原型（`docs/prototypes/module-09/`）网域纳管页安装指引区：补版本清单 / sha256 / 包大小展示位 | 无 |
| 4 | 原型采集节点状态页：修正空态深链文案；「WAL 积压」列展示名改「回传积压」 | 无 / C5 |
| 5 | 原型：若 D2 通过，补「UI 外移（A′）演进备选」说明性图示或文档链接（非交互改动） | D2 |

---

## 10. 术语

| 术语 | 本文含义 |
|------|---------|
| G1 / G2 / T | 政务外网区 / 政务云互联网区 / 腾讯云公有云区 |
| 三个面 | 数据面（指标落地与查询回源）/ 控制面（心跳、纳管、下发）/ 访问面（UI 控制台、告警出口） |
| relay / 中继网关 | 部署在互联网区的监控专用缓冲 / 转发组件，非业务跳板机 |
| 自升级 | 边缘 Agent 经既有出站链路拉取签名升级包并执行替换，非中心推送 |
