# 设计决策记录：module-03

## 会议/对齐信息

- 日期：2026-08-02
- 参与 Agent：prototype-designer、orchestrator
- 触发原因：基于现有 PRD 生成可点击原型并验证设计理解

## 关键决策

### 决策 1：原型风格与呈现方式

- 问题：原型采用何种视觉风格以便领导/业务方快速理解产品形态？
- 结论：采用火山引擎 Volcengine 设计 Token（主色 #0ECDEB、头部 #0B1B2A）作为原型风格，保持企业级云产品观感。
- 依据：用户需求 / 火山引擎品牌色参考
- 影响范围：docs/prototypes/module-03/ 全部页面

### 决策 2：模块原型独立拆分

- 问题：全模块统一原型还是按模块独立原型？
- 结论：按 prototype-designer 规范，每个模块产出独立的 Vite + React 原型项目，便于后续按模块评审、冻结与开发。
- 依据：`.kimi/agents/prototype-designer.md` 目录规则
- 影响范围：docs/prototypes/module-01/ ~ module-10/

### 决策 3：当前 PRD 范围确认

- 问题：当前 PRD 是否足以支撑原型验证？
- 结论：PRD v1.0 已覆盖本模块核心数据模型、页面与 MVP 边界，原型按 PRD 实现，未发现 [待验证] 技术缺口。
- 依据：docs/02-product-requirements/Modules/Module_03_Gateway_and_Auth.md
- 影响范围：原型页面范围

## 待确认项

- [ ] 领导评审后对页面信息架构的反馈
- [ ] 是否需要针对 MVP 范围进一步裁剪页面字段

## 关联文档

- `docs/02-product-requirements/Modules/Module_03_Gateway_and_Auth.md`
- `docs/prototypes/module-03/`

## Change Log（完整历史）

> v1.3 起主 PRD Change Log 精简为最近 3 版一句话摘要；本小节承载 v1.2 及以前的逐版完整变更详情。

| 版本 | 日期 | 变更类型 | 变更内容 | 影响范围 | 产品版本影响 | 状态 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| v1.1 | 2026-08-02 | 新增 | 完成 Volcengine 风格原型验证，输出独立可点击原型 | PRD 状态、UI/UX、原型目录 | 文档自身 | 设计中 |
| v1.0 | 2026-07-31 | 初始 | 模块 PRD 初始版本 | 全部 | v0.2 / v1.0 | draft |

## PRD v1.4 去历史化归档（2026-09-16）


> 正文 6 处内联决策标注（决策 44 ×4 / 决策 8 / 决策 23）迁本档；Change Log v1.3 / v1.2 行压缩、v1.1 行轮转本档。规格语义零变更。

**§1 L20 轻量认证（决策 44，修订决策 8）**：

```
MVP 阶段落地**轻量认证**（决策 44，修订决策 8）：用户名 + 密码登录、会话 Token、认证中间件拒绝匿名请求；**不做授权与多租户隔离**（无角色 / 权限点 / 租户数据隔离，所有登录用户等价），属已知风险，依赖部署网络隔离兜底。v1.0 阶段由本模块提供完整网关层鉴权与审计能力；用户/角色/租户/权限策略的生命周期管理由 [Module_06](Module_06_Multi_Tenant.md) 负责。
```

**§4.0 标题（决策 44 / Track B+）**：

```
## 4.0 轻量认证设计（MVP，决策 44 / Track B+）
```

**§4.0 L68 种子机制（决策 23）**：

```
- 初始管理员 `admin` 由后端启动 migration upsert 预置（幂等，与决策 23 种子机制一致），初始密码为部署配置项（默认 `admin123`），首次登录引导改密；
```

**§5 L89 依赖（决策 44）**：

```
- `platform/gateway/auth/`（MVP 轻量认证中间件，决策 44）
```

**§6 L99 验收（决策 44）**：

```
- [ ] {P0} MVP 落地轻量认证（决策 44）：`POST /api/v2/platform/auth/login` 成功返回 Token，失败统一 401 不区分原因；`logout` / `me` / `auth/password` 可用
```

**§4 L73 引用块规范化**：

```
> MVP 不启用本权限模型（决策 44：所有登录用户等价，无授权隔离）。
```

**原型版本行（原文）**：

```
> **原型版本**: v1.1（Track B+ 增量免高保真原型，决策 44）
```

**Change Log v1.1 行（轮转归档）**：

```
| v1.1 | 2026-08-03 | 修改 | PRD 状态从 ready 修正为 设计中：尚未完成原型验证 | PRD 状态 | 文档自身 | 设计中 |
```

**Change Log v1.3 行（压缩前）**：

```
| v1.3 | 2026-09-02 | 修改 | v0.2 范围收敛（2026-09-02 与产品负责人确认 v0.2 版本范围最终口径）：统一入口路由 / Ingestion 路由由 v0.2 后移 v0.3——Edge Sync Agent 直连 metric-center 拉取配置，v0.2 不依赖网关；Ingestion 路由随 Module_10 一并后移；MVP 轻量认证范围（决策 44，用户名+密码登录 / 会话 Token / 认证中间件）不变 | 头部产品版本覆盖、3 核心功能 | v0.3 | 待确认 |
```

**Change Log v1.2 行（压缩前）**：

```
| v1.2 | 2026-08-28 | 新增 | 决策 44 落版（Track B+ 轻量规格，免高保真原型）：MVP 落地轻量认证——①§1「MVP 不实现认证」修订为「轻量认证、无授权隔离」；②§3 拆分「轻量认证（P0/MVP）」与「授权 RBAC（P2/v1.0）」；③新增 §4.0 轻量认证设计（login/logout/me/改密接口、Token 会话机制、认证中间件、初始 admin 种子、前端登录页与路由守卫）；④§6 验收替换「MVP 允许无认证访问」为认证验收条目 | 1 模块目标、3 核心功能、4 权限模型、5 依赖、6 验收标准 | MVP | 待确认（dev-ready） |
```

### PRD v1.4 章节结构对齐说明（2026-09-16，同日同轮）

全量门禁「章节结构」检查揭示旧版为压缩式章节（4=权限模型 / 5=依赖 / 6=验收，缺 7–11）。v1.4 对齐 11 章模板，**原文块全部平移保留**：

| 旧位置 | 新位置 | 处置 |
|---|---|---|
| §4.0 轻量认证设计（机制约定） | §4.1 认证流程 | 平移 + 新增登录/中间件 flowchart（节点均取自原文语义） |
| §4.0 接口契约表 | §6 接口设计 | 平移 |
| §4 权限模型 | §4.2 权限模型（v1.0 阶段预留） | 平移 |
| （新增） | §5 数据模型 | `sessions` 表 3 字段（token / user_id / expires_at）**派生自 §4.1 机制约定原文**，非新增规格 |
| （新增） | §8 数据模型状态机 | 说明性条文：会话为短生命周期凭证、无中间状态流转（依 §4.1 原文），不虚构状态机 |
| §5 依赖 | §7 依赖 | 平移 |
| §6 验收标准 | §9 验收标准 | 平移 |
| （新增） | §10 术语映射 | 4 词条，均取自正文既有概念 |
| §4.0 前端条目 | §11 前端交互契约 | 平移展开为 4 条 bullet |

外部锚点核查：仅 Module_00 引用 `#3-核心功能`，§3 标题未动、锚点保留。
