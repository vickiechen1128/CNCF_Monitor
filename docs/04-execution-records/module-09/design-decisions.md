# 设计决策记录：module-09

## 会议/对齐信息

- 日期：2026-08-02
- 参与 Agent：prototype-designer、orchestrator
- 触发原因：基于现有 PRD 生成可点击原型并验证设计理解

## 关键决策

### 决策 1：原型风格与呈现方式

- 问题：原型采用何种视觉风格以便领导/业务方快速理解产品形态？
- 结论：采用火山引擎 Volcengine 设计 Token（主色 #0ECDEB、头部 #0B1B2A）作为原型风格，保持企业级云产品观感。
- 依据：用户需求 / 火山引擎品牌色参考
- 影响范围：docs/prototypes/module-09/ 全部页面

### 决策 2：模块原型独立拆分

- 问题：全模块统一原型还是按模块独立原型？
- 结论：按 prototype-designer 规范，每个模块产出独立的 Vite + React 原型项目，便于后续按模块评审、冻结与开发。
- 依据：`.kimi/agents/prototype-designer.md` 目录规则
- 影响范围：docs/prototypes/module-01/ ~ module-10/

### 决策 3：当前 PRD 范围确认

- 问题：当前 PRD 是否足以支撑原型验证？
- 结论：PRD v1.0 已覆盖本模块核心数据模型、页面与 MVP 边界，原型按 PRD 实现，未发现 [待验证] 技术缺口。
- 依据：docs/02-product-requirements/Modules/Module_09_Network_Domain_and_Edge_Config_Center.md
- 影响范围：原型页面范围

## 待确认项

- [ ] 领导评审后对页面信息架构的反馈
- [ ] 是否需要针对 MVP 范围进一步裁剪页面字段

## 关联文档

- `docs/02-product-requirements/Modules/Module_09_Network_Domain_and_Edge_Config_Center.md`
- `docs/prototypes/module-09/`

---

## 决策：网域模式与配置下发中心行为（2026-08-03）

### 背景

用户提出新需求：

- **单一网域用户**：在配置下发中心不应按网域划分管理，也不需要查看监控代理节点状态。
- **多网域用户**：需先配置不同网域，再接入各网域下的监控代理节点；完成后才能在资源管理、监控策略进行配置，并将配置下发到对应监控代理节点。

### 决策记录

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 1 | 网域生命周期与 Agent 管理归属 **Module_09** | `NetworkDomain` 数据模型唯一 Owner 为 Module_09；配置生成/预览/下发/Agent 接入均由本模块负责 | Module_09 PRD、数据模型、API |
| 2 | 单网域/多网域通过 `feature_flags.multi_site_enabled` 切换 | [02_Product_Roadmap.md](../../02-product-requirements/02_Product_Roadmap.md) 已定义该开关；避免对单机用户造成认知负担 | 前端菜单、后端权限、UI 渲染 |
| 3 | 单网域模式隐藏「网域管理」与「Agent 状态」入口 | 单网域场景无物理隔离 Edge Agent，网域与 Agent 状态页对用户无意义 | Module_09 前端页面、门户菜单 |
| 4 | 数据模型始终保留 `default` 网域 | 保证单网域与多网域切换时资源归属不中断，所有资源必须有 `network_domain_id` | `NetworkDomain` 模型、`Resource.network_domain_id` |
| 5 | 从单网域切换到多网域时，现有资源自动归属 `default` | `default` 作为初始网域，用户可后续迁移或保留作为中心直接采集域 | 迁移逻辑、后端初始化 |
| 6 | 配置下发中心在单网域模式下只面向中心 Prometheus | 不展示网域选择器、不展示多网域分发；确认后仅触发中心 `/-/reload` | 配置下发 UI、`ConfigDeployment.target_type` |
| 7 | 多网域模式下，配置下发中心按网域展示草稿/diff/下发记录 | 每个网域独立 `ConfigDraft` / `ConfigVersion` / `ConfigDeployment` | 配置下发 UI、配置包拉取 |
| 8 | 单网域模式下 Edge Sync Agent 协议接口不面向用户暴露 | **后端保留协议能力，仅 UI 隐藏**；便于单网域向多网域平滑扩展 | Edge Sync Agent API、鉴权、UI |

### 待确认项（已确认）

| 问题 | 决策 | 落地位置 |
|------|------|----------|
| `multi_site_enabled` 是平台级还是租户级开关？ | **租户级开关**，体现在 `Tenant.multi_site_enabled`；与“1 租户 : N 网域”上层设计关联更合理 | Module_06 `Tenant` 模型、Module_09 3.11 节 |
| 单网域模式下 Edge Sync Agent 协议如何处理？ | **后端保留协议能力，仅 UI 隐藏**；便于用户从单网域扩展到多网域时无需重新部署 Agent，符合产品交付“全套多网域、按需使用”的定位 | Module_09 3.11 节 |
| `default` 网域是否可编辑/删除？ | **允许修改 `name` / `description`** 以匹配用户云区域命名；**类型定义为 `management`（管理域），禁止删除** | Module_09 4.1 节 `domain_type` 字段 |
| Module_07 在单网域模式下是否隐藏「网域」列？ | **不隐藏**；网域即云区域，是从 CMDB/Excel 代入的必要字段 | Module_07 3.1、5.4 节 |

### 关联文档

- `docs/02-product-requirements/Modules/Module_09_Network_Domain_and_Edge_Config_Center.md`
- `docs/02-product-requirements/Modules/Module_06_Multi_Tenant.md`
- `docs/02-product-requirements/Modules/Module_07_Monitoring_Object_Management.md`

---

## 决策：配置生成文件映射、targets 模式与 Edge Agent 能力边界（2026-08-05）

### 背景

用户基于 Module_09 原型（v1.2）评审提出 5 个设计讨论点，经 Orchestrator 派发技术预研（Prometheus / VictoriaMetrics 官方文档）与两轮讨论后确认以下决策。

### 决策记录

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 1 | 标签模板（`LabelTemplate`）变更触发 **targets 列表 labels 变化**，而非 `prometheus.yml` 结构变化 | 资源标签（CMDB/user/system 来源）在生成 targets 时静态展开到 target labels；`prometheus.yml` 仅含网域级 `global.external_labels`（network_domain/tenant_id，由 NetworkDomain 驱动） | 配置文件映射、3.3 节、配置生成 |
| 2 | **MVP 范围 = 单网域 + 前端自动化下发**：配置生成 → UI 确认 → 自动写文件并 reload 中心 Prometheus；无 Edge Agent 协议暴露（后端保留、UI 隐藏） | 与 3.11 节单网域模式一致；MVP 聚焦中心自动化，隔离域能力延后 | MVP 边界、下发流程 |
| 3 | targets 采用 **file_sd（JSON 文件）**，暂不考虑 http_sd | 政务/金融专网断网风险：file_sd 的 targets 文件落本地磁盘，断网进程存活可继续抓取、**断网重启后本地立即恢复**；http_sd 依赖中心 API 在线、重启后 targets 丢失，不满足弱网自治要求 | 配置包结构、下发链路、3.3 / 6.2 节 |
| 4 | **MVP 即实施 file_sd**（难度中等偏低，增量约 15-20%）。关键设计点：targets 用固定文件名 `targets/<job>.json` + 原子写；**联合 checksum 需纳入 targets 内容**；生成器侧做 JSON schema 校验（`promtool check config` 对 file_sd 内容不校验）；reload 策略分离（仅结构变化才 reload，targets 变化由磁盘监听自动感知） | 最高频变化对象是 targets，file_sd 让 targets 变更免 reload、避免采集抖动；官方定位即"外部系统驱动 targets 的桥接机制" | 数据模型（ConfigDraft/ConfigVersion 承载 targets）、联合 checksum 算法、配置生成 |
| 5 | **Edge Agent 能力分层**：L1（配置同步 + vmagent 控制 + 心跳回执）为 v0.2 核心；**L2（为下游节点安装 exporter）因安全边界暂不纳入 PRD** | L2 扩大 agent 信任半径，涉及凭据管理、供应链、合规审计，需单独立项评审（详见下方风险备注） | Edge Agent 协议、模块边界、PRD 范围 |

### L2 能力风险备注（暂不纳入 PRD 的文字说明）

边缘 Agent 的 L2 能力（为网域内下游节点安装/升级 exporter）涉及安全边界扩展，暂不纳入 PRD 范围。风险点如下：

1. **凭据暴露面扩大**：Agent 需持有下游节点的 SSH/Agent 凭据才能安装 exporter。一旦 Agent 所在主机被攻破，网域内全部监控节点的凭据随之暴露，攻击面从"单个采集节点"放大为"整个网域"。
2. **供应链风险**：Agent 需向网域内节点分发并安装 exporter 二进制，若分发链路缺乏校验/签名，被篡改的恶意二进制将被批量植入所有节点。
3. **敏感配置注入**：部分 exporter 需注入账号密码等敏感配置（如 MySQL/Redis），传输与落盘环节的加密与密钥管理要求随之提高。
4. **合规与审计缺口**：自动化安装减少人工介入，必须提供完整的安装/升级/卸载审计与回执，否则难以满足政务/金融专网的变更审计要求。
5. **与现有流程冲突**：当前 `ExporterInstallationConfirmation` 采用人工确认制；自动化安装将改变该流程（转为"安装回执驱动"），需重新评审后再变更。

**结论**：L2 技术可行但信任半径过大。建议单独立项，先定义凭据管理、分发校验（checksum/签名）、回执模型与审计方案，再考虑进入 PRD。

### 补充决策（2026-08-05 第二轮原型评审）

用户基于原型 v1.4 评审提出配置包形态、targets 扩展性、rules.yml scope 语义、Edge Sync Agent 部署定位等问题，补充确认以下决策：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 6 | **配置产物形态按域类型分层**：中心管理域（default）为**本地文件集**（prometheus.yml + targets/*.json + rules.yml + blackbox.yml），直接写中心 Prometheus 配置目录，确认后 SIGHUP / `-/reload`，无 zip、无 metadata.json 下载校验（版本一致性由 ConfigVersion 记录保证）；**边缘域**为 **zip 配置包**（含 metadata.json 供拉取后 checksum 校验），Agent 心跳拉取。分层依据是域类型（management/edge），而非单/多网域开关 | 单网域模式无 Agent/无拉取，zip 与 metadata.json 仅对边缘拉取有意义 | Module_09 3.11 / 6.2、原型 renderPackageTree |
| 7 | **targets 前端数据驱动、扩展性确认**：`targets/<job_name>.json` 由 configgen 自动按 job 名生成；前端预览的 targets 子 Tab 动态遍历 `targets_files` 数据渲染，**新增 job 无需改前端**（三层解耦：文件命名=后端、展示=数据驱动、用户入口=Module_01/07 策略配置） | 原型 v1.4 已用 `Object.keys(targets_files)` 动态生成子 Tab 验证 | Module_09 3.4 / 4.4、前端展示 |
| 8 | **rules.yml scope 业务场景澄清**：MVP~v0.3 阶段 `scope` 固定 `central`（中心统一求值），用户无需配置 scope；`edge`/`both` 为 v0.4+（P2，由 Module_08 支持）预留，核心场景为**断网自治告警**（边缘 vmalert 本地求值 + 本地通知通道），`both` 用于边缘快速响应 + 中心聚合（需标签区分求值域去重），`central` 用于跨域/全局聚合规则 | Module_01 5.5 scope 定义（MVP~v0.3 固定 central） | Module_01 5.5、Module_09 3.3 |
| 9 | **Edge Sync Agent 部署定位澄清**：Edge Sync Agent 是**部署在边缘监控代理节点的独立客户端程序**，非中心平台内置进程；与中心通过 **outbound HTTPS 443 + 每网域 Token** 通信（心跳 / 配置拉取 / remote_write 全部边缘主动出站，中心无入站端口）；MVP 单网域不部署，v0.2+ 多网域每个边缘节点部署一个 | PRD 3.9 交付方式（离线二进制包 + systemd）与 6.x 协议 | Module_09 3.9、网域注册安装指引 |

### 补充决策（2026-08-05 第三轮评审：中心/边缘校验分层与衔接）

用户提问"配置生成功能的前端展示与 Edge Sync Agent 能力的关系"，确认以下设计：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 10 | **中心/边缘校验分层与衔接**：前端「配置生成/预览」对标的是**中心侧控制**（生成 + 内容校验 + 人工确认），Edge Sync Agent 对标的是**边缘侧消费**（拉取 + 传输校验 + 应用 + 回执），两者由**同一份配置产物（ConfigVersion / zip 包）**衔接，不是"对标 Agent 能力"也不是"另一套独立校验"。校验分两层：**中心①内容校验**（promtool / blackbox --config.check / targets schema，结果以 `validation_status` 在前端展示）防**生成错误**，**边缘②传输校验**（metadata.json checksum 完整性 + targets JSON 解析，PRD 6.3）防**传输损坏/篡改/半写**。设计要点：① Agent 为「哑校验」——不做 promtool 级语法校验（中心已保证产物合法，校验①失败会阻止确认下发）；② 联合 checksum 双用途——中心草稿去重裁决 + 边缘拉包完整性校验；③ `config_sync_status`（in_sync / out_of_sync / manual_override）为 Agent 应用回执，与中心 `validation_status` 构成闭环两端 | 同产物两段链路（中心控制 / 边缘消费）；校验防不同风险；Agent 哑校验降低边缘实现复杂度 | Module_09 6.3 / 5.2 / 3.5.1、校验语义 |

### 补充决策（2026-08-05 第四轮评审：安装指引 / 采集器类型 / Agent 状态页）

用户原型评审提出 3 个问题并经确认：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 11 | **安装指引步骤形式**：Steps 改为**纯人工步骤**（①下载并校验一体化离线包 ②配置 `NETWORK_DOMAIN_ID` / `TOKEN` ③启动 Edge Sync Agent），采集器与 blackbox 的自动部署并入第③步描述，不再单列"Agent 自动部署采集器"为独立步骤，避免"需手动分步装采集器"误解 | 一体化交付语义：采集器由 Agent 启动后自动部署，非人工步骤 | Module_09 3.1 安装指引、原型安装指引 Steps |
| 12 | **MVP 固定 vmagent**：MVP 阶段采集器类型固定仅 `vmagent`（注册表单收起二选一），`prometheus-agent` 保留枚举、v0.2+ 再开放。理由：vmagent 技术上更优（remote_write 流式处理/压缩成熟、内存优化、SD 覆盖全、支持多写入目标；Prometheus Agent Mode 的 remote_write 2.33+ 才稳定） | 用户技术判断 + 降低 MVP 交付面 | Module_09 4.1 agent_type、3.1 网域注册、原型注册表单 |
| 13 | **Agent 状态页：网域筛选 + 维度分组**：多网域模式增加「选择网域」筛选下拉（单网域固定 default）；表格列按维度分组并标注——**Edge Sync Agent 维度**（在线状态、最后心跳、配置同步状态 `config_sync_status`）与**采集器维度**（采集器状态、采集器版本、WAL 积压、remote_write 错误），明确展示对象为"边缘节点 Agent 部署实例（= Edge Sync Agent + 采集器组合，PRD 4.2）" | 用户反馈页面对象不明、缺网域筛选 | Module_09 3.2、原型 EdgeAgentsPage |

### 补充决策（2026-08-06 第五轮评审：注册必要性闭环与 Agent 状态组件分类）

用户对 Module_09 功能设计提出两点疑问：①「网域管理」的「注册网域」手动填写的必要性（若安装完 Edge Sync Agent 可自动抓取，是否只需保留「安装指引」）；②「Agent 状态」的采集器维度为何单一（理论上采集器有 vmagent 也有 blackbox 等，应分类展示）。经分析确认以下决策：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 14 | **注册网域为必要前置步骤（登记制闭环），不可被安装指引替代，但表单最小化**：闭环为「注册（登记制，最小化表单）→ 安装指引（下载离线包 + 注入凭据）→ Agent 心跳自动上线」。理由：① **Token 前置签发**——Edge Sync Agent 启动时必须携带平台签发的 `NETWORK_DOMAIN_ID` + `TOKEN`（PRD 6.3 第 2 条），Token 只能由注册时生成，未预注册网域无法鉴权，谈不上自动抓取；② **自动注册对象是 Agent 实例而非网域**——Agent 首包握手后自动创建的是 `EdgeAgent` 运行态记录，`NetworkDomain`（网络边界 + 租户映射）必须先于 Agent 存在（配置生成按网域分组并注入 external_labels 依赖网域→租户映射）；③ **安全信任锚点**——不做「首包自动建域」（隐式信任问题，且与 v0.4+「网域 = CMDB 云区域 1:1」边界约束冲突）；④ v0.4+ 注册将演化为 CMDB 同步，但 v0.2 前手动注册是唯一来源。**最小化表单**：必填仅网域名称 + 租户，`id` 按租户前缀自动生成、Token 自动签发、**Remote Write URL 由平台自动推导**（中心 ingress + 网域路径，可手动覆盖，减少手动填写项） | 用户疑问①：注册必要性 + 手动填写项过多 | Module_09 3.1 / 3.1.1、原型网域注册表单 |
| 15 | **Agent 状态页「网域为主 + 组件分类」**：边缘监控代理节点由多类组件构成（Edge Sync Agent 必装 + 指标采集器 vmagent/prometheus-agent 必装 + 拨测器 blackbox exporter 可选 + v0.4+ vmalert/alertmanager 可选），Agent 状态页不再以单一「采集器维度」展示，改为**一级表格按网域聚合**（在线 Agent / 采集器运行中 / 拨测器运行中 / 配置同步 / WAL 合计），**展开行按组件类型分类展示**组件实例（组件类型 Tag / 实例 / 所属节点 / 状态 / 版本 / 配置版本 / 最近错误）；组件清单由 Edge Sync Agent 心跳**附带上报**（`EdgeHeartbeat.components` → `EdgeAgent.components`，组件类型 / 版本 / 运行状态 / 配置版本 / 最近错误）；拨测器仅当网域存在 `job_type=blackbox` 的 ScrapeJob 时展示（否则「未部署」）；采集器组件状态 / 版本与顶层 `collector_status` / `collector_version` 保持一致 | 用户疑问②：采集器维度单一，应分类展示（vmagent / blackbox 等） | Module_09 3.2 / 3.8.1 / 3.9 / 4.2 / 4.3、原型 EdgeAgentsPage |

### 补充决策（2026-08-06 第六轮评审：字段对齐 / 安装指引入口 / default 无 Agent / 组件类型筛选）

用户原型评审提出三个问题：① 注册网域表单字段与网域管理列表字段不对齐（状态等信息从哪来），且「安装指引」不应藏在「操作 → 更多」；② Agent 状态 mock 出现 default 网域（PRD 已明确 default 管理域不需要安装 Agent）；③ 筛选条件应增加「组件类型」。经分析确认：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 16 | **四合一修正**：① **default 管理域不部署 Edge Agent**——default 由中心 Prometheus 直接采集，不产生 `EdgeAgent` 实例，「Agent 状态」页仅展示有 Agent 的 `edge` 网域（单网域模式本页空态，PRD 3.11 单网域本隐藏入口）；② **表单与列表字段对齐**——列表字段分**配置字段**（ID/名称/类型/Agent 类型/Token/Remote Write URL，注册或编辑设置）与**运行态字段**（状态/最后心跳，由心跳上报自动更新，注册/安装指引完成前为 unknown/'-'，正常运行明细查「Agent 状态」页）；注册/编辑表单仅维护配置字段，编辑表单补全 PRD 3.1 的可编辑字段（Agent 类型、Remote Write 目标），注册表单 Agent 类型下拉保留、MVP 仅 `vmagent` 可选（`prometheus-agent` 枚举保留、v0.2+ 开放）；③ **安装指引为核心功能**——操作列显式入口（不折叠进「更多」），注册成功自动弹出（闭环）；④ **组件类型筛选**——Agent 状态页支持网域 + 组件类型双筛选，组件类型联动展开明细与统计卡，一级表对应列仅统计匹配组件 | 用户疑问①②③：字段不对齐 + 安装指引被折叠 + default 无 Agent + 缺组件类型筛选 | Module_09 3.1/3.2/3.8.1/3.11/4.2、原型 NetworkDomainsPage / EdgeAgentsPage |

### 补充决策（2026-08-06 第七轮评审：安装指引形态——页面顶部提示区）

用户从易用性出发提出：安装指引不应放在每行（每行入口/弹窗）提示，应做成**页面上的提示**，告知用户该环节的操作流程。经分析确认：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 17 | **安装指引改为页面顶部常驻提示区（而非每行入口/弹窗）**：网域管理页顶部常驻展示「新网域接入操作流程」——**3 步人工步骤**（① 下载并校验一体化离线包 ② 配置 `NETWORK_DOMAIN_ID` / `TOKEN` ③ 启动 Edge Sync Agent（systemd），采集器与 blackbox exporter 自动部署并入第③步）+ **边缘节点组件构成**（Edge Sync Agent 必装 + 采集器 + blackbox exporter 可选）+ **凭据获取方式**（`NETWORK_DOMAIN_ID`=对应网域 ID、`TOKEN` 经网域行内复制按钮获取，UI 完全脱敏）；**行内移除安装指引按钮**（操作列收敛为编辑 / 更多[重置 Token] / 删除），**安装指引弹窗删除**；**注册成功后自动滚动并高亮页面顶部提示区**（替代原「注册成功自动弹出弹窗」），引导用户按流程完成接入 | 3 步流程对所有新注册网域**通用**（仅 NETWORK_DOMAIN_ID/TOKEN 因网域而异，且 TOKEN 行内已可复制），逐行弹窗造成"流程为什么要逐行提示"的困惑；页面常驻提示更符合操作引导（onboarding）语义 | Module_09 3.1、原型 NetworkDomainsPage |

### 补充决策（2026-08-06 第八轮评审：配置变更确认心智——解决「自动生成 vs 人工确认」矛盾）

用户提出：配置生成页展示「源数据版本 / 触发摘要 / 联合 checksum / 生成器版本」等专业技术术语，对不了解 Prometheus 的运维工程师不可理解；用户需要的是做「人工确认」，且既然平台是自动同步，为何还要人工确认——两者看似矛盾。经讨论确认：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 18 | **「配置预览与确认」升级为「配置变更确认」心智（自动生成 + 人工审批，二者不矛盾）**：**平台自动完成**「检测变更 → 生成配置 → 校验 → 过滤无实际影响的变更」（checksum 裁决去重保证"生成内容 = 策略忠实翻译"），**运维负责对进入待确认列表的变更做人工确认（发布审批 go/no-go）**——确认对象是「要不要上线 / 变更影响是否可接受」，而非「配置怎么生成」（类比 CI 自动构建 + 人工发布审批）。界面改造：① **人话变更摘要**（`summary`，如「新增 1 台服务器（10.0.1.11）加入 node-exporter 采集」）为列表主列，回答「为什么变更」；② **结构化变更清单**（`change_items`：变更类型 / 对象 / 说明 / 风险等级），高风险变更（删除目标=监控断点、告警规则变更=误报/漏报）醒目提示；③ **技术字段（源数据版本 / 生成器版本 / 联合 checksum / 触发摘要）下沉折叠**仅供排障；④ 变更检测状态人话化（检测到变更已生成待确认草稿 / 无新变更 / 内容无变化无需确认）；⑤ 页面标题 / 菜单改为「配置变更确认」。**变更摘要生成机制**：由 configgen 对比「当前生效版本」与「新草稿」的**产物数据层 diff** 生成（targets 实例集合 / job 骨架 / 规则对象，非 YAML 文本 diff），**不依赖 Module_01/07 改造**（与 pull 模式解耦）；话术套中文模板；精度边界：规则阈值精确提取（80→85）依赖规则模型结构化程度，MVP 建议规则模型结构化阈值（Module_08 协同），否则退化为「规则表达式已修改」级别 | 用户问题：技术术语不可理解 + 自动同步为何还要人工确认；类比 CI 自动构建 + 人工发布审批；确认的是发布决策而非生成正确性 | Module_09 3.4 / 4.4 / 验收标准、原型 ConfigPreviewPage |

### 补充决策（2026-08-06 第九轮评审：受影响文件高亮 / 历史记录风险与确认人 / 下发记录定位）

用户提出三个优化：① 每次影响变更的配置产物不同，YAML 预览应对受影响配置文件高亮；② 历史变更记录需展示「风险等级」与「确认人」（当前 PRD 未体现）；③ 用户不太关心每次下发记录，讨论下发记录必要性（是否可承担回退作用）。经分析与最佳实践论证确认：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 19 | **三合一优化**：① **受影响配置文件高亮**——配置预览对比当前生效版本自动判定受影响的配置文件（prometheus.yml / targets / rules.yml / blackbox.yml），受影响 Tab 加「变更」标记、**默认聚焦第一个受影响文件**（用户手动切换后不强制跳转）、提示「本次变更影响 N/M 个配置文件」；依据：变更优先呈现（类比 GitHub PR Files Changed / VS Code 变更文件列表）。② **历史变更记录展示风险等级与确认人**——待确认 / 历史列表新增「风险等级」（取该变更最高风险）与「确认人」（confirmed 显示确认人/确认时间，pending 未确认，discarded 已废弃）列，确认动作记录当前登录用户；依据：变更管理（ITIL）审计底线——生产配置变更必须可审计「谁在何时确认了何种风险等级的变更」，支撑变更审查与复盘。③ **下发记录定位为「回滚中心 + 配置变更执行台账」（保留并强化）**——下发记录是**领域业务对象**（状态机 pending/success/failed/rolled_back、可操作回滚、含版本/目标/校验等结构化字段），必要性：变更审计（ITIL）/ 故障回溯（配置最后一次生效时间与结果）/ 回滚支撑（回滚=选择历史 ConfigVersion 重新下发，回滚自身也是记录，类比 K8s rollout history / Git revert）；**与 Module_06 全局审计的边界**：ConfigDeployment 承担**领域审计**（每个网域发过什么版本、结果如何），Module_06 全局审计日志是**平台级横切操作留痕**（actor/action/resource/time，P2，请求级事件由 Module_03 收集），两者联动不重复（下发/回滚动作可写一条审计日志，但领域数据不迁移、互不替代） | 用户三个优化点；变更优先呈现最佳实践；ITIL 变更审计；领域审计 vs 平台审计分层 | Module_09 3.4 / 3.5 / 验收标准、原型 ConfigPreviewPage / DeploymentsPage |

### 补充决策（2026-08-06 第十轮评审：变更单号 / 详情抽屉式 / 检测状态引导性 / 确认人预置）

用户从用户习惯角度提出四个优化：① 历史变更的唯一标识是什么，是否加变更序号；② 变更摘要与变更清单内容上用户分不清，倾向将清单视为变更详情，改为抽屉式；③ 确认人字段 MVP 无用户管理，可预置数据后续同步；④ 变更检测状态是否需要记录历史，页面未体现操作引导流程。经分析确认：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 20 | **四合一优化（用户习惯导向）**：① **变更单号**——`ConfigDraft` 新增 `change_no`（用户可读唯一标识，如 `CHG-YYYYMMDD-NNN`，类比工单号/PR 号），列表 / 详情 / 确认 / 回滚均以变更单号为沟通与审计标识（`id` 保留内部技术键）；依据：ITIL 变更管理每个变更需用户可读的变更单号用于沟通与追溯（"回滚 CHG-xxx"），草稿 ID 是实现细节不可用于沟通。② **变更详情抽屉式**——列表点击变更行打开右侧 Drawer 查看详情：变更清单为详情核心（**摘要=列表总览、清单=抽屉明细，职责分明**消除混淆），配置预览 / Diff / 技术信息折叠 / 确认 / 废弃按钮均收纳于抽屉；依据：审批流 / 工单系统标准模式（列表聚焦待办总览，点击进详情不打断列表上下文）。③ **确认人 MVP 预置**——MVP 阶段确认人 = 预置登录用户上下文（无用户管理），Module_06 用户管理接入后同步真实用户。④ **变更检测状态改为引导性状态、不记录历史**——检测每 30s 一次，记录历史为噪音；定位为引导用户操作的状态说明，与待确认列表联动形成操作引导流（检测到 N 个待确认变更 → 前往下方确认 / 当前无变更 → 策略变更后自动生成），上次检测时间等技术信息折叠供排障 | 用户四个优化点；ITIL 变更单号；审批流/工单抽屉模式；检测状态为实时引导非历史；MVP 边界（无用户管理） | Module_09 3.4 / 4.4 / 验收标准、原型 ConfigPreviewPage |

### 补充决策（2026-08-06 第十一轮评审：状态筛选 / 变更单号生成规则 / 提示分区规范）

用户提出三个问题：① 「待确认 / 历史变更」名称易歧义，是否做成变更状态筛选；② 变更单号是否自动生成、字段是否定义；③ 页面提示哪些给用户看、哪些给产品/技术评审看、哪些给开发（AI）看，如何区分。经分析确认：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 21 | **三合一优化（用户习惯 / 受众分区）**：① **变更列表状态筛选**——按变更状态筛选（待确认 / 已确认 / 已废弃 / 全部，默认待确认，Segmented）替代原「待确认 / 历史」Switch 二分切换；依据：「历史」语义歧义（易理解成历史记录整体），状态是枚举维度，用状态筛选更清晰可扩展。② **变更单号自动生成**——configgen 生成草稿时自动分配（用户不可手填），格式 `CHG-{YYYYMMDD}-{当日序列}`（如 `CHG-20260803-003`），全局唯一；依据：变更单号类比工单号自动编号，用户只引用不创建。③ **提示分区规范（用户 / 产品评审 / 开发 AI 三类受众）**——**用户 UI 文案**：面向运维工程师、不含「决策 X」「PRD X.X」实现层引用（讲人话，看到干净的"未来原型雏形"）；**产品 / 技术评审说明**：设计决策依据与 PRD 引用集中折叠在页面底部「原型与实现说明（面向产品 / 技术评审）」区（默认折叠，用户无感知）；**开发 / AI 注释**：代码注释与 PRD 数据模型 / 技术字段承载实现细节与决策引用，供后续代码开发（含 AI）理解。依据：原型既要给用户看产品雏形，也要给产品/技术评审看设计依据，还要让开发（含 AI）能读懂实现上下文，三类信息混排会互相干扰 | 用户三个问题；状态枚举筛选替代二分；工单号自动编号惯例；原型受众分区最佳实践 | Module_09 3.4 / 4.4 / 验收标准、原型 MainLayout / ConfigPreviewPage / NetworkDomainsPage / EdgeAgentsPage |

### 补充决策（2026-08-07 第十二轮评审：变更对象统一 / 影响的配置文件 / 全链路关联）

用户提出两个问题：① 下发记录「部署 ID」「配置版本」是否自动生成，如何与配置变更关联，便于业务出问题时快速找到可回退的配置版本号回滚——当前两页互不展示对方信息（变更确认页看到变更单号 CHG-xxx，下发记录页看到 cv-xxx / deploy-xxx）；② 变更详情「变更对象」的取值逻辑未统一（自由字符串），配置文件 4 类与变更对象是否对应；targets 变化（新增实例）与抓取频率变化（job 骨架）源头都在 Module_01 采集 Job 功能，仅看变更对象无法区分影响范围，是否加一列「影响的配置文件」。经分析与用户确认：

| # | 决策 | 依据 | 影响范围 |
|---|------|------|----------|
| 22 | **三合一优化（用户易用性 / 变更确认与下发记录关联）**：① **变更对象 = 源数据对象统一枚举 + 影响的配置文件派生列**——「变更对象」统一为源数据对象枚举（采集 Job / 采集目标 / 告警规则 / 拨测目标 / 标签模板，与 Module_01 / 07 功能对象对齐，非配置文件本身），同时新增「影响的配置文件」列（configgen 对比产物差异派生：仅 targets 变化 → `targets/*.json`；job 骨架 / relabel → `prometheus.yml`；规则 → `rules.yml`；blackbox 模块 → `blackbox.yml`）；依据：用户在 Module_01 / 07 修改的根源对象与配置产物是两层语义，新增实例与改抓取频率源头都在「采集 Job」，仅看变更对象无法理解影响范围，双列并排让用户无需理解 Prometheus 文件结构即可判断影响。② **变更单级确认**——确认粒度为整张变更单 go/no-go（一次确认 / 废弃整单），变更清单各行仅作影响信息展示、不逐行确认、不拆分发布；依据：发布审批心智（类比 CI 构建 + 人工发布门禁），逐行确认操作成本高且需拆分产物发布，复杂度显著上升。③ **全链路关联（change_no → cv → deploy 双向可追溯）**——`ConfigVersion` 继承来源 draft 的 `change_no`（4.5 新增字段），`ConfigDeployment` 记录 `source_change_no`（4.6 新增字段，经 cv → change_no 透传）；部署 ID（deploy-xxx）与配置版本号（cv-xxx）均为系统自动生成；变更确认页展示「已发布配置版本」+「查看发布记录」入口（列表「已发布版本」列含「记录」快捷入口），下发记录页新增「来源变更单号」列；回滚中心仍以下发记录页为主（变更页只提供入口不重复实现）；依据：业务出问题时用户路径 = 从变更单找到配置版本号 → 进下发记录页按版本一键回滚，两页双向可追溯消除「不知道回滚哪个版本」的断点 | 用户两个问题；源对象 vs 产物两层语义；发布审批心智；工单/版本/部署三号关联惯例 | Module_09 3.4 / 3.5 / 4.4 / 4.5 / 4.6 / 验收标准、原型 ConfigPreviewPage / DeploymentsPage / MainLayout / mock / test |

## Change Log（完整历史）

> v1.24 起主 PRD Change Log 精简为最近 3 版一句话摘要；本小节承载 v1.21 及以前的逐版完整变更详情（业务沟通决策记录）。

| 版本 | 日期 | 变更类型 | 变更内容 | 影响范围 | 产品版本影响 | 状态 |
|------|------|----------|----------|----------|--------------|------|
| v1.21 | 2026-08-06 | 修改 | 决策 21 落地（用户习惯 / 受众分区）：① **变更列表状态筛选**——3.4 草稿列表改为按变更状态筛选（待确认 / 已确认 / 已废弃 / 全部，默认待确认，Segmented），替代原「待确认 / 历史」二分切换（消除「历史」歧义，状态维度清晰可扩展）；② **变更单号自动生成**——4.4 `change_no` 补充生成规则：configgen 生成草稿时自动分配（用户不可手填），格式 `CHG-{YYYYMMDD}-{当日序列}`（如 `CHG-20260803-003`），全局唯一；③ **提示分区规范**——3.4 新增规范：用户 UI 文案不含「决策 X」「PRD X.X」实现层引用（讲人话）；设计决策依据集中折叠在页面底部「原型与实现说明（面向产品 / 技术评审）」区（默认折叠，用户无感知）；代码注释与 PRD 承载实现细节供开发 / AI 参考；验收标准补充决策 21 条目 | 3.4/4.4/验收标准 | MVP / v0.2 | 设计中 |
| v1.21 | 2026-08-06 | 修改 | 原型 v1.17 同步更新：配置变更确认页「变更列表」改为**状态筛选**（Segmented：待确认 / 已确认 / 已废弃 / 全部，默认待确认）替代原 Switch；**提示分区**——各页面用户可见文案去除「决策 X」「PRD X.X」引用（含网域管理字段语义 / 状态列 Tooltip / Agent 状态页脚 / 配置变更确认抽屉内配置产物与校验说明 / 设计意图 Banner），MainLayout 底部新增「原型与实现说明（面向产品 / 技术评审）」全局折叠区集中承载决策清单与 PRD 指引；代码注释保留决策引用 | 原型目录、UI/UX | MVP / v0.2 | 设计中 |
| v1.20 | 2026-08-06 | 修改 | 决策 20 落地（用户习惯导向）：① **变更单号**——4.4 `ConfigDraft` 新增 `change_no`（用户可读唯一标识，如 `CHG-YYYYMMDD-NNN`，类比工单号/PR 号），列表 / 详情 / 确认 / 回滚以变更单号为沟通与审计标识（`id` 保留内部技术键）；② **变更详情抽屉式**——3.4 变更详情改为右侧抽屉（变更清单为详情核心，摘要=列表总览、清单=抽屉明细职责分明，配置预览 / Diff / 技术信息 / 确认废弃按钮均收纳于抽屉）；③ **变更检测状态改为引导性状态**——不记录检测历史（30s 轮询记录为噪音），与待确认列表联动形成操作引导流（检测到 N 个待确认变更 → 前往下方确认 / 当前无变更 → 自动生成提示），上次检测时间等技术信息折叠；④ **确认人 MVP 预置**——确认动作记录预置登录用户上下文，Module_06 用户管理接入后同步真实用户；验收标准补充决策 20 条目 | 3.4/4.4/验收标准 | MVP / v0.2 | 设计中 |
| v1.20 | 2026-08-06 | 修改 | 原型 v1.16 同步更新：配置变更确认页——列表新增「变更单号」主标识列（CHG-20260803-xxx），点击行打开**右侧抽屉**查看变更详情（抽屉标题=变更单号+状态/风险/校验标签+摘要，变更清单为核心，配置预览/diff、技术信息折叠、确认/废弃按钮于抽屉操作区），移除原列表下方堆叠详情区；变更检测状态改为**引导性状态条**（检测到 N 个待确认变更含高风险数 / 当前无待确认变更），技术信息折叠；确认动作以变更单号 + 预置确认人提示 | 原型目录、数据模型、UI/UX | MVP / v0.2 | 设计中 |
| v1.19 | 2026-08-06 | 修改 | 决策 19 落地：① **受影响配置文件高亮**——3.4「配置预览」对比当前生效版本自动判定受影响的配置文件（prometheus.yml / targets / rules.yml / blackbox.yml），受影响 Tab 加「变更」标记、默认聚焦第一个受影响文件、提示「本次变更影响 N/M 个配置文件」（每次变更影响产物不同，变更优先呈现）；② **历史变更记录展示风险等级与确认人**——3.4 草稿列表新增「风险等级」（取该变更最高风险）与「确认人」（confirmed 显示确认人/确认时间，pending 未确认，discarded 已废弃）列，确认动作记录当前登录用户，支撑变更审计复盘「谁确认了高风险变更」；③ **下发记录定位为回滚中心 + 配置变更执行台账**——3.5 明确下发记录必要性（变更审计 ITIL / 故障回溯 / 回滚支撑，回滚=选择历史 ConfigVersion 重新下发且回滚自身也是记录，类比 K8s rollout history / Git revert），并澄清与 Module_06 全局审计的边界（ConfigDeployment=领域业务对象承担领域审计 vs Module_06=平台级横切操作留痕，联动不重复）；版本回滚优先级提升为 P0；验收标准补充决策 19 条目 | 3.4/3.5/验收标准 | MVP / v0.2 | 设计中 |
| v1.19 | 2026-08-06 | 修改 | 原型 v1.15 同步更新：配置变更确认页——受影响文件 Tab 加「变更」Tag 高亮、默认聚焦第一个受影响文件（用户手动切换后不强制跳转）、标题旁提示「本次变更影响 N/M 个配置文件」；待确认/历史列表新增「风险等级」「确认人」列（确认动作记录当前登录用户「张伟（运维）」）；下发记录页标题改为「配置发布与回滚记录」并新增定位 Alert（回滚中心 + 变更执行台账 + 与 Module_06 全局审计边界说明） | 原型目录、UI/UX | MVP / v0.2 | 设计中 |
| v1.18 | 2026-08-06 | 修改 | 决策 18 落地：**「配置预览与确认」升级为「配置变更确认」心智**（面向不了解 Prometheus 的运维工程师，解决「自动同步 vs 人工确认」矛盾）——明确职责定位：平台自动完成「检测变更 → 生成配置 → 校验 → 过滤无实际影响的变更」（自动生成），运维对进入待确认列表的变更做**人工确认（发布审批 go/no-go）**，确认对象是「要不要上线 / 影响是否可接受」而非「配置怎么生成」；3.4 新增**变更摘要（人话）**与**变更清单（结构化 + 风险等级）**功能，高风险变更（删除目标 / 告警规则变更）醒目提示；**技术字段（源数据版本 / 生成器版本 / 联合 checksum / 触发摘要）下沉折叠**仅供排障；变更检测状态人话化（检测到变更已生成待确认草稿 / 无新变更 / 内容无变化无需确认）；新增「变更摘要生成机制」说明（产物数据层 diff 生成话术，不依赖 Module_01/07 改造，含精度边界：规则阈值精确提取依赖规则模型结构化程度）；4.4 `ConfigDraft` 新增 `summary` / `change_items` 字段；验收标准补充决策 18 条目 | 3.4/4.4/验收标准 | MVP / v0.2 | 设计中 |
| v1.18 | 2026-08-06 | 修改 | 原型 v1.14 同步更新：菜单与页面标题改为「配置变更确认」；待确认列表以**变更摘要（人话）为主列** + 「含高风险变更」标签（移除源数据版本 / 触发摘要 / 联合 checksum 列）；详情新增「变更清单（本次变更的影响）」表格（变更类型 / 对象 / 说明 / 风险等级）；技术信息（源数据版本 / 生成器版本 / 联合校验值 / 触发摘要 / 来源摘要）折叠展示；变更检测状态人话化 + 技术信息折叠；页面顶部删除 pull 模式 / checksum 算法等技术说明，改为人话「本页确认什么」（自动生成 + 人工审批职责边界）；mock 新增 draft-gov-002（告警规则阈值 80→85，high 风险）演示，`ConfigDraft` 新增 `summary` / `change_items` 字段 | 原型目录、数据模型、UI/UX | MVP / v0.2 | 设计中 |
| v1.17 | 2026-08-06 | 修改 | 决策 17 落地：**安装指引由「每行入口/弹窗」改为「页面顶部常驻提示区」**（用户易用性）——网域管理页顶部常驻展示「新网域接入操作流程」（3 步人工步骤 + 边缘节点组件构成 + 凭据获取方式：`NETWORK_DOMAIN_ID`=对应网域 ID、`TOKEN` 经网域行内复制按钮获取），流程对所有新注册网域通用、无需逐行提示；**行内移除安装指引按钮**（操作列收敛为编辑 / 更多[重置 Token] / 删除）；**注册成功后自动滚动并高亮页面顶部提示区**（替代原「注册成功自动弹出弹窗」）；3.1 网域注册/安装指引行与验收标准同步更新 | 3.1/验收标准 | MVP / v0.2 | 设计中 |
| v1.17 | 2026-08-06 | 修改 | 原型 v1.13 同步更新：网域管理页顶部新增「新网域接入操作流程（安装指引）」常驻提示区（Alert + 3 步 Steps + 组件构成列表 + 凭据获取与交付说明），行内「安装指引」按钮移除（操作列仅「编辑 / 更多（重置 Token）/ 删除」）、安装指引弹窗删除；注册成功后自动滚动并高亮顶部提示区（guideHighlight 描边高亮 4s） | 原型目录、UI/UX | MVP / v0.2 | 设计中 |
| v1.16 | 2026-08-06 | 修改 | 决策 16 落地：① **default 管理域不部署 Edge Agent**——3.11 / 4.2 明确 default 由中心直接采集、不产生 `EdgeAgent` 实例，「Agent 状态」页仅展示有 Agent 的 `edge` 网域（单网域模式本页空态）；② **表单与列表字段对齐**——3.1 新增「字段语义」说明（配置字段 vs 运行态字段：状态 / 最后心跳由心跳上报自动更新、注册阶段为 unknown / '-'），注册表单 Agent 类型改为下拉保留、MVP 仅 vmagent 可选，编辑表单补全 PRD 3.1 要求的可编辑配置字段（Agent 类型、Remote Write 目标），运行态字段仅列表展示并标注来源；③ **安装指引入口提升**——操作列显式入口（不折叠进「更多」）+ 注册成功自动弹出（闭环）；④ **Agent 状态页组件类型筛选**——3.2 / 3.8.1 支持网域 + 组件类型双筛选，组件类型联动展开明细与统计卡、一级表对应列仅统计匹配组件；验收标准补充决策 16 条目 | 3.1/3.2/3.8.1/3.11/4.2/验收标准 | MVP / v0.2 | 设计中 |
| v1.16 | 2026-08-06 | 修改 | 原型 v1.12 同步更新：mock 删除 default 域 Agent（ea-default），补充「default 管理域不产生 EdgeAgent」断言；Agent 状态页仅展示有 Agent 的网域（单网域模式空态 Alert），新增**组件类型筛选下拉**（联动展开明细与统计卡，统计卡随筛选动态展示），网域下拉只列有 Agent 的网域；网域管理页「安装指引」提升为操作列显式按钮 + 注册成功自动弹出，注册表单 Agent 类型改为下拉（MVP 仅 vmagent 可选、编辑可改），编辑表单补全 Agent 类型 / Remote Write URL，列表「状态 / 最后心跳」列头 Tooltip 标注运行态字段来源，页脚补充字段语义说明 | 原型目录、数据模型、UI/UX | MVP / v0.2 | 设计中 |
| v1.15 | 2026-08-06 | 修改 | 决策 14/15 落地：新增 3.1.1「注册 → 安装指引 → 自动上线（登记制闭环）」——明确「注册网域」为必要前置步骤不可被「安装指引」替代（Token 前置签发 / Agent 自动注册的是实例而非网域 / 安全信任锚点 / v0.4+ 演化为 CMDB 同步），注册表单最小化（必填仅网域名称 + 租户，id 自动生成、Token 自动签发、**Remote Write URL 由平台自动推导**可手动覆盖）；3.1「网域注册」行同步补充登记制与自动推导语义；3.2「Agent 状态列表页」/ 3.8.1 升级为**「网域为主 + 组件分类」**结构（一级按网域聚合：在线 Agent / 采集器运行中 / 拨测器运行中 / 配置同步 / WAL 合计；展开行按组件类型分类展示组件实例：Edge Sync Agent / 指标采集器 / 拨测器 blackbox exporter / v0.4+ vmalert / alertmanager）；3.9 补充「组件清单随心跳上报」；4.2 `EdgeAgent` 新增 `components` 字段、4.3 `EdgeHeartbeat` 新增 `components` 字段、6.1 心跳示例同步；验收标准补充登记制闭环与组件分类条目 | 3.1/3.2/3.8.1/3.9/4.2/4.3/6.1/验收标准 | MVP / v0.2 | 设计中 |
| v1.15 | 2026-08-06 | 修改 | 原型 v1.11 同步更新：Agent 状态页改为「网域为主 + 组件分类」——一级表格按网域聚合（网域名称+类型 Tag / 在线 Agent x/y / 采集器运行中 x/y / 拨测器运行中 x/y 或未部署 / 配置同步 / WAL 合计 / 最后心跳），展开行按组件类型分类展示组件明细子表（组件类型 Tag + Tooltip / 组件实例 / 所属节点 / 状态 / 版本 / 配置版本 / 最近错误），统计卡改为「网域数 / 在线 Agent / 采集器运行中 / 拨测器运行中」；EdgeAgent mock 新增 `components` 组件清单（Edge Sync Agent 必装 + 采集器必装 + blackbox exporter 仅 blackbox job 网域附带）；网域注册表单 Remote Write URL 改为**留空自动推导**（中心 ingress + 网域路径，决策 14），注册表单新增「注册 → 安装指引 → 自动上线」闭环 Alert；新增 `deriveRemoteWriteUrl` helper 与对应测试 | 原型目录、数据模型、UI/UX | MVP / v0.2 | 设计中 |
| v1.14 | 2026-08-05 | 修改 | 决策 11-13 落地：3.1「安装指引」改为 **3 步人工步骤**（① 下载并校验一体化离线包 ② 配置 `NETWORK_DOMAIN_ID` / `TOKEN` 环境变量 ③ 启动 Edge Sync Agent（systemd）），采集器与 blackbox exporter 自动部署并入第③步描述、不单列为人工步骤；3.1「网域注册」/ 4.1 `agent_type` 明确 **MVP 阶段采集器类型固定 `vmagent`**（注册时无需选择）、`prometheus-agent` 保留枚举、v0.2+ 开放为可选；3.2「Agent 状态列表页」补充多网域模式**按网域筛选**（单网域模式固定 default）与**列按维度分组**（Edge Sync Agent 维度：在线状态、最后心跳、配置同步状态 `config_sync_status`；采集器维度：采集器状态、采集器版本、WAL 积压、remote_write 错误），页面对象为边缘节点 Agent 部署实例（Edge Sync Agent + 采集器组合，PRD 4.2）；验收标准补充对应条目 | 3.1/3.2/4.1/验收标准 | MVP / v0.2 | 设计中 |
| v1.14 | 2026-08-05 | 修改 | 原型 v1.10 同步更新：安装指引 Steps 改为 3 步人工步骤（下载校验一体化离线包 → 配置环境变量 → 启动 Edge Sync Agent，采集器/blackbox 自动部署并入第③步）；注册表单采集器类型收起、固定 vmagent（Tag + 说明「MVP 固定 vmagent；prometheus-agent v0.2+ 开放」）；Agent 状态页新增「选择网域」筛选下拉（复用 currentTenant.multi_site_enabled，单网域固定 default）、列按维度分组（表头分组标题 + Tooltip）、统计卡随筛选联动、页脚补充展示对象说明（边缘节点 Agent 部署实例 = Edge Sync Agent + 采集器组合） | 原型目录、数据模型、UI/UX | MVP / v0.2 | 设计中 |
| v1.13 | 2026-08-05 | 修改 | 新增 6.4「中心/边缘校验分层与衔接」（决策 10 固化）：同产物两段链路关系图（中心侧控制 → 配置产物 → 边缘侧消费）+ 分层校验对照表（中心①内容校验防生成错误、边缘②传输校验防传输损坏/篡改/半写），明确设计要点（Agent 哑校验不做 promtool 级语法校验、联合 checksum 双用途、config_sync_status 与 validation_status 构成闭环两端）；3.5.1 补充交叉引用（中心内容校验与边缘传输校验的分层关系见 6.4）；5.2 时序步骤后补充指向 6.4 的引用；验收标准补充校验分层条目 | 6.3/6.4/3.5.1/5.2/验收标准 | MVP / v0.2 | 设计中 |
| v1.13 | 2026-08-05 | 修改 | 原型 v1.9 同步更新：配置生成/预览页补充「校验分层（PRD 6.4）」说明（中心①内容校验结果以 validation_status 展示、失败阻止确认下发；边缘②传输校验体现于 Agent 状态页 config_sync_status；Agent 哑校验；联合 checksum 双用途），校验失败 Alert 与 mfg 域 targets schema 失败案例文案标注「中心内容校验失败」；Agent 状态页新增「采集器版本」列（collector_version，EdgeAgent.version 保留为 Edge Sync Agent 版本经 Tooltip 展示，PRD 4.2 组合语义）与「采集器状态」列（collector_status：running/stopped/unknown，Tooltip 说明本节点采集器由 Edge Sync Agent 部署守护），页脚补充边缘传输校验与采集器进程管理（PRD 3.2/6.3）说明；EdgeAgent 数据模型新增 collector_version / collector_status 字段 | 原型目录、数据模型、UI/UX | MVP / v0.2 | 设计中 |
| v1.12 | 2026-08-05 | 修改 | Edge Sync Agent 职责范围明确（三层：① 通信层—心跳上报、配置拉取 checksum 校验、状态回执；② 配置应用层—写配置、原子替换、触发采集器/blackbox reload；③ 本节点组件管理层—一体化交付后管理本节点采集器与 blackbox exporter 进程生命周期）；3.2 新增「采集器进程管理」功能（P0，随一体化离线包安装、启动守护、健康检查、异常自动重启，版本/运行状态纳入心跳上报与 Agent 状态展示）；3.9 补充「一体化交付 + 职责边界」（离线二进制包为一体化包，安装后 Agent 自动部署本节点采集器/blackbox 进程，启动顺序 blackbox → 采集器，不做下游节点 exporter 安装）；6.3 新增第 1 条本节点组件部署守护行为（保留手动兜底），原有编号顺延；安装指引改为一步安装语义（原型 v1.8 同步更新）；验收标准补充一体化交付条目 | 3.2/3.9/6.3/验收标准 | MVP / v0.2 | 设计中 |
| v1.11 | 2026-08-05 | 修改 | 原型 v1.7 同步更新（UI 修复）：3.1「Token 管理」补充 Token 在 UI 中完全脱敏展示（不显示任何明文片段，含首尾 6 位），完整值仅可通过「复制」按钮获取；网域列表操作列收敛为「编辑 / 更多（安装指引、重置 Token）/ 删除」，避免操作按钮超出表格宽度；验收标准补充 Token 完全脱敏条目 | 3.1/验收标准 | MVP / v0.2 | 设计中 |
| v1.11 | 2026-08-05 | 修改 | 原型 v1.6 同步更新：3.1 新增「安装指引」功能（P1，离线交付方式/校验和/`NETWORK_DOMAIN_ID`/`TOKEN` 环境变量/systemd 部署步骤/blackbox exporter 附带说明）；3.9 补充「边缘节点组件构成」说明段（边缘监控代理节点 = Edge Sync Agent 必装独立组件（非中心内置，负责心跳/配置拉取/控制采集器）+ 采集器（vmagent 或 prometheus-agent，由 `NetworkDomain.agent_type` 登记，二选一）+ blackbox exporter（可选，blackbox job 时附带）；明确注册时登记的 `agent_type` 是采集器类型，Edge Sync Agent 为必装组件、无需登记）；4.2 补充 EdgeAgent 模型语义（实例代表「边缘节点上的 Agent 部署 = Edge Sync Agent + 采集器组合」，`agent_type` 为采集器类型，心跳由 Edge Sync Agent 上报携带采集器类型/版本/config_version/WAL 积压）；验收标准补充安装指引与组件构成条目 | 3.1/3.9/4.2/验收标准 | MVP / v0.2 | 设计中 |
| v1.10 | 2026-08-05 | 修改 | 原型 v1.5 同步更新：配置产物形态按域类型分层（3.11/6.2：中心管理域 default=本地文件集、无 zip/metadata.json，边缘域=zip 配置包含 metadata.json，分层依据为域类型而非单/多网域开关）；3.9 补充 Edge Sync Agent 部署定位（边缘监控代理节点独立客户端程序、outbound HTTPS 443 + 每网域 Token 通信、中心无入站端口，MVP 单网域不部署、v0.2+ 多网域每边缘节点一个）；3.3 补充 scope 业务场景（MVP~v0.3 固定 central，edge/both 为 v0.4+ P2 断网自治告警预留，链接 Module_01 5.5）；3.4 补充 targets 前端数据驱动说明（targets/<job_name>.json 由 configgen 生成，前端动态遍历 targets_files 渲染，新增 job 无需改前端）；验收标准补充配置产物形态分层条目 | 3.3/3.4/3.9/3.11/6.2/验收标准 | MVP / v0.2 | 设计中 |
| v1.9 | 2026-08-05 | 修改 | 原型 v1.4 同步更新：3.4 草稿列表显式展示所属网域并定义默认仅展示待确认（pending）草稿、历史草稿（confirmed/discarded）可切换查看、「人工确认下发」仅对 pending 生效；3.3.3 补充变更检测状态可观测（上次检测时间、源数据版本、检测结果，P1）；验收标准补充草稿默认待确认过滤与检测状态可观测条目 | 3.3/3.4/验收标准 | MVP / v0.2 | 设计中 |
| v1.8 | 2026-08-05 | 修改 | 原型 v1.3 同步更新：配置预览改为多文件 Tabs（prometheus.yml / targets / rules.yml / blackbox.yml / metadata.json）并按文件 diff；配置包结构树与预览含 `targets/` 目录（file_sd 目标文件）；targets_files 数据模型落地（按 job 组织、文件_sd 骨架、联合 checksum 纳入 targets 内容）；校验失败定位到对应文件 Tab；网域注册新增安装指引（离线交付 + systemd + 环境变量 + 心跳认领三步）；Agent 状态列表新增 Agent IP 列（心跳登记） | 原型目录、UI/UX | 文档自身 | 设计中 |
| v1.8 | 2026-08-05 | 修改 | targets 采用 file_sd（JSON 目标文件）：3.3「按网域生成配置」明确 scrape_configs 以 `file_sd_configs` 引用本域 `targets/*.json`（固定文件名覆盖写），prometheus.yml 仅含 job 骨架；3.3.2 blackbox 目标同样写入 targets JSON；新增「配置文件 × 源数据」映射语义（prometheus.yml=网域级+job 结构级，targets=资源级+标签模板级，LabelTemplate 变更驱动 targets labels 而非 prometheus.yml 结构）；联合 checksum 纳入 targets 内容（sha256(prometheus.yml+rules_yml+blackbox_yml+targets 内容)）；ConfigDraft / ConfigVersion 新增 `targets_files` 承载字段；configgen 侧新增 targets JSON schema 校验并说明 promtool 对 file_sd 只查存在性的缺口；3.5 明确 targets 原子写与 reload 策略分离（仅结构变化触发 reload），6.3 补充 Edge Agent 解压后 targets 解析校验失败回滚、targets 更新不触发 reload；3.4 配置预览扩展为多文件预览与按文件 diff；http_sd 记录为未来演进选项（v0.4+），因弱网自治要求暂不采用 | 配置生成、配置包结构、数据模型、下发流程、验收标准 | MVP / v0.2 | 设计中 |
| v1.7 | 2026-08-04 | 修改 | 确认设计决策 12 规则作用域分层（scope）：3.3「按网域生成配置」中 `rules.yml` 按 `scope` 生成的语义已于 v1.6 澄清（中心域含 `central`/`both`，边缘域 v0.4+ 含 `edge`/`both`），与 Module_01 5.5 `MonitoringRule.scope` 字段及网域无关性说明一致；本轮仅记录确认、正文无需再调整 | 配置生成 | MVP / v0.2 | 设计中 |
| v1.6 | 2026-08-04 | 修改 | 澄清 3.3「按网域生成配置」中 `rules.yml` 的生成语义：规则由中心统一求值，`rules.yml` 按规则作用域生成（中心域含 `central`/`both`，边缘域仅当存在 `edge`/`both` 规则时 v0.4+ 随包下发），避免与「规则不绑网域」设计冲突 | 配置生成 | MVP / v0.2 | 设计中 |
| v1.5 | 2026-08-04 | 修改 | 澄清变更检测为 pull 模式：Module_09 异步轮询（默认 30s）检测 Module_01/07 各源表 `updated_at` 变化，Module_01/07 不主动通知、不感知 Module_09，写库即完成职责；补充 5.2 变更检测与配置生成全链路时序图（人工确认为唯一同步环节）；7.1 边界表新增「变更检测/配置生成触发」行；验收标准新增轮询触发与检测延迟约束 | 配置生成、模块边界、下发时序、验收标准 | MVP / v0.2 | 设计中 |
| v1.4 | 2026-08-04 | 修改 | 明确变更检测采用「源数据版本触发预筛 + 生成后联合 checksum 裁决」混合机制，差异检测优先级从 P1 提升为 P0；新增 3.3.3 变更检测与草稿去重说明；细化 `ConfigDraft` / `ConfigVersion` metadata 字段（`source_data_version`、`trigger_summary`、联合 checksum）；明确 Edge Agent 拉取包 checksum 完整性校验行为 | 配置生成、数据模型、下发时序、验收标准 | MVP / v0.2 | 设计中 |
| v1.3 | 2026-08-04 | 修改 | 明确 blackbox exporter 同域部署：配置包按需包含 `blackbox.yml`；blackbox job 生成 `metrics_path=/probe` 的 scrape_config 并将 `__address__` 指向本地 blackbox exporter；增加下发前 `promtool` / `--config.check` 校验与 Edge Sync Agent 触发 blackbox 重载要求；补充离线交付物中 blackbox 二进制、capability 与 systemd 依赖说明 | 配置生成、下发流程、交付物、数据模型、验收标准 | MVP / v0.2 | 设计中 |
| v1.2 | 2026-08-03 | 修改 | PRD 状态从 ready 修正为 设计中：尚未完成原型验证 | PRD 状态 | 文档自身 | 设计中 |
| v1.2 | 2026-08-03 | 修改 | 新增 3.11 节：明确单网域/多网域模式行为；将开关从平台级 feature flag 调整为租户级 `Tenant.multi_site_enabled`；`default` 网域新增 `domain_type=management` 且允许修改名称/描述、禁止删除；Edge Sync Agent 协议后端保留能力、UI 隐藏 | 功能范围、UI/UX、MVP 边界、数据模型 | MVP / v0.2 | 设计中 |
| v1.1 | 2026-08-02 | 新增 | 完成 Volcengine 风格原型验证，输出独立可点击原型 | PRD 状态、UI/UX、原型目录 | 文档自身 | 设计中 |
| v1.0 | 2026-07-31 | 初始 | 模块 PRD 初始版本 | 全部 | MVP / v0.2 / v1.0 | draft |

## 评审记录

### 第十三轮评审（2026-08-07）：PRD v1.22 vs 原型 v1.18 一致性核查 + PRD 骨架规范落地

- **参与方**：chenrt（产品 + 架构师）/ prototype-designer
- **第一段用户走查结论**：PRD 功能清单（3.1~3.11）与原型页面（NetworkDomains / EdgeAgents / ConfigPreview / Deployments）逐项对齐，用户任务闭环；用户可见文案干净（决策 21 已清理）；「变更确认 ↔ 下发记录」全链路关联（决策 22）双向可追溯验证通过
- **第二段技术核对结论**：
  - 数据模型一致：`change_no` / `affected_files` / `source_change_no` 与 mock 契约一致，测试 40 条覆盖；
  - **发现并修复**：4.6 `ConfigDeployment` 字段表缺失 `validation_status` / `includes_blackbox`（原型已实现、PRD 表漏列），v1.23 补齐；
  - **按新 PRD 骨架规范补齐**：4.1~4.6 字段表新增「UI 展示名」列、新增第 10 章「术语映射（用户词汇表）」、验收标准分层（9.1 用户 / 9.2 技术）
- **问题清单**：4.6 字段缺失（已修复）；PRD 骨架规范缺口（已修复）
- **遗留项**：3.8.2 边缘诊断看板（P1/P2）未实现（MVP 范围外占位）；PRD 骨架规范（UI 展示名 / 术语映射 / 验收分层）需推广到 Module_01/02/07 等其他模块 PRD

### 关联文档

- `docs/02-product-requirements/Modules/Module_09_Network_Domain_and_Edge_Config_Center.md`
- `docs/prototypes/module-09/`
- 技术预研依据：
  - Prometheus file_sd：https://prometheus.io/docs/prometheus/latest/configuration/configuration/#file_sd_config
  - Prometheus file-sd 官方指南：https://prometheus.io/docs/guides/file-sd/
  - Prometheus http_sd：https://prometheus.io/docs/prometheus/latest/http_sd/
  - VictoriaMetrics Service Discovery：https://docs.victoriametrics.com/sd_configs/
