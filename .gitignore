# MetricCenter Git Ignore

# 自动下载的工具链
# 注意：不要写成 `.tools/`（尾斜杠只匹配目录）。本仓库的 `.tools` 常是指向兄弟工作树
# 的符号链接，符号链接不是目录 → 会漏忽略，`git status` 长期显示 `?? .tools`，
# 且 `git add -A` 会把该软链提交进版本库。
.tools

# 构建产物
platform/cmd/metric-center/metric-center
upstream/prometheus/prometheus
upstream/prometheus/promtool
upstream/node_exporter/node_exporter
*.exe
dist/

# UI 构建产物
ui-custom/web/node_modules/
ui-custom/web/dist/
ui-custom/web/.vite/
ui-custom/web/coverage/

# 原型项目构建产物与依赖
docs/prototypes/*/node_modules/
docs/prototypes/*/dist/
docs/prototypes/*/.vite/
docs/prototypes/*/coverage/
docs/prototypes/_site/

# Go 构建缓存
*.o
*.a
*.so
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# 日志
*.log

# SQLite 运行时数据库
metric_center.db
*.db

# 临时文件
/tmp/
*.tmp
tmp_test/
_gcc_make.zip

# WorkBuddy 项目本地元数据（记忆/自动化等，不纳入版本控制）
.workbuddy/

# Python 缓存（scripts/ 下的检查脚本运行时产生）
__pycache__/
*.pyc

# M09 local 下发产物与 Prometheus TSDB 运行时数据（make run-prometheus 生成）
config-output/
data/
